# Team notes

Run `npm test` before pushing.

<!-- BEGIN AGILEFLOW MANAGED BLOCK -->
<!-- This region is generated and maintained by AgileFlow. It is refreshed on every install. Do not edit inside the markers; put your own content OUTSIDE this block so it is preserved. -->

# AgileFlow

This file is read by agentic coding tools that support a root `AGENTS.md`
(Cursor, Codex, Copilot, Windsurf, Gemini, Zed, Cline, and others). It
carries the same working preferences AgileFlow applies inside a session.

## User Preferences (agileflow light mode)

Apply these preferences throughout this session across all skills and tasks:

- **Ask at decision points** — end responses with AskUserQuestion (specific options, one marked Recommended). Never generic "Continue?".
- **Plan mode for non-trivial work** — EnterPlanMode before implementing anything beyond a one-liner.
- **Track progress** — TaskCreate for 3+ step tasks, TaskUpdate as each completes.

To change: run `agileflow setup`.
<!-- END AGILEFLOW MANAGED BLOCK -->
