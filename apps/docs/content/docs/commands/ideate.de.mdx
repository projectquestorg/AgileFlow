---
title: /ideate
description: Generieren Sie kategorisierte Verbesserungsideen mithilfe einer Multi-Experten-Analyse
---
# /ideate

Generieren Sie Verbesserungsideen, indem Sie mehrere Domänenexperten parallel einsetzen, um Ihre Codebasis aus verschiedenen Perspektiven zu analysieren (Sicherheit, Leistung, Codequalität, UX, Tests, API/Architektur).

## Schnellstart

```bash
/agileflow:ideate SCOPE=all DEPTH=quick OUTPUT=report
```

## Parameter

| Parameter | Erforderlich | Standard | Beschreibung |
|-----------|----------|---------|-------------|
| `SCOPE` | Nein | `all` | Welche Domänen analysiert werden sollen: `all`, `security`, `perf`, `code`, `ux` |
| `DEPTH` | Nein | `quick` | Analysetiefe: `quick` (3 Ideen), `deep` (5 Ideen) |
| `OUTPUT` | Nein | `report` | Ausgabetyp: `report` (nur Ideenfindung), `stories` (Storys erstellen), `both` |

## Beispiele

### Alle Domänen analysieren – Quick Scope

```bash
/agileflow:ideate SCOPE=all DEPTH=quick OUTPUT=report
```

Setzt 6 Experten ein (Sicherheit, Leistung, Refactor, UI, Tests, API), um jeweils 3 Verbesserungsideen zu generieren. ReErgibt einen Ideenbericht mit Ideen mit hohem/mittlerem Vertrauen.

### Tiefgreifende Sicherheitsanalyse

```bash
/agileflow:ideate SCOPE=security DEPTH=deep OUTPUT=stories
```

Konzentriert sich auf Sicherheit mit eingehender Analyse (5 Ideen pro Experte). Erstellt sofort Geschichten für besonders vertrauenswürdige Artikel.

### Leistungsanalyse mit Bericht und Geschichten

```bash
/agileflow:ideate SCOPE=perf DEPTH=deep OUTPUT=both
```

Analysiert die Leistung mit Experten (Leistung, Datenbank, API), erstellt Berichte und erstellt Implementierungsgeschichten.

## Bereichszuordnung

| UMFANG | Experten im Einsatz | Am besten für |
|-------|----|----------|
| `all` | Sicherheit, Leistung, Refaktorierung, Benutzeroberfläche, Tests, API | Umfassende Analyse (6 Experten) |
| `security` | Sicherheit, API, Testen | Sicherheitsüberprüfung (3 Experten) |
| `perf` | Leistung, Datenbank, API | Leistungsoptimierung (3 Experten) |
| `code` | Refactoring, Testen, API | Codequalität (3 Experten) |
| `ux` | Benutzeroberfläche, Barrierefreiheit, API | Benutzererfahrung (3 Experten) |

## Ausgabestruktur

### Ideenbericht (`output.md`)

Der Befehl generiert `docs/08-project/ideation-<YYYYMMDD>.md` mit:

1. **Zusammenfassung**: Gesamtideen, Vertrauensverlust
2. **Verbesserungen mit hohem Vertrauen**: Ideen, die von mehr als zwei Experten vereinbart wurden (diese priorisieren)
3. **Möglichkeiten mit mittlerem Vertrauen**: Einzelner Experte mit Beweisen (es lohnt sich, sie zu erkunden)
4. **Zusammenfassung nach Kategorie**: Tabelle der Ideenanzahl pro Domain
5. **Empfohlene nächste Schritte**: Implementierungsleitfaden

### Vertrauensbewertung

| Vertrauen | Kriterien | Aktion |
|------------|----------|--------|
| **HOCH** | 2+ Experten schlagen ähnliche Idee vor | An prominenter Stelle einbinden, sofortiges Handeln empfehlen |
| **MITTEL** | 1 Experte mit spezifischen Beweisen (Dateipfade, Metriken) | Als Chance einbeziehen |
| **NIEDRIG** | 1 Experte, vage/keine Beweise | Aus Bericht ausschließen |

### Beispielberichtsabschnitt

```markdown
# Ideation Report

**Generated**: 2025-12-30
**Scope**: all
**Depth**: quick
**Total Ideas**: 15 (High: 4, Medium: 8, Low: 3)

## 🎯 High-Confidence Improvements
*Agreed by multiple experts - prioritize these*

### 1. Add request rate limiting to API
**Category**: Security | **Impact**: High | **Effort**: 1-2 days
**Experts**: Security, API
**Why**: Prevents DDoS attacks and brute force attempts
**Files**: `src/api/middleware/auth.ts`, `src/api/routes/`

## 💡 Medium-Confidence Opportunities
*Single expert with evidence - worth exploring*

### 5. Implement caching layer for database queries
**Category**: Performance | **Impact**: High | **Effort**: 2-3 days
**Expert**: Performance
**Why**: Your homepage renders 200+ components, this would fix 2-second load time
**Files**: `src/db/queries.ts`, `src/cache/`
```

### Generierte Geschichten (optional)

Wenn `OUTPUT=stories` oder `OUTPUT=both`, werden Implementierungsgeschichten in `docs/06-stories/` für jede hochzuverlässige Idee erstellt:

```markdown
---
story_id: US-XXXX
type: improvement
ideation_source: ideation-20251230.md
estimate: 1d
---

# US-XXXX: Idea from Ideation

## Background
Identified in ideation report. Agreed by: Security, API experts.

## Acceptance Criteria
- [ ] Specific criterion 1
- [ ] Specific criterion 2
- [ ] Tests pass

## Technical Notes
Implementation approach from ideation analysis.
```

## Wie es funktioniert

1. **Parse-Argumente**: Bestimmt, welche Experten eingesetzt werden sollen, und die Analysetiefe
2. **Experten parallel bereitstellen**: Startet alle Experten gleichzeitig
3. **Jeder Experte generiert Ideen**: 3-5 spezifische, umsetzbare Verbesserungsideen
4. **Ergebnisse sammeln**: Wartet, bis alle Experten den Vorgang abgeschlossen haben
5. **Synthesieren**: Gruppiert sich überschneidende Ideen und bewertet das Vertrauen
6. **Ausgabe generieren**: Erstellt einen Bericht (und optionale Storys)

## Best Practices

### Holen Sie das Beste aus /ideate heraus

**Mach das:**
- Verwenden Sie `SCOPE=all` vierteljährlich für eine umfassende Analyse
- Verwenden Sie einen spezifischen SCOPE für gezielte Verbesserungsbereiche
– Verwenden Sie `DEPTH=deep`, wenn Sie eine bestimmte Domäne untersuchen
- Sehen Sie sich zuerst Ideen mit HOHEM Vertrauen an
- Erstellen Sie Geschichten für Verbesserungen mit HOHEM Vertrauen

**Tu das nicht:**
- Ignorieren Sie Ideen mit mittlerem Vertrauen und spezifischen Beweisen
- Versuchen Sie, alles auf einmal umzusetzen
– Verwenden Sie es, ohne Git auszuführen, um den neuesten Codekontext abzurufen
- Überspringen Sie den Abschnitt „Warum“, wenn Sie sich für die Implementierung entscheiden

### Ergebnisse interpretieren

**Ideen mit hohem Vertrauen** (2+ Experten):
- Starkes Signal für echte Verbesserungsmöglichkeiten
- Empfohlen für sofortiges Handeln
- Normalerweise sind 1–3 Tage Aufwand erforderlich

**Ideen mit mittlerem Vertrauen** (einzelner Experte):
- Wertvoll, aber weniger sicher
- Eine Diskussion mit dem Team lohnt sich
- Oft gute Kandidaten für den nächsten Sprint

**Vage Ideen**:
- Automatisch vom Bericht ausgeschlossen
- Zu allgemein ohne Dateipfade oder Metriken
- Bei Interesse führen Sie zunächst eine gezielte Recherche durch

## Verwandte Befehle

- [`/epic`](/commands/epic) – Erstellen Sie ein Epos zur Verbesserung der Ideenfindung
- [`/story`](/commands/story) – Erstellen Sie eine Story für spezifische Verbesserungen
- [`/research:analyze`](/commands/research-analyze) – Tiefer Einblick in bestimmte Verbesserungsbereiche
- [`/babysit`](/commands/babysit) – Erhalten Sie Hilfe bei der Umsetzung einer Verbesserung

## Integration

### Mit Sprint Planning

Verwenden Sie nach der Ideenfindung hochzuverlässige Elemente, um den Sprint zu füllen:

```bash
/agileflow:ideate SCOPE=all DEPTH=quick OUTPUT=stories
# Stories created → add to sprint planning with /agileflow:sprint
```

### Mit Retrospektiven

Führen Sie nach Retrospektiven eine Ideenfindung durch, um systemische Verbesserungen zu finden:

```bash
/agileflow:ideate SCOPE=code DEPTH=deep
# Analyze code quality problems identified in retro
```

### Mit ADRs

Konvertieren Sie architektonische Ideen in ADRs:

```bash
/agileflow:ideate SCOPE=architecture OUTPUT=report
# Review HIGH-confidence architectural improvements
/agileflow:adr NUMBER=0045 TITLE="..." CONTEXT="From ideation"
```