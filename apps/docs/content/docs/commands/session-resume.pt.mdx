---
title: "/sessão:currículo"
description: Mudar para uma sessão diferente com carregamento de contexto
---

# /sessão:currículo

Mude para uma sessão diferente do Claude Code. Mostra as sessões disponíveis e fornece o comando para alternar.

## Início rápido

```bash
/agileflow:session:resume
```

## Como funciona

1. Lista todas as sessões registradas com status
2. Apresenta seletor interativo para selecionar uma sessão
3. Mostra o comando do terminal para alternar

## Exemplo de interação

```
Which session do you want to resume?

○ Session 1 (main) - /home/user/myproject [active]
○ Session 2 "auth" - /home/user/myproject-auth [active]
○ Session 3 - /home/user/myproject-3 [stale]
```

Depois de selecionar a Sessão 2:

```
To resume Session 2 "auth":

  cd /home/user/myproject-auth && claude --resume

Note: Use a new terminal window/tab for best experience.
```

## Estados de Sessão

| State | Can Resume? | Notes |
|-------|-------------|-------|
| `active` | Yes | Another Claude instance is running there |
| `stale` | Yes | Previous session ended, directory still exists |
| `current` | No | You're already in this session |

## O sinalizador --resume

O `claude --resume` flag diz a Claude Code para:
- Carregar histórico de conversas da sessão anterior
- Restaure o contexto sobre o que você estava trabalhando
- Continue de onde parou

Sem `--resume`, Claude começa com um novo contexto.

## Exemplos de fluxo de trabalho

### Alternando entre tarefas

```bash
# Currently in Session 1 working on feature A
/agileflow:session:resume
# Select Session 2 "auth"

# In new terminal:
cd /home/user/myproject-auth && claude --resume
# Continue auth work with full context
```

### Retornando após o intervalo

```bash
# Start Claude Code
claude

# See: "2 other active sessions"
/agileflow:session:resume
# Pick the session you want to continue
```

### Limpando antes de retomar

Se uma sessão for exibida como obsoleta:

```bash
/agileflow:session:status
# Session 3 shows "stale"

/agileflow:session:end
# Select Session 3 to clean up

/agileflow:session:resume
# Now switch to a clean session
```

## Dicas de terminais

**Best practice**: Abra uma nova guia/janela de terminal para cada sessão.

```bash
# Tab 1: Session 1 (main project)
cd /home/user/myproject && claude

# Tab 2: Session 2 (auth feature)
cd /home/user/myproject-auth && claude --resume

# Tab 3: Session 3 (API work)
cd /home/user/myproject-3 && claude --resume
```

## Comandos Relacionados

- [`/sessão:status`](/commands/session-status) - Veja todas as sessões primeiro
- [`/sessão:novo`](/commands/session-new) - Crie uma nova sessão
- [`/sessão:fim`](/commands/session-end) - Limpe antes de trocar
- [`/sessão:história`](/commands/session-history) - Veja sessões anteriores
