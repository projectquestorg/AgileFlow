---
title: /مهارة:ترقية
description: ترقية المهارات الحالية مع القدرة على التعلم التحسين الذاتي
---

# /مهارة:ترقية

أضف القدرة على التحسين الذاتي إلى المهارات حتى يتعلموا من تصحيحاتك ويتحسنوا باستمرار.

## بداية سريعة

```bash
# Interactive mode
/agileflow:skill:upgrade

# Upgrade specific skill
/agileflow:skill:upgrade supabase-swift

# Upgrade all skills at once
/agileflow:skill:upgrade --all
```

## ماذا يفعل

يمكن ترقية المهارات التي تم إنشاؤها قبل تقديم نظام التعلم للحصول على:

1. **Learnings file** - مسارات التفضيلات والاتفاقيات والأنماط
2. **Self-improvement** - يتعلم من التصحيحات الخاصة بك
3. **Preference persistence** - يتذكر ما تفضله
4. **Context accumulation** - يبني المعرفة الخاصة بالمشروع
5. **Team consistency** - التعلم المشترك عبر الفريق

## كيف يعمل

عملية الترقية:

1. **Creates learnings file** - `.agileflow/skills/_learnings/<skill>.yaml`
2. **Updates SKILL.md** - يضيف تعليمات التعلم
3. **Preserves content** - محتوى المهارة الحالي دون تغيير
4. **Enables learning** - يمكن الآن تعلم المهارة من التصحيحات

## نظام التعلم

### ما هي المهارات التي تعلمها

تتبع المهارات ثلاثة أنواع من التعلم:

**Preferences** - كيف تحب أن تتم الأمور:
```yaml
preferences:
  code_style: "async/await over callbacks"
  error_handling: "Result<T, Error> type"
  naming: "camelCase for functions"
```

**Conventions** - أنماط المشروع الواجب اتباعها:
```yaml
conventions:
  - "Always validate input at service layer"
  - "Use interfaces for dependency injection"
  - "Database queries in separate service files"
```

**Anti-patterns** - الأشياء التي يجب تجنبها:
```yaml
anti_patterns:
  - "Avoid callback patterns (use async/await)"
  - "Never hardcode configuration"
  - "Don't put business logic in controllers"
```

### هيكل ملف التعلم

تحصل كل مهارة تمت ترقيتها على ملف تعليمي:

```
.agileflow/skills/_learnings/
├── supabase-swift.yaml
├── react-components.yaml
└── api-client.yaml
```

تنسيق الملف:

```yaml
skill: supabase-swift
version: 1
last_updated: 2025-01-09T10:30:00Z

preferences:
  async_pattern: "async/await"
  error_handling: "Result<T, Error>"

conventions:
  - "Separate data, service, and UI layers"
  - "Always handle network errors explicitly"

anti_patterns:
  - "Avoid callback patterns"
  - "Never hardcode API keys"

context:
  # Project-specific learnings

examples:
  good:
    - "let user = try await supabase.auth.user()"
  bad:
    - "var apiKey = 'sk-...'"

metadata:
  corrections_count: 3
  approvals_count: 5
  last_correction: 2025-01-08T10:30:00Z
  created: 2025-01-01T00:00:00Z
```

## سير العمل

### الخطوة 1: حدد المهارة

إذا لم يتم تحديده:

```bash
/agileflow:skill:upgrade
```

**Choose a skill:**
```
Which skill would you like to upgrade?

[1] supabase-swift
    Supabase database operations for Swift iOS apps

[2] ui-components
    React component patterns with accessibility

[3] api-error-handling
    REST API error handling patterns

Select: 1
```

### الخطوة 2: التحقق من الحالة

يتحقق الأمر مما إذا كانت المهارة قد تمت ترقيتها بالفعل:

```
Checking upgrade status for: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Skill is eligible for upgrade

Current state:
  - SKILL.md: exists
  - Learnings file: NOT YET CREATED
  - Learning capability: NOT ENABLED
```

إذا تمت ترقيته بالفعل:

```
✅ Skill "supabase-swift" already has self-improving capability!

Learnings file: .agileflow/skills/_learnings/supabase-swift.yaml
  - Corrections: 5
  - Last learning: 2025-01-08T10:30:00Z

View learnings: /agileflow:skill:edit supabase-swift
```

### الخطوة 3: عرض ما سيتم إضافته

```
📦 Upgrade: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Changes to be made:

1. SKILL.md - Add sections:
   - ## Self-Improving Learnings
   - Update ## Instructions to include learning workflow

2. Create new file:
   - .agileflow/skills/_learnings/supabase-swift.yaml

The skill will now learn from your corrections!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### الخطوة 4: المعاينة (اختياري)

```
Proceed with upgrade?

[1] Yes, upgrade skill
    Add learning capability now

[2] Preview changes first (Recommended)
    Show exact changes before applying

[3] Cancel
    Exit without changes

Select: 2
```

إذا قمت بالمعاينة:

```
📝 Changes to SKILL.md (preview):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ## When to Use

+ ## Self-Improving Learnings
+
+ This skill learns from your corrections and preferences.
+
+ **On invocation**:
+ 1. Check if `.agileflow/skills/_learnings/supabase-swift.yaml` exists
+ 2. If exists, read and apply learned preferences
+ 3. Follow conventions and avoid anti-patterns from learnings

New file preview:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

.agileflow/skills/_learnings/supabase-swift.yaml

skill: supabase-swift
version: 1
last_updated: 2025-01-09T10:30:00Z

preferences: {}
conventions: []
anti_patterns: []
...

Apply changes? (Yes/No)
```

### الخطوة 5: إنشاء ملف التعلم

النظام يخلق `.agileflow/skills/_learnings/<skill>.yaml`:

```yaml
skill: supabase-swift
version: 1
last_updated: 2025-01-09T10:30:00Z

# =============================================================================
# PREFERENCES
# =============================================================================
preferences:
  # Preferences will be learned from corrections

# =============================================================================
# CONVENTIONS
# =============================================================================
conventions:
  # - "Convention will be added from corrections"

# =============================================================================
# ANTI-PATTERNS
# =============================================================================
anti_patterns:
  # - "Things to avoid will be added here"

# =============================================================================
# CONTEXT
# =============================================================================
context:
  # Project-specific context will be learned

# =============================================================================
# EXAMPLES
# =============================================================================
examples:
  good: []
  bad: []

# =============================================================================
# METADATA
# =============================================================================
metadata:
  corrections_count: 0
  approvals_count: 0
  created: 2025-01-09T10:30:00Z
  sessions: []
```

### الخطوة 6: تحديث SKILL.md

يضيف النظام إلى SKILL.md:

**New section:**
```markdown
## Self-Improving Learnings

This skill learns from your corrections and preferences.

**On invocation**:
1. Check if `.agileflow/skills/_learnings/supabase-swift.yaml` exists
2. If exists, read and apply learned preferences
3. Follow conventions and avoid anti-patterns from learnings

**On correction**:
1. When user corrects output, extract the signal
2. Determine confidence level:
   - **high**: Explicit correction ("never do X", "always do Y")
   - **medium**: User approved or pattern worked well
   - **low**: Observation to review later
3. Update the learnings file with new preference

**Learnings file location**: `.agileflow/skills/_learnings/supabase-swift.yaml`
```

### الخطوة 7: تأكيد النجاح

```
✅ Skill upgraded successfully!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Modified: .claude/skills/supabase-swift/SKILL.md
Created:  .agileflow/skills/_learnings/supabase-swift.yaml

The skill will now:
  • Load learned preferences on each invocation
  • Extract signals from your corrections
  • Build up conventions over time
  • Avoid learned anti-patterns

Start using the skill - it will learn from your feedback!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## أمثلة

### مثال 1: ترقية مهارة واحدة

```bash
/agileflow:skill:upgrade api-client
```

**Response:**
```
✅ api-client upgraded successfully!

Created: .agileflow/skills/_learnings/api-client.yaml

The skill now has:
  • Learnings file for tracking preferences
  • Self-improvement capability
  • Context accumulation from corrections
```

### المثال 2: ترقية جميع المهارات دفعة واحدة

```bash
/agileflow:skill:upgrade --all
```

**Response:**
```
📦 Upgrading all skills...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Skills to upgrade:
  [1/3] supabase-swift... ✅
  [2/3] ui-components... ✅
  [3/3] api-client... ✅

✅ Upgraded 3 skills successfully!

All skills now have self-improving capability.
```

### المثال 3: تمت ترقيته بالفعل

```bash
/agileflow:skill:upgrade react-components
```

**Response:**
```
✅ react-components already has self-improving capability!

Learnings file:
  .agileflow/skills/_learnings/react-components.yaml

Stats:
  - Corrections: 8
  - Last learning: 2025-01-08T15:30:00Z
  - Created: 2025-01-01T00:00:00Z

The skill is learning from your feedback. Keep using it!
```

## كيف تتعلم المهارات

### المرحلة الأولى: في استدعاء المهارة

عند استخدام مهارة تمت ترقيتها:

1. Claude checks if learnings file exists
2. يقرأ التفضيلات والاتفاقيات والأنماط المضادة
3. يطبقها أثناء تنفيذ المهارة
4. في المرة القادمة سوف تتذكر تفضيلاتك

### المرحلة الثانية: في التصحيح

عندما تقوم بتصحيح إخراج كلود:

1. **You provide feedback**: "لا، استخدم المزامنة/الانتظار بدلاً من ذلك"
2. **Claude extracts signal**: "تفضيل المزامنة/الانتظار على عمليات الاسترجاعات"
3. **Determines confidence**:
   - **High**: تصحيح صريح (إضافة إلى الدروس)
   - **Medium**: لقد وافقت على المخرجات (إضافة إلى الدروس)
   - **Low**: الملاحظة (ملاحظة للمراجعة)
4. **Updates learnings file**: تم حفظ التفضيل الجديد
5. **Next use applies it**: المهارة تتذكر تفضيلاتك

### مثال دورة التعلم

**Session 1:**
```
You: "Create an API error handler"

Claude: [uses try-catch blocks]

You: "No, prefer using Result<T, Error> type"

Claude: Extracts signal and updates learnings file
  preferences.error_handling = "Result<T, Error>"
```

**Session 2:**
```
You: "Create another API error handler"

Claude: [reads learnings file]
Claude: [uses Result<T, Error> type]

You: "Perfect!"

Claude: Updates learnings metadata: approvals_count++
```

**Session 3:**
```
You: "New API handler needed"

Claude: [automatically uses Result<T, Error>]
Claude: [follows all learned conventions]

The skill improved through learning!
```

## استخدام التعلمات

### مراجعة يدوية

تحقق مما تعلمته المهارة:

```bash
cat .agileflow/skills/_learnings/supabase-swift.yaml
```

### التحديثات اليدوية

أضف تفضيلاتك الخاصة قبل التصحيحات:

```bash
# Edit the learnings file directly
vim .agileflow/skills/_learnings/api-client.yaml

# Add your preferences
preferences:
  error_handling: "Custom error types"
  logging: "Structured JSON logging"
```

### مشاركة الفريق

الالتزام بالتعلم على git حتى يحصل عليها الفريق:

```bash
git add .agileflow/skills/_learnings/
git commit -m "Add api-client learning from team feedback"
```

أعضاء الفريق:
```bash
git pull
# Now has the same learnings
```

## أفضل الممارسات

### متى يتم الترقية

قم بترقية مهارة عندما:

1. **Skill is frequently used** - المزيد من التصحيحات = تعلم أفضل
2. **Team has patterns** - التعلم التقاط تفضيلات الفريق
3. **Creating new skills** - تمكين التعلم من البداية
4. **Consolidating skills** - دمج الدروس المستفادة من المهارات القديمة

### استخدام التعلم بشكل فعال

1. **Correct mistakes** - كل تصحيح يحسن المهارة
2. **Approve good output** - الموافقات هي أيضا الدروس المستفادة
3. **Document patterns** - إضافة الاتفاقيات يدويا
4. **Review periodically** - التحقق مما تم تعلمه
5. **Commit to git** - شارك الدروس المستفادة مع الفريق

### الحفاظ على التعلمات

1. **Keep files updated** - تبقى الدروس محدثة
2. **Monitor corrections** - العد الصحيح يوضح الاستخدام
3. **Archive old learnings** - إذا كان تعزيز المهارات
4. **Share improvements** - تماسك الفريق

## استكشاف الأخطاء وإصلاحها

### ترقية بالفعل؟

إذا كانت المهارة لديها تعلم بالفعل:

```
✅ Skill already upgraded!

To:
  - View learnings: cat .agileflow/skills/_learnings/<name>.yaml
  - Edit learnings: /agileflow:skill:edit <name>
  - Clear learnings: rm .agileflow/skills/_learnings/<name>.yaml
```

### لم يتم إنشاء ملف التعلم

إذا نجحت الترقية ولكن لا يوجد ملف تعليمي:

1. التحقق من نظام الملفات:
   ```bash
   ls .agileflow/skills/_learnings/
   ```

2. التحقق من وجود الدليل:
   ```bash
   mkdir -p .agileflow/skills/_learnings/
   ```

3. إعادة تشغيل الترقية:
   ```bash
   /agileflow:skill:upgrade <skill-name>
   ```

### فشلت الترقية

إذا رأيت خطأً:

```
❌ Failed to upgrade skill: Permission denied
```

**Fix**: التحقق من الأذونات:
```bash
chmod -R u+w .agileflow/skills/
chmod -R u+w .agileflow/skills/_learnings/
```

ثم أعد محاولة الترقية.

## بعد الترقية

بمجرد الترقية:

1. **Use the skill** - ابدأ العمل بها
2. **Correct Claude** - كل تصحيح يعلم المهارة
3. **Monitor learning** - التحقق من تراكم الملفات التعليمية
4. **Share with team** - الالتزام بالتعلم لبوابة

## الأوامر ذات الصلة

- [`/المهارة: إنشاء`](/commands/skill-create) - إنشاء مهارة مع تمكين التعلم
- [`/ المهارة: القائمة`](/commands/skill-list) - الاطلاع على جميع المهارات
- [`/المهارة: تحرير`](/commands/skill-edit) - تحرير ملف الدروس
- [`/المهارة:اختبار`](/commands/skill-test) - التحقق من أعمال المهارة
- [`/المهارة:حذف`](/commands/skill-delete) - إزالة المهارة

## الخطوات التالية

1. **List skills**: `/agileflow:skill:list`
2. **Upgrade frequently used**: `/agileflow:skill:upgrade <skill>`
3. **Use the skill** - العمل على المهام وتصحيح كلود
4. **Review learnings**: `cat .agileflow/skills/_learnings/<skill>.yaml`
5. **Commit to git**: شارك الدروس المستفادة مع الفريق
