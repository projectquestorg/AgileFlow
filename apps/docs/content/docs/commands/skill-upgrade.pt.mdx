---
title: "/habilidade:atualizar"
description: Atualize as habilidades existentes com capacidade de aprendizagem de autoaperfeiçoamento
---

# /habilidade:atualizar

Adicione capacidade de autoaperfeiçoamento às habilidades para que aprendam com suas correções e melhorem continuamente.

## Início rápido

```bash
# Interactive mode
/agileflow:skill:upgrade

# Upgrade specific skill
/agileflow:skill:upgrade supabase-swift

# Upgrade all skills at once
/agileflow:skill:upgrade --all
```

## O que isso faz

As habilidades criadas antes da introdução do sistema de aprendizagem podem ser atualizadas para obter:

1. **Learnings file** - Rastreia preferências, convenções, padrões
2. **Self-improvement** - Aprende com suas correções
3. **Preference persistence** - Lembra o que você prefere
4. **Context accumulation** - Constrói conhecimento específico do projeto
5. **Team consistency** - Aprendizados compartilhados entre a equipe

## Como funciona

O processo de atualização:

1. **Creates learnings file** - `.agileflow/skills/_learnings/<skill>.yaml`
2. **Updates SKILL.md** - Adiciona instruções de aprendizagem
3. **Preserves content** - Conteúdo de habilidade existente inalterado
4. **Enables learning** - A habilidade agora pode aprender com as correções

## Sistema de aprendizagem

### Quais habilidades aprendem

As habilidades acompanham três tipos de aprendizagem:

**Preferences** - Como você gosta que as coisas sejam feitas:
```yaml
preferences:
  code_style: "async/await over callbacks"
  error_handling: "Result<T, Error> type"
  naming: "camelCase for functions"
```

**Conventions** - Padrões de projeto a seguir:
```yaml
conventions:
  - "Always validate input at service layer"
  - "Use interfaces for dependency injection"
  - "Database queries in separate service files"
```

**Anti-patterns** - Coisas a evitar:
```yaml
anti_patterns:
  - "Avoid callback patterns (use async/await)"
  - "Never hardcode configuration"
  - "Don't put business logic in controllers"
```

### Estrutura do arquivo de aprendizagem

Cada habilidade atualizada recebe um arquivo de aprendizagem:

```
.agileflow/skills/_learnings/
├── supabase-swift.yaml
├── react-components.yaml
└── api-client.yaml
```

Formato de arquivo:

```yaml
skill: supabase-swift
version: 1
last_updated: 2025-01-09T10:30:00Z

preferences:
  async_pattern: "async/await"
  error_handling: "Result<T, Error>"

conventions:
  - "Separate data, service, and UI layers"
  - "Always handle network errors explicitly"

anti_patterns:
  - "Avoid callback patterns"
  - "Never hardcode API keys"

context:
  # Project-specific learnings

examples:
  good:
    - "let user = try await supabase.auth.user()"
  bad:
    - "var apiKey = 'sk-...'"

metadata:
  corrections_count: 3
  approvals_count: 5
  last_correction: 2025-01-08T10:30:00Z
  created: 2025-01-01T00:00:00Z
```

## Fluxo de trabalho

### Etapa 1: selecione a habilidade

Se não for especificado:

```bash
/agileflow:skill:upgrade
```

**Choose a skill:**
```
Which skill would you like to upgrade?

[1] supabase-swift
    Supabase database operations for Swift iOS apps

[2] ui-components
    React component patterns with accessibility

[3] api-error-handling
    REST API error handling patterns

Select: 1
```

### Etapa 2: verificar o status

O comando verifica se a habilidade já foi atualizada:

```
Checking upgrade status for: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Skill is eligible for upgrade

Current state:
  - SKILL.md: exists
  - Learnings file: NOT YET CREATED
  - Learning capability: NOT ENABLED
```

Se já tiver atualizado:

```
✅ Skill "supabase-swift" already has self-improving capability!

Learnings file: .agileflow/skills/_learnings/supabase-swift.yaml
  - Corrections: 5
  - Last learning: 2025-01-08T10:30:00Z

View learnings: /agileflow:skill:edit supabase-swift
```

### Etapa 3: mostre o que será adicionado

```
📦 Upgrade: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Changes to be made:

1. SKILL.md - Add sections:
   - ## Self-Improving Learnings
   - Update ## Instructions to include learning workflow

2. Create new file:
   - .agileflow/skills/_learnings/supabase-swift.yaml

The skill will now learn from your corrections!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Etapa 4: visualização (opcional)

```
Proceed with upgrade?

[1] Yes, upgrade skill
    Add learning capability now

[2] Preview changes first (Recommended)
    Show exact changes before applying

[3] Cancel
    Exit without changes

Select: 2
```

Se você visualizar:

```
📝 Changes to SKILL.md (preview):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ## When to Use

+ ## Self-Improving Learnings
+
+ This skill learns from your corrections and preferences.
+
+ **On invocation**:
+ 1. Check if `.agileflow/skills/_learnings/supabase-swift.yaml` exists
+ 2. If exists, read and apply learned preferences
+ 3. Follow conventions and avoid anti-patterns from learnings

New file preview:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

.agileflow/skills/_learnings/supabase-swift.yaml

skill: supabase-swift
version: 1
last_updated: 2025-01-09T10:30:00Z

preferences: {}
conventions: []
anti_patterns: []
...

Apply changes? (Yes/No)
```

### Etapa 5: criar arquivo de aprendizagem

O sistema cria `.agileflow/skills/_learnings/<skill>.yaml`:

```yaml
skill: supabase-swift
version: 1
last_updated: 2025-01-09T10:30:00Z

# =============================================================================
# PREFERENCES
# =============================================================================
preferences:
  # Preferences will be learned from corrections

# =============================================================================
# CONVENTIONS
# =============================================================================
conventions:
  # - "Convention will be added from corrections"

# =============================================================================
# ANTI-PATTERNS
# =============================================================================
anti_patterns:
  # - "Things to avoid will be added here"

# =============================================================================
# CONTEXT
# =============================================================================
context:
  # Project-specific context will be learned

# =============================================================================
# EXAMPLES
# =============================================================================
examples:
  good: []
  bad: []

# =============================================================================
# METADATA
# =============================================================================
metadata:
  corrections_count: 0
  approvals_count: 0
  created: 2025-01-09T10:30:00Z
  sessions: []
```

### Etapa 6: atualizar SKILL.md

O sistema adiciona ao SKILL.md:

**New section:**
```markdown
## Self-Improving Learnings

This skill learns from your corrections and preferences.

**On invocation**:
1. Check if `.agileflow/skills/_learnings/supabase-swift.yaml` exists
2. If exists, read and apply learned preferences
3. Follow conventions and avoid anti-patterns from learnings

**On correction**:
1. When user corrects output, extract the signal
2. Determine confidence level:
   - **high**: Explicit correction ("never do X", "always do Y")
   - **medium**: User approved or pattern worked well
   - **low**: Observation to review later
3. Update the learnings file with new preference

**Learnings file location**: `.agileflow/skills/_learnings/supabase-swift.yaml`
```

### Etapa 7: confirme o sucesso

```
✅ Skill upgraded successfully!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Modified: .claude/skills/supabase-swift/SKILL.md
Created:  .agileflow/skills/_learnings/supabase-swift.yaml

The skill will now:
  • Load learned preferences on each invocation
  • Extract signals from your corrections
  • Build up conventions over time
  • Avoid learned anti-patterns

Start using the skill - it will learn from your feedback!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Exemplos

### Exemplo 1: Atualizar Habilidade Única

```bash
/agileflow:skill:upgrade api-client
```

**Response:**
```
✅ api-client upgraded successfully!

Created: .agileflow/skills/_learnings/api-client.yaml

The skill now has:
  • Learnings file for tracking preferences
  • Self-improvement capability
  • Context accumulation from corrections
```

### Exemplo 2: atualização em lote de todas as habilidades

```bash
/agileflow:skill:upgrade --all
```

**Response:**
```
📦 Upgrading all skills...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Skills to upgrade:
  [1/3] supabase-swift... ✅
  [2/3] ui-components... ✅
  [3/3] api-client... ✅

✅ Upgraded 3 skills successfully!

All skills now have self-improving capability.
```

### Exemplo 3: já atualizado

```bash
/agileflow:skill:upgrade react-components
```

**Response:**
```
✅ react-components already has self-improving capability!

Learnings file:
  .agileflow/skills/_learnings/react-components.yaml

Stats:
  - Corrections: 8
  - Last learning: 2025-01-08T15:30:00Z
  - Created: 2025-01-01T00:00:00Z

The skill is learning from your feedback. Keep using it!
```

## Como as habilidades aprendem

### Fase 1: Na Invocação de Habilidades

Quando você usa uma habilidade atualizada:

1. Claude verifica se o arquivo de aprendizagem existe
2. Lê preferências, convenções, antipadrões
3. Aplica-os durante a execução da habilidade
4. Da próxima vez lembrarei de suas preferências

### Fase 2: Na Correção

Quando você corrige a saída de Claude:

1. **You provide feedback**: "Não, use async/await"
2. **Claude extracts signal**: "Prefira async/await a retornos de chamada"
3. **Determines confidence**:
   - **High**: Correção explícita (adicionar aos aprendizados)
   - **Medium**: você aprovou o resultado (adicionar aos aprendizados)
   - **Low**: Observação (nota para revisão)
4. **Updates learnings file**: Nova preferência salva
5. **Next use applies it**: Skill lembra sua preferência

### Exemplo de Ciclo de Aprendizagem

**Session 1:**
```
You: "Create an API error handler"

Claude: [uses try-catch blocks]

You: "No, prefer using Result<T, Error> type"

Claude: Extracts signal and updates learnings file
  preferences.error_handling = "Result<T, Error>"
```

**Session 2:**
```
You: "Create another API error handler"

Claude: [reads learnings file]
Claude: [uses Result<T, Error> type]

You: "Perfect!"

Claude: Updates learnings metadata: approvals_count++
```

**Session 3:**
```
You: "New API handler needed"

Claude: [automatically uses Result<T, Error>]
Claude: [follows all learned conventions]

The skill improved through learning!
```

## Usando aprendizados

### Revisão manual

Verifique o que a habilidade aprendeu:

```bash
cat .agileflow/skills/_learnings/supabase-swift.yaml
```

### Atualizações manuais

Adicione suas próprias preferências antes das correções:

```bash
# Edit the learnings file directly
vim .agileflow/skills/_learnings/api-client.yaml

# Add your preferences
preferences:
  error_handling: "Custom error types"
  logging: "Structured JSON logging"
```

### Compartilhamento de equipe

Comprometa os aprendizados com o git para que a equipe os obtenha:

```bash
git add .agileflow/skills/_learnings/
git commit -m "Add api-client learning from team feedback"
```

Membros da equipe:
```bash
git pull
# Now has the same learnings
```

## Melhores Práticas

### Quando atualizar

Atualize uma habilidade quando:

1. **Skill is frequently used** - Mais correções = melhor aprendizado
2. **Team has patterns** - Os aprendizados capturam as preferências da equipe
3. **Creating new skills** - Permita o aprendizado desde o início
4. **Consolidating skills** - Mesclar aprendizados de habilidades antigas

### Usando o aprendizado de forma eficaz

1. **Correct mistakes** - Cada correção melhora a habilidade
2. **Approve good output** - Aprovações também são aprendizados
3. **Document patterns** - Adicione convenções manualmente
4. **Review periodically** - Verifique o que foi aprendido
5. **Commit to git** - Compartilhe aprendizados com a equipe

### Mantendo Aprendizados

1. **Keep files updated** - Os aprendizados permanecem atualizados
2. **Monitor corrections** - A contagem correta mostra o uso
3. **Archive old learnings** - Se consolidar competências
4. **Share improvements** - Consistência da equipe

## Solução de problemas

### Já atualizou?

Se a habilidade já possui aprendizado:

```
✅ Skill already upgraded!

To:
  - View learnings: cat .agileflow/skills/_learnings/<name>.yaml
  - Edit learnings: /agileflow:skill:edit <name>
  - Clear learnings: rm .agileflow/skills/_learnings/<name>.yaml
```

### Nenhum arquivo de aprendizagem criado

Se a atualização for bem-sucedida, mas não houver arquivo de aprendizagem:

1. Verifique o sistema de arquivos:
   ```bash
   ls .agileflow/skills/_learnings/
   ```

2. Verifique se o diretório existe:
   ```bash
   mkdir -p .agileflow/skills/_learnings/
   ```

3. Execute novamente a atualização:
   ```bash
   /agileflow:skill:upgrade <skill-name>
   ```

### Falha na atualização

Se você vir um erro:

```
❌ Failed to upgrade skill: Permission denied
```

**Fix**: Verifique as permissões:
```bash
chmod -R u+w .agileflow/skills/
chmod -R u+w .agileflow/skills/_learnings/
```

Em seguida, tente atualizar novamente.

## Após a atualização

Depois de atualizado:

1. **Use the skill** - Comece a trabalhar com isso
2. **Correct Claude** - Cada correção ensina a habilidade
3. **Monitor learning** - Verifique o acúmulo de arquivos de aprendizagem
4. **Share with team** - Comprometa o aprendizado com o git

## Comandos Relacionados

- [`/habilidade:criar`](/commands/skill-create) - Crie habilidade com aprendizagem habilitada
- [`/habilidade:lista`](/commands/skill-list) - Veja todas as habilidades
- [`/habilidade:editar`](/commands/skill-edit) - Editar arquivo de aprendizagem
- [`/habilidade:teste`](/commands/skill-test) - Verifique se a habilidade funciona
- [`/habilidade:excluir`](/commands/skill-delete) - Remover habilidade

## Próximas etapas

1. **List skills**: `/agileflow:skill:list`
2. **Upgrade frequently used**: `/agileflow:skill:upgrade <skill>`
3. **Use the skill** - Trabalhe nas tarefas e corrija Claude
4. **Review learnings**: `cat .agileflow/skills/_learnings/<skill>.yaml`
5. **Commit to git**: Compartilhe aprendizados com a equipe
