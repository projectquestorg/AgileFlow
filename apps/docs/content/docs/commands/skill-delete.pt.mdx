---
title: "/habilidade:excluir"
description: Remover uma habilidade instalada de .claude/skills/
---

# /habilidade:excluir

Remova com segurança uma habilidade de `.claude/skills/` com recursos completos de segurança, incluindo visualização, confirmação e backup opcional.

## Início rápido

```bash
# Interactive mode
/agileflow:skill:delete

# Delete specific skill
/agileflow:skill:delete supabase-swift
```

## O que isso faz

O comando de exclusão:

1. **Shows what will be deleted** - Visualização em árvore de todos os arquivos
2. **Confirms with you** - Requer aprovação explícita
3. **Offers backup option** - Salve uma cópia antes de excluir
4. **Permanently removes** - Exclui o diretório de habilidades
5. **Suggests next actions** - O que fazer após a exclusão

## Recursos de segurança

Este comando possui múltiplas camadas de segurança:

- **No accidental deletion** - Sempre confirme antes de remover
- **Tree view preview** - Veja exatamente o que foi excluído
- **Backup option** - Exportar habilidade antes da exclusão
- **One at a time** - Excluir habilidades individuais (sem exclusão em lote)
- **Clear confirmation** - Escolha explícita de SIM/NÃO

## Fluxo de trabalho

### Etapa 1: selecione a habilidade

Se você não especificar uma habilidade:

```bash
/agileflow:skill:delete
```

**Choose from available skills:**
```
Which skill would you like to delete?

[1] supabase-swift
    Supabase database operations for Swift iOS apps

[2] ui-components (deprecated)
    Old component patterns (no longer using)

[3] api-error-handling
    REST API error handling patterns

Select: 2
```

### Etapa 2: visualizar o que será excluído

O comando mostra exatamente o que há na habilidade:

```
⚠️  About to delete: ui-components
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This will permanently remove:

  .claude/skills/ui-components/
  ├── SKILL.md
  ├── references.md
  ├── cookbook/
  │   ├── create-component.md
  │   ├── styling-patterns.md
  │   └── accessibility.md
  └── .mcp.json

Total: 6 files in 2 directories

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Etapa 3: confirmar a exclusão

```
Are you sure you want to delete this skill?
This cannot be undone.

[1] Yes, delete permanently
    Remove the skill and all its files

[2] No, keep it
    Cancel and return

[3] Export first
    Save a backup before deleting

Select: 3
```

### Etapa 4: criar backup (se solicitado)

Se você escolher "Exportar primeiro":

```
📦 Creating backup...

Backup saved to:
  .claude/skills-backup/ui-components-20251209-143052/

This contains a copy of everything. You can restore it later
if needed, or just delete the backup directory.

Ready to delete? (Yes/No)
```

Local de backup:
```
.claude/skills-backup/
└── ui-components-20251209-143052/
    ├── SKILL.md
    ├── references.md
    ├── cookbook/
    └── .mcp.json
```

### Etapa 5: exclusão confirmada

```
✅ Deleted: ui-components

The skill has been permanently removed from .claude/skills/
```

### Etapa 6: Próximas ações

Após a exclusão, você terá opções:

```
What would you like to do next?

[1] Delete another skill
    Remove more skills

[2] Create new skill
    Generate a replacement skill

[3] List remaining skills
    See what's still installed

[4] Done
    Exit skill management

Select: 4
```

## Exemplos

### Exemplo 1: Excluir habilidade obsoleta

```bash
/agileflow:skill:delete old-pattern
```

**Response:**
```
⚠️  About to delete: old-pattern
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This will permanently remove:
  .claude/skills/old-pattern/
  ├── SKILL.md
  └── references.md

Total: 2 files

Are you sure? (Yes/No/Export first)
> Yes

✅ Deleted: old-pattern
```

### Exemplo 2: Excluir com Backup

```bash
/agileflow:skill:delete supabase-swift
```

**Response:**
```
⚠️  About to delete: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This will permanently remove:
  .claude/skills/supabase-swift/
  ├── SKILL.md
  ├── references.md
  ├── cookbook/ (3 files)
  └── .mcp.json

Total: 6 files

Are you sure? (Yes/No/Export first)
> Export first

📦 Creating backup...

Backup saved to:
  .claude/skills-backup/supabase-swift-20251209-150000/

Ready to delete? (Yes/No)
> Yes

✅ Deleted: supabase-swift
```

### Exemplo 3: Seleção Interativa

```bash
/agileflow:skill:delete
```

**Response:**
```
Which skill would you like to delete?

[1] api-integration
[2] database-queries
[3] react-components

Select: 2

⚠️  About to delete: database-queries
...
```

## Gerenciamento de backup

### Local de backup

Os backups são salvos com carimbo de data/hora:

```
.claude/skills-backup/
├── ui-components-20251208-093000/
├── api-client-20251208-103500/
└── database-20251209-150000/
```

Format: `<skill-name>-<YYYYMMDD-HHMMSS>/`

### Restaurar do backup

Se você excluir uma habilidade e quiser devolvê-la:

```bash
# Copy from backup
cp -r .claude/skills-backup/supabase-swift-20251209-150000 \
      .claude/skills/supabase-swift

# Verify it works
/agileflow:skill:test supabase-swift
```

### Limpe backups antigos

Remova periodicamente backups antigos:

```bash
# List backups
ls -la .claude/skills-backup/

# Remove old backup
rm -rf .claude/skills-backup/old-skill-20250101-120000/
```

## Quando excluir

Excluir habilidades quando:

- **No longer needed** - A tecnologia está obsoleta
- **Outdated** - Existe alternativa melhor
- **Duplicated** - Consolidado em uma habilidade
- **Low quality** - Habilidade não é útil
- **Organization** - Muitas habilidades semelhantes

## Antes de excluir

Considerar:

1. **Are you sure?** - A exclusão é permanente
2. **Document why** - Nota no git commit
3. **Backup first** - Use a opção "Exportar primeiro"
4. **Create replacement** - Se consolidar competências
5. **Notify team** - Se compartilhado no git

## Alternativa para excluir

Em vez de excluir, você poderia:

1. **Rename** para indicar obsoleto:
   ```bash
   /agileflow:skill:edit <skill-name>
   # Select "Rename skill"
   # Rename to: deprecated-<old-name>
   ```

2. **Archive** saindo das habilidades ativas:
   ```bash
   mv .claude/skills/old-skill .claude/skills-archive/
   ```

3. **Mark in documentation**:
   - Edite SKILL.md para adicionar aviso de suspensão de uso
   - Ainda disponível, mas marcado como obsoleto

## Tratamento de erros

### Habilidade não encontrada

```
❌ Skill "<name>" not found.

Available skills:
  - supabase-swift
  - ui-components
  - api-error-handling

Use /agileflow:skill:list to see all skills.
```

**Fix**: verifique a ortografia do nome da habilidade ou liste as habilidades disponíveis.

### Nenhuma habilidade para excluir

```
❌ No skills to delete.

No skills are currently installed in .claude/skills/

Create a skill first: /agileflow:skill:create
```

**Fix**: crie uma habilidade antes de tentar excluir uma.

### Falha na exclusão

```
❌ Failed to delete skill: Permission denied

The skill directory may be in use or have permission issues.
Try manually: rm -rf .claude/skills/<skill>/
```

**Fix**: Verifique as permissões do arquivo:
```bash
ls -la .claude/skills/<skill>/
chmod -R u+w .claude/skills/<skill>/
rm -rf .claude/skills/<skill>/
```

### Falha na criação de backup

```
⚠️  Could not create backup.

Check permissions for:
  .claude/skills-backup/

Manual backup:
  cp -r .claude/skills/<skill> .claude/skills-backup/
```

**Fix**: certifique-se de que o diretório de backup seja gravável:
```bash
mkdir -p .claude/skills-backup/
chmod u+w .claude/skills-backup/
```

## Melhores Práticas

### Fluxo de trabalho de exclusão

1. **Use `/skill:list`** para ver todas as habilidades
2. **Test before deleting** se não tiver certeza:
   ```bash
   /agileflow:skill:test <skill-name>
   ```
3. **Use "Export first"** opção para backup
4. **Confirm deletion** explicitamente
5. **Note in git** por que a habilidade foi excluída

### Estratégia de backup

1. **Always export** para habilidades criadas há mais de um mês
2. **Keep backups** por 30 dias no mínimo
3. **Document** quais habilidades são apoiadas
4. **Test restores** periodicamente

### Fluxo de trabalho da equipe

Se estiver compartilhando habilidades no git:

1. **Commit deletion** com razão:
   ```bash
   git commit -m "Remove: deprecated-api-client skill - \
   consolidated into api-integration skill"
   ```

2. **Notify team** de exclusão

3. **Keep backup** no branch git (opcional):
   ```bash
   git checkout -b backup-deprecated-skills
   git checkout main
   ```

## Comandos Relacionados

- [`/habilidade:lista`](/commands/skill-list) - Veja todas as habilidades antes de excluir
- [`/habilidade:criar`](/commands/skill-create) - Criar habilidade de substituição
- [`/habilidade:editar`](/commands/skill-edit) - Renomear em vez de excluir
- [`/habilidade:teste`](/commands/skill-test) - Verifique antes da exclusão
- [`/habilidade:atualizar`](/commands/skill-upgrade) - Melhorar as habilidades existentes

## Próximas etapas

1. **List skills**: `/agileflow:skill:list`
2. **Identify obsolete**: Quais não são usados?
3. **Backup first**: Use a opção "Exportar primeiro"
4. **Delete carefully**: Confirme cada exclusão
5. **Clean backups**: Remova backups antigos periodicamente
