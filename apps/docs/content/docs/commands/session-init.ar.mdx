---
title: / الجلسة: الحرف الأول
description: تهيئة تسخير الجلسة مع التحقق من الاختبار
---
# / الجلسة: init

تهيئة أو التحقق من نظام إدارة الجلسة. يقوم هذا الأمر بإعداد البنية الأساسية لتتبع الجلسة.

## بداية سريعة

__لا تترجم_0__

## ماذا يفعل

1. قم بإنشاء دليل `.agileflow/sessions/` إذا كان مفقودًا
2. تهيئة `registry.json` بالمخطط
3. يسجل الدليل الحالي كجلسة
4. يقوم بإنشاء ملف قفل بمعرف العملية الحالي
5. يعرض حالة الجلسة

## إخراج المثال

### الإعداد لأول مرة

__لا تترجم_1__

### تمت تهيئته بالفعل

__لا تترجم_2__

### جلسات أخرى نشطة

__لا تترجم_3__

## متى يتم استدعاؤه؟

### تلقائي (موصى به)

يستدعي خطاف SessionStart هذا تلقائيًا عندما يبدأ Claude Code:

__لا تترجم_4__

يسجل البرنامج النصي الترحيبي الجلسة ويعرض الحالة.

### دليل

تشغيل يدويا إلى:
- التحقق من عمل نظام الجلسة
- إعادة التسجيل بعد مشاكل النظام
- تصحيح مشاكل الجلسة

__لا تترجم_5__

## الملفات التي تم إنشاؤها

### ملف التسجيل

__لا تترجم_15__:

__لا تترجم_6__

### قفل الملف

__لاANSLATE_16__:

__لا تترجم_7__

## استكشاف الأخطاء وإصلاحها

### عدم اكتشاف الجلسة

__لا تترجم_8__

### تحذير القفل القديم

إذا رأيت أقفالًا قديمة، فسيقوم الأمر init بتنظيفها:

__لا تترجم_9__

### مشكلات الأذونات

تأكد من أن لديك حق الوصول للكتابة إلى `.agileflow/`:

__لا تترجم_10__

## التكامل مع برنامج الترحيب

يتضمن البرنامج النصي الترحيبي AgileFlow الكشف عن الجلسة:

__لا تترجم_11__

أو مع الصراعات:

__لا تترجم_12__

## الأوامر ذات الصلة

- [`/session:status`](/commands/session-status) - عرض كافة الجلسات
- [`/session:new`](/commands/session-new) - إنشاء جلسة موازية
- [`/session:end`](/commands/session-end) - إنهاء الجلسة الحالية