---
title: /ideate
description: قم بإنشاء أفكار تحسين مصنفة باستخدام التحليل متعدد الخبراء
---

# /ideate

أنشئ أفكارًا للتحسين من خلال نشر خبراء متعددين في المجال بالتوازي لتحليل قاعدة التعليمات البرمجية الخاصة بك من وجهات نظر مختلفة (الأمان والأداء وجودة التعليمات البرمجية وتجربة المستخدم والاختبار وواجهة برمجة التطبيقات/الهندسة المعمارية).

## بداية سريعة

```bash
/agileflow:ideate SCOPE=all DEPTH=quick OUTPUT=report
```

## حدود

| Parameter | Required | Default | Description |
|-----------|----------|---------|-------------|
| `SCOPE` | No | `all` | Which domains to analyze: `all`, `security`, `perf`, `code`, `ux` |
| `DEPTH` | No | `quick` | Analysis depth: `quick` (3 ideas), `deep` (5 ideas) |
| `OUTPUT` | No | `report` | Output type: `report` (ideation only), `stories` (create stories), `both` |

## أمثلة

### تحليل جميع المجالات - النطاق السريع

```bash
/agileflow:ideate SCOPE=all DEPTH=quick OUTPUT=report
```

نشر 6 خبراء (الأمان، والأداء، وإعادة البناء، وواجهة المستخدم، والاختبار، وواجهة برمجة التطبيقات) لإنشاء 3 أفكار للتحسين لكل منهم. النتائج في تقرير التفكير بأفكار ذات ثقة عالية/متوسطة.

### التحليل الأمني ​​العميق

```bash
/agileflow:ideate SCOPE=security DEPTH=deep OUTPUT=stories
```

يركز على الأمن من خلال التحليل العميق (5 أفكار لكل خبير). يقوم بإنشاء قصص للعناصر عالية الثقة على الفور.

### تحليل الأداء مع كل من التقارير والقصص

```bash
/agileflow:ideate SCOPE=perf DEPTH=deep OUTPUT=both
```

يحلل الأداء مع الخبراء (الأداء، قاعدة البيانات، واجهة برمجة التطبيقات)، وينشئ تقريرًا وينشئ قصص التنفيذ.

## رسم خرائط النطاق

| SCOPE | Experts Deployed | Best For |
|-------|------------------|----------|
| `all` | Security, Performance, Refactor, UI, Testing, API | Comprehensive analysis (6 experts) |
| `security` | Security, API, Testing | Security review (3 experts) |
| `perf` | Performance, Database, API | Performance optimization (3 experts) |
| `code` | Refactor, Testing, API | Code quality (3 experts) |
| `ux` | UI, Accessibility, API | User experience (3 experts) |

## هيكل الإخراج

### تقرير الفكرة (`output.md`)

The command generates `docs/08-project/ideation-<YYYYMMDD>.md` with:

1. **Summary**: مجموع الأفكار، وانهيار الثقة
2. **High-Confidence Improvements**: الأفكار التي وافق عليها أكثر من خبيرين (حدد أولوياتها)
3. **Medium-Confidence Opportunities**: Single expert with evidence (worth exploring)
4. **Summary by Category**: جدول عدد الأفكار لكل مجال
5. **Recommended Next Steps**: توجيهات التنفيذ

### سجل الثقة

| Confidence | Criteria | Action |
|------------|----------|--------|
| **HIGH** | 2+ experts suggest similar idea | Include prominently, recommend immediate action |
| **MEDIUM** | 1 expert with specific evidence (file paths, metrics) | Include as opportunity |
| **LOW** | 1 expert, vague/no evidence | Exclude from report |

### قسم التقارير النموذجية

```markdown
# Ideation Report

**Generated**: 2025-12-30
**Scope**: all
**Depth**: quick
**Total Ideas**: 15 (High: 4, Medium: 8, Low: 3)

## 🎯 High-Confidence Improvements
*Agreed by multiple experts - prioritize these*

### 1. Add request rate limiting to API
**Category**: Security | **Impact**: High | **Effort**: 1-2 days
**Experts**: Security, API
**Why**: Prevents DDoS attacks and brute force attempts
**Files**: `src/api/middleware/auth.ts`, `src/api/routes/`

## 💡 Medium-Confidence Opportunities
*Single expert with evidence - worth exploring*

### 5. Implement caching layer for database queries
**Category**: Performance | **Impact**: High | **Effort**: 2-3 days
**Expert**: Performance
**Why**: Your homepage renders 200+ components, this would fix 2-second load time
**Files**: `src/db/queries.ts`, `src/cache/`
```

### القصص التي تم إنشاؤها (اختياري)

متى `OUTPUT=stories` أو `OUTPUT=both`، ينشئ قصص التنفيذ في `docs/06-stories/` لكل فكرة عالية الثقة:

```markdown
---
story_id: US-XXXX
type: improvement
ideation_source: ideation-20251230.md
estimate: 1d
---

# US-XXXX: Idea from Ideation

## Background
Identified in ideation report. Agreed by: Security, API experts.

## Acceptance Criteria
- [ ] Specific criterion 1
- [ ] Specific criterion 2
- [ ] Tests pass

## Technical Notes
Implementation approach from ideation analysis.
```

## كيف يعمل

1. **Parse Arguments**: يحدد الخبراء الذين سيتم نشرهم وعمق التحليل
2. **Deploy Experts in Parallel**: يقوم بتشغيل جميع الخبراء في وقت واحد
3. **Each Expert Generates Ideas**: 3-5 أفكار تحسين محددة وقابلة للتنفيذ
4. **Collect Results**: ينتظر اكتمال كافة الخبراء
5. **Synthesize**: مجموعات متداخلة الأفكار، وعشرات الثقة
6. **Generate Output**: إنشاء تقرير (وقصص اختيارية)

## أفضل الممارسات

### احصل على أقصى استفادة من /ideate

**Do This:**
- يستخدم `SCOPE=all` ربع سنوية للتحليل الشامل
- استخدم نطاقًا محددًا لمجالات التحسين المركزة
- يستخدم `DEPTH=deep` عند التحقيق في مجال معين
- قم بمراجعة الأفكار عالية الثقة أولاً
- أنشئ قصصًا لتحسينات الثقة العالية

**Don't Do This:**
- تجاهل أفكار الثقة المتوسطة مع أدلة محددة
- حاول تنفيذ كل شيء دفعة واحدة
- استخدمه بدون تشغيل git للحصول على أحدث سياق للتعليمات البرمجية
- تخطي قسم "لماذا" عند اتخاذ قرار بالتنفيذ

### تفسير النتائج

**High-Confidence Ideas** (2+ خبراء):
- إشارة قوية لفرصة التحسين الحقيقية
- يوصى باتخاذ إجراءات فورية
- عادة ما تتطلب 1-3 أيام من الجهد

**Medium-Confidence Ideas** (خبير واحد):
- قيمة ولكن أقل يقينا
- يستحق المناقشة مع الفريق
- في كثير من الأحيان المرشحين الجيدين للسباق المقبل

**Vague Ideas**:
- يتم استبعاده تلقائيًا من التقرير
- عام جدًا بدون مسارات أو مقاييس للملفات
- إذا كنت مهتمًا، قم بإجراء بحث مركز أولاً

## الأوامر ذات الصلة

- [`/ملحمة`](/commands/epic) - إنشاء ملحمة لتحسين التفكير
- [`/قصة`](/commands/story) - إنشاء قصة لتحسين محدد
- [`/بحث:تحليل`](/commands/research-analyze) - الغوص العميق في منطقة تحسين محددة
- [`/ جليسة الأطفال`](/commands/babysit) - احصل على مساعدة في تنفيذ التحسين

## اندماج

### مع تخطيط سبرينت

بعد التفكير، استخدم عناصر عالية الثقة لملء سباق السرعة:

```bash
/agileflow:ideate SCOPE=all DEPTH=quick OUTPUT=stories
# Stories created → add to sprint planning with /agileflow:sprint
```

### مع استرجاعيات

قم بتشغيل التفكير بعد استرجاع الأحداث للعثور على تحسينات نظامية:

```bash
/agileflow:ideate SCOPE=code DEPTH=deep
# Analyze code quality problems identified in retro
```

### مع التفاعلات الدوائية

تحويل الأفكار المعمارية إلى ADRs:

```bash
/agileflow:ideate SCOPE=architecture OUTPUT=report
# Review HIGH-confidence architectural improvements
/agileflow:adr NUMBER=0045 TITLE="..." CONTEXT="From ideation"
```
