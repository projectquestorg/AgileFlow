---
title: /verificar
description: Ejecute pruebas de proyecto y actualice el estado de las pruebas de la historia
---

# /verificar

Ejecute las pruebas de su proyecto y actualice automáticamente el estado de las pruebas de la historia en su sistema de seguimiento. Verify es un componente central del arnés de sesión de AgileFlow que garantiza la validación de pruebas durante todo el flujo de trabajo de desarrollo.

## Inicio rápido

```bash
# Run all tests and update all in-progress stories
/agileflow:verify

# Run tests and update specific story
/agileflow:verify US-0042
```

El comando:
1. Cargue la configuración de prueba desde su proyecto
2. Ejecutar el conjunto de pruebas
3. Resultados de la prueba de análisis
4. Actualizar el estado de la prueba de la historia (aprobada/reprobada)
5. Muestre un informe claro con indicadores visuales

## Características clave

### Análisis automático de pruebas

Verify detecta y analiza automáticamente los resultados de las pruebas de:

| Framework | Example Output |
|-----------|-----------------|
| Jest/Vitest (Node.js) | `Tests: 42 passed, 42 total` |
| Pytest (Python) | `42 passed in 2.5s` |
| Cargo (Rust) | `test result: ok. 42 passed` |
| Go Test | `ok github.com/user/project` |

Si el análisis falla, vuelve al código de salida (0 = aprobado, distinto de cero = fallido).

### Actualizaciones de estado de la historia

Los resultados de las pruebas se registran automáticamente en sus historias:

```json
{
  "story_id": "US-0042",
  "test_status": "passing",
  "test_results": {
    "last_run": "2025-12-06T10:30:00Z",
    "command": "npm test",
    "passed": 42,
    "failed": 0,
    "exit_code": 0,
    "output_summary": "All tests passed (42/42)"
  }
}
```

### Seguimiento de varios pisos

Sin un ID de historia específico, verifique todas las actualizaciones `in_progress` historias:

```bash
/agileflow:verify
# Updates test_status for:
# - US-0042: passing
# - US-0043: passing
# - US-0044: failing
```

## Uso

### Ejecute todas las pruebas

```bash
/agileflow:verify
```

Ejecuta su conjunto de pruebas y actualiza todas las historias en progreso con los resultados.

### Ejecute pruebas para una historia específica

```bash
/agileflow:verify US-0042
```

Ejecuta pruebas y actualiza únicamente el estado de la prueba de US-0042. Útil cuando estás trabajando en una sola historia.

### Parámetros

| Parameter | Required | Default | Description |
|-----------|----------|---------|-------------|
| `STORY` | No | All in-progress | Story ID to update (e.g., US-0042) |

## Ejemplos

### Flujo de trabajo de desarrollo típico

```bash
# Make code changes...

# Verify tests still pass
/agileflow:verify

# Output:
# ✅ PASSED | npm test | 42 passed, 0 failed | 12.3s
# Updated stories:
# - US-0042: passing
# - US-0043: passing
```

### Las pruebas fallan: corríjalas

```bash
/agileflow:verify

# Output:
# ❌ FAILED | npm test | 40 passed, 2 failed | 8.7s
#
# Failed tests:
#   ❌ auth.test.ts:42 - Expected redirect
#   ❌ api.test.ts:67 - Session token not persisting
#
# Updated stories:
# - US-0042: failing
# - US-0043: failing
```

### Antes de crear la línea base

```bash
# Ensure all tests pass before marking a milestone
/agileflow:verify

# If passing:
/agileflow:baseline "Sprint 3 complete"

# If failing:
# (Fix tests first, then baseline)
```

## Formato de salida

### Informe de éxito

```text
🧪 Test Verification Complete

Command: npm test
Duration: 12.3s
Result: ✅ PASSED

Tests: 42 passed, 0 failed, 42 total

Updated stories:
- US-0042: passing
- US-0043: passing

All in-progress stories have passing tests ✅
```

### Informe de falla

```text
🧪 Test Verification Complete

Command: npm test
Duration: 8.7s
Result: ❌ FAILED

Tests: 40 passed, 2 failed, 42 total

Failed tests:
  ❌ auth.test.ts:42
     Expected redirect to /dashboard
     Got redirect to /login

  ❌ api.test.ts:67
     Session token not persisting
     Cookie not set after login

Updated stories:
- US-0042: failing
- US-0043: failing

⚠️  WARNING: 2 stories now have failing tests
```

### Se acabó el tiempo

```text
🧪 Test Verification Failed

Command: npm test
Result: ⏱️  TIMEOUT (60s)

The test suite did not complete within 60 seconds.

Updated stories:
- US-0042: failing (timeout)

Consider:
1. Increase test_timeout_ms in environment.json
2. Optimize slow tests
3. Run specific test suites instead of all tests
```

## Configuración

Verificar utiliza la configuración de `docs/00-meta/environment.json`:

```json
{
  "test_command": "npm test",
  "test_timeout_ms": 60000,
  "project_type": "nodejs"
}
```

Para personalizar el comportamiento de la prueba:

1. Editar `docs/00-meta/environment.json`
2. Cambiar `test_command` a tu mando
3. Cambiar `test_timeout_ms` al tiempo de espera deseado
4. Ejecute verificar nuevamente

## Requisitos previos

Verificar requiere:

- **Session harness initialized** - Correr `/agileflow:session:init` primero
- **docs/00-meta/environment.json** - Archivo de configuración de prueba
- **docs/09-agents/status.json** - Seguimiento de historias
- **Working tests** - Su proyecto debe tener un conjunto de pruebas.

### Inicializar sesión de arnés

Si recibe el error "Arnés de sesión no inicializado":

```bash
/agileflow:session:init
```

Esto:
- Detecta tu tipo de proyecto
- Crear entorno.json con configuración de prueba
- Configurar la verificación de prueba para todas las historias

## Integración con otros comandos

Verify es utilizado automáticamente por:

| Command | When |
|---------|------|
| `/babysit` | After implementing code changes |
| `/baseline` | Before creating verified checkpoints |
| `/session:resume` | At start of work session |
| `/pr` | Can include test status in PR description |

También puede ejecutar la verificación manualmente en cualquier momento para verificar el estado.

## Escenarios comunes

### "¿Están pasando mis pruebas?"

```bash
/agileflow:verify
# Shows immediate pass/fail status with counts
```

### "Reparar pruebas fallidas"

```bash
# See which tests failed
/agileflow:verify

# Fix the failing tests in your code editor
# Then verify again
/agileflow:verify
# Should show ✅ PASSED now
```

### "Una historia fracasa, otras pasan"

```bash
# Check which story has failing tests
/agileflow:verify

# Review that story's test_status in output
# Focus on fixing just that story's tests

# Run verify with specific story to confirm fix
/agileflow:verify US-0042
```

### "Las pruebas son lentas y tardan demasiado"

```bash
# Timeout occurred
/agileflow:verify

# Options:
# 1. Increase timeout in environment.json
# 2. Run subset of tests: npm test -- --testPathPattern=auth
# 3. Optimize slow tests

# Increase timeout and try again
```

## Solución de problemas

### "Arnés de sesión no inicializado"

**Error:**
```text
⚠️  Session harness not initialized
Run /agileflow:session:init to set up test verification
```

**Fix:**
```bash
/agileflow:session:init
```

### "Pruebas no encontradas"

**Error:**
```text
❌ Test Command Failed
Command: npm test
Error: npm: command not found
```

**Fix:**
- Instalar dependencias: `npm install`
- Verifique que el comando de prueba en Environment.json coincida con su configuración
- Check PATH incluye las herramientas necesarias

### "Historia no encontrada"

**Error:**
```text
⚠️  Story US-0099 not found in status.json
Available in_progress stories:
- US-0042
- US-0043
```

**Fix:**
- Verifique que el ID de la historia sea correcto (verifique status.json)
- Ejecute sin ID de historia para verificar todas las historias en progreso

### "Estado.json no válido"

**Error:**
```text
❌ Cannot Update Status
File docs/09-agents/status.json is invalid or missing.
```

**Fix:**
```bash
# Re-initialize AgileFlow setup
/agileflow:setup
```

## Consejos

### Ejecute pruebas con frecuencia

```bash
# After every major change
/agileflow:verify

# Before committing
/agileflow:verify

# Before creating PR
/agileflow:verify

# Before baseline
/agileflow:verify
```

### Detectar las regresiones temprano

Verify rastrea automáticamente el estado de la prueba por historia, para que puedas:
- Ver cuando las pruebas fallan (cambios test_status)
- Identificar qué historia introdujo el fracaso.
- Solucionar los problemas antes de que se acumulen

### Usar con línea de base

```bash
# Ensure all tests pass
/agileflow:verify

# If all passing:
/agileflow:baseline "Milestone complete"

# If any failing:
# (Fix first, then baseline)
```

## Comandos relacionados

- [`/cuidar`](/commands/babysit) - Mentor de implementación (ejecuta la verificación automáticamente)
- [`/línea de base`](/commands/baseline) - Crear puntos de control verificados (requiere pasar pruebas)
- [`/sesión:inicio`](/commands/session-init) - Inicializar sesión de arnés
- [`/sesión:reanudar`](/commands/session-resume) - Reanudar sesión (ejecuta verificar)
- [`/pr`](/commands/pr) - Generar solicitud de extracción (muestra el estado de la prueba)
