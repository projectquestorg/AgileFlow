---
title: /deps
description: Visualize o gráfico de dependência com detecção de caminho crítico
---

# /deps

Visualize e analise gráficos de dependências históricas/épicas com detecção de caminho crítico, identificação de dependência circular e recomendações acionáveis.

## Início rápido

```bash
/agileflow:deps
```

## Parâmetros

| Parameter | Required | Default | Description |
|-----------|----------|---------|-------------|
| `SCOPE` | No | `all` | `story`, `epic`, or `all` - scope of visualization |
| `EPIC` | No | - | Show dependencies within specific epic (e.g., `EP-0010`) |
| `STORY` | No | - | Show dependencies for specific story (e.g., `US-0032`) |
| `FORMAT` | No | `ascii` | Output format: `ascii`, `mermaid`, `graphviz`, or `json` |
| `ANALYSIS` | No | `all` | Analysis type: `critical-path`, `circular`, `blocking`, or `all` |

## Exemplos

### Ver gráfico de dependência completo

```bash
/agileflow:deps
```

Mostra gráfico ASCII de todas as histórias e suas dependências com indicadores de status, caminho crítico e análise de história de bloqueio.

### Analise dependências épicas

```bash
/agileflow:deps EPIC=EP-0010
```

Visualiza dependências apenas dentro do épico de autenticação, ajudando a entender a ordem de trabalho.

### Verifique dependências específicas da história

```bash
/agileflow:deps STORY=US-0032
```

Mostra do que essa história depende e quais histórias dependem dela, além de análise de impacto.

### Exportar como Diagrama Sereia

```bash
/agileflow:deps FORMAT=mermaid
```

Gera formato de diagrama Mermaid adequado para documentação ou GitHub.

### Encontre apenas o caminho crítico

```bash
/agileflow:deps ANALYSIS=critical-path
```

Mostra apenas o caminho de dependência mais longo (gargalo para conclusão do projeto).

### Detectar dependências circulares

```bash
/agileflow:deps ANALYSIS=circular
```

Verifica ciclos impossíveis de concluir e sugere soluções.

## Formato de saída

### Gráfico ASCII (padrão)

```text
Story Dependency Graph
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Legend:
  ✅ Done    🟢 Ready    🟡 In Progress    🔵 In Review    ⚪ Blocked

EP-0010: Authentication Epic
┌────────────────────────────────────────────────────────────┐
│                                                            │
│  ✅ US-0030 - Database schema                              │
│  │                                                         │
│  ├─▶ ✅ US-0031 - User model                               │
│  │   │                                                     │
│  │   ├─▶ 🟡 US-0032 - Login endpoint (in-progress)        │
│  │   │   │                                                 │
│  │   │   ├─▶ 🟢 US-0033 - Login UI                         │
│  │   │   │                                                 │
│  │   │   └─▶ ⚪ US-0045 - Password reset (blocked)         │
│  │   │                                                     │
│  │   └─▶ ✅ US-0034 - JWT middleware                       │
│  │                                                         │
│  └─▶ 🔵 US-0035 - User registration (in-review)            │
│                                                            │
└────────────────────────────────────────────────────────────┘

Critical Path:
  US-0030 → US-0031 → US-0032 → US-0033  (11 days total)

Blocking Stories:
  US-0040 blocks: US-0041, US-0042  (⚠️ High impact)
  US-0032 blocks: US-0033, US-0045  (⚠️ High impact)

Circular Dependencies: None detected ✅
```

### Formato de diagrama de sereia

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="/images/docs/commands/deps-graph.dark.svg" />
  <img alt="Dependency Graph Example" src="/images/docs/commands/deps-graph.light.svg" />
</picture>

## Tipos de análise

### Caminho Crítico
**Shows**: A cadeia de dependência mais longa do início ao fim

Ajuda a identificar:
- Quais histórias são gargalos
- Caminho mais crítico para a conclusão do projeto
- Onde os atrasos têm impacto máximo
- Histórias a serem priorizadas para conclusão

```text
Critical Path: US-0030 → US-0031 → US-0032 → US-0033
Duration: 8 days
⚠️ US-0032 is in-progress on critical path
   → Any delay here delays entire epic
```

### Dependências Circulares
**Shows**: Histórias que dependem umas das outras em um ciclo

Exemplo de problema de dependência circular:
```text
❌ CIRCULAR DETECTED!

Cycle: US-0050 → US-0051 → US-0052 → US-0050

Impact: ⚠️ CRITICAL - These stories cannot be completed
```

**How to fix**:
1. Revise o projeto arquitetônico
2. Quebre a dependência introduzindo abstração
3. Criar história de interface/contrato
4. Refatore uma história para quebrar o ciclo

### Bloqueando histórias
**Shows**: Histórias que bloqueiam a maioria dos outros trabalhos

Identifica gargalos de alto impacto:
```text
High Impact Blockers:
┌────────┬──────────────────┬────────┬──────────────┐
│ Story  │ Title            │ Status │ Blocks       │
├────────┼──────────────────┼────────┼──────────────┤
│ US-0040│ Stripe integ.    │ 🟢 Ready│ 2 stories  │
│ US-0032│ Login endpoint   │ 🟡 WIP │ 2 stories  │
└────────┴──────────────────┴────────┴──────────────┘

Action: Start US-0040 immediately (blocks payment epic)
```

## Indicadores de status da história

| Indicator | Meaning | Action |
|-----------|---------|--------|
| ✅ Done | Story completed | Doesn't block others anymore |
| 🟢 Ready | Ready for development | Can be started anytime |
| 🟡 In Progress | Currently being worked | Monitor for delays |
| 🔵 In Review | Awaiting approval | May complete soon |
| ⚪ Blocked | Waiting on dependencies | Can't start until unblocked |

## Cenários Comuns

### Cenário: a história tem alta prioridade, mas está bloqueada

```text
⚪ US-0045 - Password reset (blocked)
   Waiting for: US-0032 (Login endpoint, 75% done)

Recommendation:
  - Check US-0032 progress daily
  - Consider pairing to accelerate if delayed
  - US-0045 can start as soon as US-0032 is done
```

### Cenário: Encontrada uma Dependência Circular

```text
❌ Services have circular dependency

Current: Auth → User → Session → Auth (cycle)

Solution:
  1. Create US-0055: "Auth/User service contract"
  2. Make all three depend on US-0055 (new foundation)
  3. Now: Auth/User/Session → US-0055 (no cycle)
```

### Cenário: História do Caminho Crítico em Risco

```text
US-0032 is on CRITICAL PATH but in-progress
⚠️ Currently 75% done, but original estimate was 2d

If it slips:
  - US-0033 blocked (1d delay)
  - US-0045 blocked (2d delay)
  - Entire epic completion delayed

Recommendation:
  - Daily standup on progress
  - Pair if needed to accelerate
  - Identify blockers immediately
```

## Oportunidades de trabalho paralelo

O comando identifica histórias que podem ser trabalhadas em paralelo:

```text
Stories That Can Start Now (No Blockers):

EP-0010: Authentication
  ✅ US-0033 (Login UI)         - AG-UI available
  ✅ US-0040 (Stripe integ.)    - HIGH PRIORITY

Recommended Assignments:
  AG-UI:  US-0033 (ready now)
  AG-API: US-0040 (high impact blocker)
```

## Integração com outros comandos

- **Before sprint planning**: Correr `/deps` para entender os riscos
- **During sprint**: Monitore o caminho crítico com `/board`
- **When investigating blockers**: Usar `/deps` para ver o que depende dessa história
- **When adding dependencies**: Execute novamente para verificar novos riscos
- **When a story is blocked**: Usar `/deps STORY=US-XXXX` para ver a corrente

## Melhores Práticas

- **Check before each sprint** - As dependências podem mudar à medida que as histórias são refinadas
- **Prioritize blocking stories** - Iniciar bloqueadores de alto impacto cedo
- **Monitor critical path** - Histórias no caminho crítico merecem atenção extra
- **Detect cycles early** - Correr `/deps ANALYSIS=circular` regularmente
- **Sequence dependent work** - Comece as histórias fundamentais primeiro
- **Document dependencies clearly** - Use o frontmatter da história para vincular dependências

## Comandos Relacionados

- [`/sprint`](/commands/sprint) - O planejamento do sprint respeita as dependências
- [`/placa`](/commands/board) - Veja o progresso do sprint e bloqueadores
- [`/bloqueadores`](/commands/blockers) - Rastreie e resolva histórias bloqueadas
- [`/validar história`](/commands/story-validate) - Validar dependências existentes
- [`/métricas`](/commands/metrics) - Correlacionar o tempo do ciclo com a profundidade da dependência
