---
title: /Rezension
description: KI-gestützte Codeüberprüfung mit Qualitätsvorschlägen
---
# /rezension

Führen Sie eine umfassende KI-gestützte Codeüberprüfung bei Pull-Request-Änderungen durch. Review analysiert Code auf Qualität, Sicherheit, Leistung und Best Practices und liefert umsetzbares Feedback mit spezifischen Codebeispielen.

## Schnellstart

```bash
# Review current branch against main
/agileflow:review

# Review specific branch
/agileflow:review BRANCH=feature/auth

# Review with focus on security
/agileflow:review FOCUS=security
```

Der Befehl wird:
1. Codeänderungen abrufen (git diff)
2. Analysieren Sie in 6 Kategorien (Qualität, Sicherheit, Leistung, Best Practices, Tests, Dokumentation).
3. Erstellen Sie einen detaillierten Bericht mit nach Schweregrad geordneten Problemen
4. Berechnen Sie den Codequalitätswert
5. Schlagen Sie automatische Korrekturen und Folgearbeiten vor

## Hauptmerkmale

### Umfassende Analyse

Review untersucht Ihre Codeänderungen in 6 wichtigen Kategorien:

| Kategorie | Schecks |
|----------|--------|
| **Codequalität** | Komplexität, Duplizierung, Benennung, Funktions-/Dateilänge |
| **Sicherheit** | SQL-Injection, XSS, hartcodierte Geheimnisse, unsichere Deserialisierung |
| **Leistung** | N+1 Abfragen, ineffiziente Algorithmen, Speicherlecks |
| **Best Practices** | Fehlerbehandlung, Protokollierung, Typsicherheit, asynchron/awaes |
| **Testen** | Fehlende Tests, Testqualität, Abdeckungslücken |
| **Dokumentation** | Fehlendes JSDoc, veraltete Kommentare, README-Updates |

### Schweregradbasierte Priorisierung

Probleme werden nach Auswirkung kategorisiert:

```text
🔴 CRITICAL - Must fix before merge
🟠 HIGH    - Fix before merge (blocks release)
🟡 MEDIUM  - Address soon (technical debt)
⚪ LOW     - Nice to have (style/consistency)
```

### Code-Qualitätsfaktor

Ihre PR erhält eine Punktzahl (0-100) mit Aufschlüsselung nach Kategorie:

```text
Code Quality Score: 72/100

Breakdown:
- Security: 40/100 (critical issues found)
- Performance: 75/100 (one N+1 query)
- Maintainability: 80/100 (some complexity)
- Testing: 65/100 (coverage gaps)
- Style: 90/100 (mostly consistent)
```

### Konstruktives Feedback

Jede Ausgabe enthält:
- Wo es ist (Datei, Zeilennummer)
- Warum es wichtig ist (Risiko/Auswirkung)
- So beheben Sie das Problem (spezifische Codebeispiele)
- Beispiel für ❌ SCHLECHTEN Code
- Beispiel für ✅ GUTEN Code

## Nutzung

### Basic – Überprüfen Sie den aktuellen Zweig

```bash
/agileflow:review
```

Überprüft Änderungen am aktuellen Zweig gegenüber Main/Master.

### Überprüfen Sie einen bestimmten Zweig

```bash
/agileflow:review BRANCH=feature/user-auth
```

Überprüft den benannten Zweig mit dem Hauptzweig.

### Überprüfung gegen andere Basis

```bash
/agileflow:review BRANCH=feature/auth BASE=develop
```

Vergleicht mit einem anderen Basiszweig.

### Rezension mit Fokusbereich

```bash
# Security-only review
/agileflow:review FOCUS=security

# Performance-only review
/agileflow:review FOCUS=performance

# Testing coverage review
/agileflow:review FOCUS=tests
```

### Nach Schweregrad filtern

```bash
# Show only critical/high issues
/agileflow:review SEVERITY=high

# Show all issues
/agileflow:review SEVERITY=all
```

## Parameter

| Parameter | Erforderlich | Standard | Optionen |
|-----------|----------|---------|---------|
| `BRANCH` | Nein | Aktuell | Zu überprüfender Filialname |
| `BASE` | Nein | main/master | Basiszweig zum Vergleich |
| `FOCUS` | Nein | alle | alles, Sicherheit, Leistung, Stil, Tests |
| `SEVERITY` | Nein | alle | kritisch, hoch, mittel, niedrig, alle |

## Beispiele

### Typische PR-Rezension

```bash
/agileflow:review

# Output:
# AI Code Review Report
# Branch: feature/user-auth
# Files Changed: 5
# Critical: 1 | High: 3 | Medium: 5 | Low: 2
# Code Quality Score: 72/100
#
# 🔴 CRITICAL ISSUES
# - Hardcoded API key in stripe.ts:15
#
# 🟠 HIGH ISSUES
# - N+1 query in posts API
# - Missing error handling in login
# ...
```

### Sicherheitsorientierte Überprüfung

```bash
/agileflow:review FOCUS=security

# Analyzes only for:
# - Hardcoded secrets
# - SQL injection risks
# - XSS vulnerabilities
# - Authentication issues
# - CORS misconfiguration
```

### Leistungsbeurteilung

```bash
/agileflow:review FOCUS=performance

# Checks for:
# - N+1 database queries
# - Inefficient algorithms
# - Missing indexes
# - Memory leaks
# - Bundle size issues
```

## Berichtsstruktur überprüfen

### Kopfzeile

```text
# AI Code Review Report

**Branch**: feature/user-auth
**Base**: main
**Files Changed**: 8
**Lines Added**: 245
**Lines Removed**: 32
**Generated**: 2025-10-16T10:30:00Z
```

### Zusammenfassung

```text
## Summary

**Critical**: 2 | **High**: 5 | **Medium**: 12 | **Low**: 8
**Must Fix Before Merge**: 7 issues
```

### Probleme nach Schweregrad

Für jede Ausgabe:

```text
### 1. Hardcoded API Key (SECURITY)
**File**: src/api/payments/stripe.ts:15
**Severity**: CRITICAL

// ❌ BAD
const stripeKey = "sk_live_abc123...";

// ✅ GOOD
const stripeKey = process.env.STRIPE_SECRET_KEY;
if (!stripeKey) throw new Error("STRIPE_SECRET_KEY not set");

**Risk**: API key exposed in version control.
**Fix**: Move to environment variable. Rotate immediately.
**Priority**: Block merge
```

### Positive Beobachtungen

```text
## Positive Observations ✅

- ✅ Good use of TypeScript strict mode
- ✅ Consistent error handling pattern
- ✅ Well-structured component separation
- ✅ Clear commit messages
```

### Aufschlüsselung der Codequalität

```text
## Code Quality Score: 72/100

**Breakdown**:
- Security: 40/100 (2 critical issues)
- Performance: 75/100 (1 N+1 query)
- Maintainability: 80/100 (some complexity)
- Testing: 65/100 (coverage gaps)
- Style: 90/100 (mostly consistent)
```

## Anpassung

### Projektspezifische Regeln

Erstellen Sie benutzerdefinierte Überprüfungsregeln in:
- `.agileflow/review-rules.md`
- `docs/02-practices/code-standards.md`

Beispiel:

```markdown
# Code Review Rules

## Critical
- No console.log in production code
- All API endpoints must have rate limiting
- All database queries must use ORM

## High
- Functions >30 lines should be refactored
- Test coverage must be >85%
```

## Aktionen nach der Überprüfung

### Probleme automatisch beheben

```text
Review asks: "Fix auto-fixable issues? (YES/NO)"

If YES:
  - Runs npm run lint -- --fix
  - Fixes 8 style issues automatically
  - Shows which issues were fixed
```

### Erstellen Sie Folgegeschichten

```text
Review asks: "Create stories for follow-up work?"

If YES:
  - Creates story for each medium/low issue
  - Adds to your backlog
  - Links to code review report
```

### Blockzusammenführung für Kritische Probleme

```text
Review asks: "Block merge for critical/high issues?"

If YES:
  - Marks merge as blocked
  - Provides failure reason
  - Suggests resolution steps
```

### Bericht speichern

Bericht automatisch gespeichert unter:
```text
docs/08-project/code-reviews/<YYYYMMDD>-<BRANCH>.md
```

Beispiel:
```text
docs/08-project/code-reviews/20251016-feature-user-auth.md
```

## Sicherheitsüberprüfungen

Die Überprüfung prüft automatisch Folgendes:

### Geheimnisse und Anmeldeinformationen
- Festcodierte API-Schlüssel
- Datenbank-Passwörter
- OAuth-Tokens
- JWT-Geheimnisse
- AWS-Anmeldeinformationen

### Häufige Schwachstellen
- SQL-Injection (unparametrisierte Abfragen)
- XSS-Angriffe (unentdeckte Ausgabe)
- Unsichere Deserialisierung
- CSRF-Schwachstellen
- Fehlende CORS-Validierung

### Unsichere Muster
- eval() oder Function()
– Unsichere Zeichenfolgenverkettung
- Fehlende Eingabevalidierung
- Schwache Verschlüsselung
- Unsichere Abhängigkeiten

## Leistungsprüfungen

Rezension sucht nach:

### Datenbank
- N+1-Abfrageprobleme
- Fehlende Indizes
- Ineffiziente Verknüpfungen
- Unnötige Abfragen

### Algorithmen
- O(n²), wenn O(n) möglich
- Verschachtelte Schleifen, die flach sein könnten
- Wiederholte Berechnungen
- Nicht optimierte Rekursion

### Erinnerung
- Speicherlecks
- Große Objekte werden nicht freigegeben
- Zirkelverweise
- Speicherbeanspruchende Datenstrukturen

## Testing-Abdeckung

Überprüfungsprüfungen:

### Neuer Code
- Gibt es einen Test für neue Funktionalität?
- Sind Randfälle abgedeckt?
- Ist die Testqualität gut?

### Deckungslücken
- Zeilen ohne Tests hinzugefügt
- Funktionen ohne Testabdeckung
- Fehlerpfade nicht getestet

### Testqualität
- Sind Aussagen sinnvoll?
- Verifizieren Tests das Verhalten?
- Sind Tests wartbar?

## Ausgabe und Empfehlungen

### Sofortige Aktionen (Blockzusammenführung)

```text
1. Remove hardcoded API key (critical)
2. Fix SQL injection (critical)
3. Add error handling to login (high)
```

### Vor dem Zusammenführen

```text
4. Add tests for auth endpoints (high)
5. Fix N+1 query in posts API (high)
6. Reduce complexity in validator.ts (medium)
```

### Follow-up (Stories erstellen)

```text
7. Standardize naming conventions (medium)
8. Add ESLint rule for secrets (medium)
9. Set up automated security scanning (low)
```

## Integration mit CI

Schlagen Sie vor, zu `.github/workflows/pr.yml` hinzuzufügen:

```yaml
- name: AI Code Review
  run: npx claude-code /agileflow:review BRANCH=${{ github.head_ref }}

- name: Check for critical issues
  run: |
    if grep -q "CRITICAL" code-review-report.md; then
      echo "::error::Critical issues found."
      exit 1
    fi
```

## Tipps

### Bevor Sie PR einreichen

```bash
/agileflow:review
# Fix any CRITICAL or HIGH issues
# Address MEDIUM issues if possible
# Then submit PR with confidence
```

### Iterative Verbesserung

```bash
# After feedback, run review again
/agileflow:review

# See if score improved
# Ensure critical issues fixed
# Check test coverage
```

### Security-First-Ansatz

```bash
# Always check security first
/agileflow:review FOCUS=security

# Fix any CRITICAL security issues
# Then review other categories
```

## Prinzipien

- **Seien Sie konstruktiv** – Geben Sie umsetzbares Feedback, nicht nur Kritik
- **Beispiele anzeigen** - ❌ SCHLECHTEN und ✅ GUTEN Code einschließen
- **Priorisieren** – Kritische Themen zuerst, Stil zuletzt
- **Feiern** - Positive Beobachtungen einbeziehen
- **VorschlagST-Fixes** – Identifizieren Sie nicht nur Probleme
- **Keine Force-Commits** - Fragen Sie vor der automatischen Korrektur immer nach

## Verwandte Befehle

- [`/pr`](/commands/pr) - PR-Beschreibung erstellen (Überprüfung vor dem Absenden)
- [`/impact`](/commands/impact) – Auswirkungen der Änderung analysieren
- [`/babysit`](/commands/babysit) – Implementierungsleitfaden (führt Überprüfung vor PR durch)
- [`/verify`](/commands/verify) – Überprüfen Sie, ob die Tests bestanden wurden
- [`/baseline`](/commands/baseline) – Verifizierten Prüfpunkt erstellen