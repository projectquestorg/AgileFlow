---
title: /sincronización de documentos
description: Sincronizar documentación con cambios de código
---

# /sincronización de documentos

Sincronice su documentación con los cambios en la base del código detectando código nuevo y asegurándose de que exista la documentación correspondiente.

## Inicio rápido

```bash
/agileflow:docs-sync
```

Para comparar con una rama específica:

```bash
/agileflow:docs-sync BRANCH=feature-auth BASE=main
```

## Parámetros

| Parameter | Required | Default | Description |
|-----------|----------|---------|-------------|
| `BRANCH` | No | Current branch | Branch to analyze for changes |
| `BASE` | No | main/master | Base branch to compare against |
| `AUTO_CREATE` | No | no | Automatically create missing documentation (yes/no) |

## Ejemplos

### Analizar sucursal actual

```bash
/agileflow:docs-sync
```

Analiza tu sucursal actual contra `main` e informes:
- Nuevos puntos finales API sin documentación
- Nuevos componentes de UI sin documentación.
- Cambios de configuración no documentados
- Documentación obsoleta

### Comparar rama de funciones

```bash
/agileflow:docs-sync BRANCH=feature-oauth BASE=main
```

compara `feature-oauth` contra `main` para encontrar todas las lagunas de documentación.

### Crear automáticamente documentos faltantes

```bash
/agileflow:docs-sync BRANCH=feature-payments AUTO_CREATE=yes
```

Genera automáticamente documentación resguardada para todos los elementos que faltan y se confirma con un mensaje `docs: sync with codebase changes`.

## Producción

El comando genera un informe completo:

```markdown
# Documentation Sync Report

**Branch**: feature-auth | **Base**: main | **Generated**: 2025-12-24T10:30:00Z

## Missing Documentation

### API Endpoints (3)
- ❌ POST /api/auth/login (src/api/auth/login.ts)
- ❌ POST /api/auth/logout (src/api/auth/logout.ts)
- ❌ GET /api/auth/verify (src/api/auth/verify.ts)

### UI Components (2)
- ❌ LoginForm (src/components/LoginForm.tsx)
- ⚠️ UserProfile (src/components/UserProfile.tsx) - stub exists, needs details

### Configuration (1)
- ❌ New env var: JWT_SECRET (.env.example)

## Outdated Documentation

### Deprecated
- 📄 docs/04-architecture/api.md mentions /api/v1/login (removed in this branch)

## Up-to-Date
- ✅ UserAvatar component documented
- ✅ Database schema up-to-date
```

## Cómo funciona

### Detección de cambios

El comando:
1. **Compares branches** usando `git diff` para identificar todos los cambios de código
2. **Categorizes changes** en puntos finales de API, componentes de UI, servicios, configuración y esquemas de bases de datos
3. **Maps to documentation** Ubicaciones donde deberían existir los documentos.

### Inferencia inteligente

La documentación se infiere de:
- **TypeScript types and interfaces** - Firmas de funciones y tipos de retorno.
- **JSDoc comments** - Documentación de código existente
- **Test files** - Ejemplos de uso
- **OpenAPI decorators** - Especificaciones API

### Análisis de brechas

Para cada archivo modificado, el comando verifica:
- **Missing documentation**: Código recién agregado sin documentos
- **Outdated documentation**: Referencias a código eliminado o modificado
- **Incomplete documentation**: Talones que necesitan detalles

## Mapeo de documentación

Los cambios de código se asignan a la documentación esperada:

| Code Location | Expected Documentation |
|--------------|----------------------|
| `src/api/` or `src/routes/` | `docs/04-architecture/api.md` or OpenAPI spec |
| `src/components/` or `src/pages/` | `docs/04-architecture/components.md` or Storybook |
| `src/services/` or `src/utils/` | `docs/04-architecture/services.md` |
| `*.config.js`, `.env.example` | `docs/02-practices/configuration.md` |
| `migrations/` or `schema/` | `docs/04-architecture/database.md` |

## Mejores prácticas

### Antes de cometer el código
Correr `docs-sync` Para identificar lagunas en la documentación con antelación:

```bash
/agileflow:docs-sync BRANCH=your-feature BASE=main
```

### Al agregar nuevos puntos finales
El comando los marcará automáticamente, lo que facilitará recordar la documentación:

```bash
/agileflow:docs-sync AUTO_CREATE=yes
```

Esto crea resguardos marcados con "TODO: Agregar detalles" para su revisión manual.

### Secciones de documentación a mantener
Manténgalos sincronizados con el código:
- Ejemplos de solicitud/respuesta de API
- Definiciones de accesorios de componentes
- Opciones de configuración
- Cambios en el esquema de la base de datos
- Puntos finales de API obsoletos

## Comandos relacionados

- [`/ contexto`](/commands/context) - Generar un resumen contextual completo.
- [`/readme-sync`](/commands/readme-sync) - Sincronizar archivos README con el contenido de la carpeta
- [`/ dirección`](/commands/adr) - Documentar decisiones arquitectónicas.
