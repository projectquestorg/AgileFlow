---
title: /habilidade:teste
description: Verifique se uma habilidade funciona corretamente testando sua ativação e funcionalidade
---

# /habilidade:teste

Teste uma habilidade para verificar se ela é ativada corretamente, tem estrutura adequada e produz os resultados esperados.

## Início rápido

```bash
# Interactive mode
/agileflow:skill:test

# Test specific skill
/agileflow:skill:test supabase-swift
```

## O que isso faz

O comando de teste valida habilidades através de três fases:

1. **Structure Validation** - Os arquivos existem e estão formatados corretamente
2. **Content Validation** - Seções obrigatórias presentes, sem links quebrados
3. **Activation Test** (opcional) - Verifique os gatilhos de habilidade em exemplos de prompts

## Fluxo de trabalho

### Fase 1: Selecionar Habilidade

Se você não especificar uma habilidade:

```bash
/agileflow:skill:test
```

**Choose from available skills:**
```
Which skill would you like to test?

[1] supabase-swift
    Supabase database operations for Swift iOS apps

[2] ui-components
    React component patterns with accessibility

[3] api-error-handling
    REST API error handling patterns

Select: 1
```

### Fase 2: Validação da Estrutura

O teste executa verificações automatizadas:

```
🧪 Testing: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE VALIDATION
  ✅ SKILL.md exists
  ✅ Frontmatter valid (name: supabase-swift, description: present)
  ✅ references.md exists
  ✅ cookbook/ directory found (2 entries)
  ⚠️  No .mcp.json (MCP not configured)

CONTENT VALIDATION
  ✅ "When to Use" section present
  ✅ Description under 1024 characters (467 chars)
  ✅ SKILL.md under 500 lines (287 lines)
  ✅ All cookbook files referenced in SKILL.md exist
  ✅ No broken file references

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Fase 3: Teste de ativação opcional

Após validação da estrutura:

```
Would you like to test skill activation with a sample prompt?

[1] Yes, test activation (Recommended)
    I'll simulate using the skill

[2] Skip activation test
    Structure validation is enough

[3] View SKILL.md content
    Read the full skill instructions

Select: 1
```

Se você escolher o teste de ativação:

```
Sample prompts that should activate this skill:

[1] "Help me set up Supabase authentication in my iOS app"
[2] "Create CRUD operations for my user database"
[3] "How do I query Supabase from Swift?"

Which would you like to test? (or enter custom prompt)
> 1
```

### Fase 4: Resultados do Teste

```
🧪 Test Results: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE VALIDATION
  ✅ PASS (5/5 checks)

CONTENT VALIDATION
  ✅ PASS (5/5 checks)

ACTIVATION TEST
  ✅ PASS - Skill triggered on test prompt
  ✅ Response includes cookbook references
  ✅ All referenced files are accessible

MCP CONFIGURATION
  ⚠️  NOT CONFIGURED - Skills work without MCP

OVERALL: ✅ SKILL IS FUNCTIONAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Verificações de validação

### Verificações de estrutura

| Check | What It Verifies |
|-------|------------------|
| **SKILL.md exists** | Main skill file is present |
| **Frontmatter valid** | Has `name:` and `description:` |
| **references.md** | Documentation links file (recommended) |
| **cookbook/ directory** | Workflows exist (if referenced) |
| **.mcp.json valid** | JSON syntax correct (if present) |

### Verificações de conteúdo

| Check | What It Verifies |
|-------|------------------|
| **"When to Use" section** | Activation triggers defined |
| **Description length** | Under 1024 characters |
| **SKILL.md size** | Under 500 lines |
| **Cookbook references** | All files in SKILL.md exist |
| **No broken links** | Local file paths are valid |

### Indicadores

- **✅ PASS** - Cheque aprovado
- **⚠️ WARNING** - Arquivo opcional ausente (não obrigatório)
- **❌ FAIL** - Falha na verificação obrigatória

## Exemplos

### Exemplo 1: Habilidade de aprovação em teste

```bash
/agileflow:skill:test react-components
```

**Output:**
```
🧪 Test Results: react-components
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE:     ✅ PASS (all 5 checks)
CONTENT:       ✅ PASS (all 5 checks)
ACTIVATION:    ✅ PASS (triggered correctly)

Overall: ✅ FULLY FUNCTIONAL
```

### Exemplo 2: Habilidade com Avisos

```bash
/agileflow:skill:test api-basic
```

**Output:**
```
🧪 Test Results: api-basic
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE:     ⚠️  WARNINGS (3/5 checks)
  ✅ SKILL.md exists
  ✅ Frontmatter valid
  ⚠️  No references.md (recommended but not required)
  ⚠️  No cookbook/ directory
  ✅ No .mcp.json (simple skill)

CONTENT:       ✅ PASS (all 5 checks)
ACTIVATION:    ✅ PASS (triggered correctly)

Overall: ⚠️  FUNCTIONAL WITH RECOMMENDATIONS

Recommendations:
  - Add references.md to link external documentation
  - Consider adding cookbook/ entries for multiple use cases
```

### Exemplo 3: Habilidade com Falhas

```bash
/agileflow:skill:test broken-skill
```

**Output:**
```
🧪 Test Results: broken-skill
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE:     ❌ FAIL (1/5 checks)
  ❌ SKILL.md missing or corrupt
  ❌ Cannot parse frontmatter
  ⚠️  references.md exists
  ⚠️  No cookbook/
  ⚠️  No .mcp.json

CONTENT:       ⚠️  SKIPPED (structure issues)
ACTIVATION:    ❌ FAIL (cannot test without valid SKILL.md)

Overall: ❌ SKILL NEEDS FIXING

Issues Found:
  1. SKILL.md frontmatter is invalid
  2. Missing "When to Use" section
  3. Description exceeds 1024 characters

Fix With: /agileflow:skill:edit broken-skill
```

## Próximas ações após o teste

### Se o teste passar

```
✅ Skill is working perfectly!

What would you like to do?

[1] Test another skill
[2] Use this skill now
[3] Upgrade with learning capability
    /agileflow:skill:upgrade <skill-name>
[4] Done
```

### Se o teste tiver avisos

```
⚠️  Skill works but could be improved.

What would you like to do?

[1] Edit skill to fix issues
    /agileflow:skill:edit <skill-name>
[2] Ignore warnings and continue
[3] View detailed report
[4] Done
```

### Se o teste falhar

```
❌ Skill has issues that need fixing.

What would you like to do?

[1] Edit skill to fix problems
    /agileflow:skill:edit <skill-name>
[2] Delete and recreate skill
    /agileflow:skill:delete <skill-name>
[3] View detailed report
[4] Done
```

## Interpretando Resultados

### Problemas de estrutura

**Problem**: "SKILL.md faltando"
- **Cause**: O diretório Skill não contém SKILL.md
- **Fix**: Verifique se o diretório existe: `ls .claude/skills/<name>/SKILL.md`
- **Solution**: Usar `/skill:edit` recriar

**Problem**: "Frontmatter inválido"
- **Cause**: SKILL.md YAML está malformado
- **Fix**: Verifique se as primeiras 5 linhas têm YAML válido:
  ```yaml
  ---
  name: skill-name
  description: Purpose
  ---
  ```
- **Solution**: Usar `/skill:edit` para consertar o frontmatter

**Problem**: "Sem seção 'Quando usar'"
- **Cause**: Gatilhos de ativação não definidos
- **Fix**: Adicionar seção ao SKILL.md:
  ```markdown
  ## When to Use

  This skill activates when:
  - Condition 1
  - Keywords: "keyword1", "keyword2"
  ```
- **Solution**: Usar `/skill:edit` para adicionar seção

### Problemas de conteúdo

**Problem**: "A descrição excede 1.024 caracteres"
- **Cause**: A descrição da habilidade é muito longa
- **Fix**: Encurte a descrição no frontmatter SKILL.md
- **Solution**: Usar `/skill:edit` para reduzir o comprimento

**Problem**: "SKILL.md excede 500 linhas"
- **Cause**: A habilidade é muito grande/complexa
- **Fix**: Mover conteúdo para livro de receitas/arquivos
- **Solution**: Usar `/skill:edit` reorganizar

**Problem**: "Link quebrado: cookbook/missing.md referenciado, mas não encontrado"
- **Cause**: Referência de arquivo do livro de receitas sem arquivo real
- **Fix**: Crie o arquivo ou remova a referência
- **Solution**: Usar `/skill:edit` consertar

### Problemas de MCP

**Problem**: "Json inválido em .mcp.json"
- **Cause**: Erro de sintaxe na configuração do MCP
- **Fix**: Verifique se a sintaxe JSON é válida
- **Solution**: Usar `/skill:edit` corrigir JSON

**Problem**: "Nenhum objeto mcpServers"
- **Cause**: .mcp.json sem estrutura necessária
- **Fix**: Adicione estrutura adequada:
  ```json
  {
    "mcpServers": {
      "name": { ... }
    }
  }
  ```
- **Solution**: Usar `/skill:edit` atualizar

## Teste de fluxo de trabalho

### Teste rápido (somente estrutura)

```bash
/agileflow:skill:test supabase-swift
# When asked: select "Skip activation test"
```

Leva alguns segundos para verificar a estrutura e o conteúdo do arquivo.

### Teste Completo (Estrutura + Ativação)

```bash
/agileflow:skill:test supabase-swift
# When asked: select "Yes, test activation"
# When asked: pick or enter test prompt
```

Testa se a habilidade realmente é ativada em prompts relevantes.

### Teste Detalhado (Estrutura + Ativação + Visualização de Conteúdo)

```bash
/agileflow:skill:test supabase-swift
# When asked: select "View SKILL.md content"
# Then manually review the skill
```

Ajuda a entender o que a habilidade faz antes de usá-la.

## Melhores Práticas

### Teste após a criação

Sempre teste uma habilidade logo após criá-la:

```bash
/agileflow:skill:create my-skill
/agileflow:skill:test my-skill  # ← Right after creation
```

### Teste após edição

Teste depois de fazer alterações:

```bash
/agileflow:skill:edit my-skill
/agileflow:skill:test my-skill  # ← Verify changes worked
```

### Auditorias Regulares

Teste todas as habilidades periodicamente:

```bash
/agileflow:skill:list
# For each skill:
/agileflow:skill:test <skill-name>
```

### Teste novos gatilhos

Ao adicionar novos gatilhos de ativação:

```bash
/agileflow:skill:edit my-skill  # Add trigger keywords
/agileflow:skill:test my-skill  # Test with those keywords
```

## Solução de problemas

### Teste trava

Se o teste parecer travar:

1. **Wait up to 30 seconds** - O teste de ativação pode levar algum tempo
2. **Check internet connection** - Se estiver testando fontes da web
3. **Cancel (Ctrl+C)** e tente novamente
4. **Skip activation test** - Experimente o teste apenas de estrutura

### Resultados inconsistentes

Se os resultados do teste variarem:

1. **Network issues** - Algumas fontes podem estar temporariamente indisponíveis
2. **Race conditions** - Tente testar novamente
3. **Check file system** - Certifique-se de que os arquivos sejam legíveis:
   ```bash
   ls -la .claude/skills/<skill>/
   ```

### Falha no teste de ativação

Se "Habilidade acionada corretamente" aparecer como falha:

1. Atualizar gatilhos de ativação:
   ```bash
   /agileflow:skill:edit <skill-name>
   # Edit "When to Use" section
   ```

2. Teste com prompts diferentes

3. Verifique se as palavras-chave são específicas o suficiente

## Comandos Relacionados

- [`/habilidade:criar`](/commands/skill-create) - Gerar uma nova habilidade
- [`/habilidade:lista`](/commands/skill-list) - Ver todas as habilidades
- [`/habilidade:editar`](/commands/skill-edit) - Corrigir problemas de habilidade
- [`/habilidade:excluir`](/commands/skill-delete) - Remover uma habilidade
- [`/habilidade:atualizar`](/commands/skill-upgrade) - Adicionar capacidade de aprendizagem

## Próximas etapas

1. **Test a skill**: `/agileflow:skill:test <skill-name>`
2. **If passes**: Use a habilidade em seu trabalho
3. **If fails**: Editar com `/agileflow:skill:edit`
4. **After fixing**: Teste novamente para verificar
