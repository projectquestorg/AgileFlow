---
title: /habilidad:eliminar
description: Eliminar una habilidad instalada de .claude/skills/
---

# /habilidad:eliminar

Eliminar de forma segura una habilidad de `.claude/skills/` con funciones de seguridad completas que incluyen vista previa, confirmación y copia de seguridad opcional.

## Inicio rápido

```bash
# Interactive mode
/agileflow:skill:delete

# Delete specific skill
/agileflow:skill:delete supabase-swift
```

## Qué hace

El comando de eliminación:

1. **Shows what will be deleted** - Vista de árbol de todos los archivos.
2. **Confirms with you** - Requiere aprobación explícita
3. **Offers backup option** - Guarde una copia antes de eliminarla.
4. **Permanently removes** - Elimina el directorio de habilidades.
5. **Suggests next actions** - Qué hacer después de la eliminación

## Características de seguridad

Este comando tiene múltiples capas de seguridad:

- **No accidental deletion** - Confirmar siempre antes de retirar
- **Tree view preview** - Ver exactamente lo que se elimina
- **Backup option** - Exportar habilidad antes de eliminarla.
- **One at a time** - Eliminar habilidades individuales (sin eliminación por lotes)
- **Clear confirmation** - Opción explícita SÍ/NO

## Flujo de trabajo

### Paso 1: seleccione la habilidad

Si no especificas una habilidad:

```bash
/agileflow:skill:delete
```

**Choose from available skills:**
```
Which skill would you like to delete?

[1] supabase-swift
    Supabase database operations for Swift iOS apps

[2] ui-components (deprecated)
    Old component patterns (no longer using)

[3] api-error-handling
    REST API error handling patterns

Select: 2
```

### Paso 2: Vista previa de lo que se eliminará

El comando muestra exactamente lo que hay en la habilidad:

```
⚠️  About to delete: ui-components
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This will permanently remove:

  .claude/skills/ui-components/
  ├── SKILL.md
  ├── references.md
  ├── cookbook/
  │   ├── create-component.md
  │   ├── styling-patterns.md
  │   └── accessibility.md
  └── .mcp.json

Total: 6 files in 2 directories

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Paso 3: confirmar la eliminación

```
Are you sure you want to delete this skill?
This cannot be undone.

[1] Yes, delete permanently
    Remove the skill and all its files

[2] No, keep it
    Cancel and return

[3] Export first
    Save a backup before deleting

Select: 3
```

### Paso 4: crear una copia de seguridad (si se solicita)

Si elige "Exportar primero":

```
📦 Creating backup...

Backup saved to:
  .claude/skills-backup/ui-components-20251209-143052/

This contains a copy of everything. You can restore it later
if needed, or just delete the backup directory.

Ready to delete? (Yes/No)
```

Ubicación de la copia de seguridad:
```
.claude/skills-backup/
└── ui-components-20251209-143052/
    ├── SKILL.md
    ├── references.md
    ├── cookbook/
    └── .mcp.json
```

### Paso 5: Eliminar confirmado

```
✅ Deleted: ui-components

The skill has been permanently removed from .claude/skills/
```

### Paso 6: Próximas Acciones

Después de la eliminación, se le ofrecen opciones:

```
What would you like to do next?

[1] Delete another skill
    Remove more skills

[2] Create new skill
    Generate a replacement skill

[3] List remaining skills
    See what's still installed

[4] Done
    Exit skill management

Select: 4
```

## Ejemplos

### Ejemplo 1: Eliminar habilidad obsoleta

```bash
/agileflow:skill:delete old-pattern
```

**Response:**
```
⚠️  About to delete: old-pattern
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This will permanently remove:
  .claude/skills/old-pattern/
  ├── SKILL.md
  └── references.md

Total: 2 files

Are you sure? (Yes/No/Export first)
> Yes

✅ Deleted: old-pattern
```

### Ejemplo 2: Eliminar con copia de seguridad

```bash
/agileflow:skill:delete supabase-swift
```

**Response:**
```
⚠️  About to delete: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This will permanently remove:
  .claude/skills/supabase-swift/
  ├── SKILL.md
  ├── references.md
  ├── cookbook/ (3 files)
  └── .mcp.json

Total: 6 files

Are you sure? (Yes/No/Export first)
> Export first

📦 Creating backup...

Backup saved to:
  .claude/skills-backup/supabase-swift-20251209-150000/

Ready to delete? (Yes/No)
> Yes

✅ Deleted: supabase-swift
```

### Ejemplo 3: Selección interactiva

```bash
/agileflow:skill:delete
```

**Response:**
```
Which skill would you like to delete?

[1] api-integration
[2] database-queries
[3] react-components

Select: 2

⚠️  About to delete: database-queries
...
```

## Gestión de copias de seguridad

### Ubicación de la copia de seguridad

Las copias de seguridad se guardan con marca de tiempo:

```
.claude/skills-backup/
├── ui-components-20251208-093000/
├── api-client-20251208-103500/
└── database-20251209-150000/
```

Format: `<skill-name>-<YYYYMMDD-HHMMSS>/`

### Restaurar desde copia de seguridad

Si eliminas una habilidad y quieres recuperarla:

```bash
# Copy from backup
cp -r .claude/skills-backup/supabase-swift-20251209-150000 \
      .claude/skills/supabase-swift

# Verify it works
/agileflow:skill:test supabase-swift
```

### Limpiar copias de seguridad antiguas

Elimine periódicamente las copias de seguridad antiguas:

```bash
# List backups
ls -la .claude/skills-backup/

# Remove old backup
rm -rf .claude/skills-backup/old-skill-20250101-120000/
```

## Cuando eliminar

Eliminar habilidades cuando:

- **No longer needed** - La tecnología está en desuso
- **Outdated** - Existe una mejor alternativa
- **Duplicated** - Consolidado en una habilidad
- **Low quality** - La habilidad no es útil
- **Organization** - Demasiadas habilidades similares

## Antes de eliminar

Considerar:

1. **Are you sure?** - La eliminación es permanente
2. **Document why** - Nota en git commit
3. **Backup first** - Utilice la opción "Exportar primero"
4. **Create replacement** - Si consolida habilidades
5. **Notify team** - Si se comparte en git

## Alternativa a eliminar

En lugar de eliminar, podrías:

1. **Rename** para indicar obsoleto:
   ```bash
   /agileflow:skill:edit <skill-name>
   # Select "Rename skill"
   # Rename to: deprecated-<old-name>
   ```

2. **Archive** al salir de las habilidades activas:
   ```bash
   mv .claude/skills/old-skill .claude/skills-archive/
   ```

3. **Mark in documentation**:
   - Edite SKILL.md para agregar un aviso de obsolescencia
   - Aún disponible pero marcado como obsoleto

## Manejo de errores

### Habilidad no encontrada

```
❌ Skill "<name>" not found.

Available skills:
  - supabase-swift
  - ui-components
  - api-error-handling

Use /agileflow:skill:list to see all skills.
```

**Fix**: compruebe la ortografía del nombre de la habilidad o enumere las habilidades disponibles.

### No hay habilidades para eliminar

```
❌ No skills to delete.

No skills are currently installed in .claude/skills/

Create a skill first: /agileflow:skill:create
```

**Fix**: crea una habilidad antes de intentar eliminar una.

### Eliminación fallida

```
❌ Failed to delete skill: Permission denied

The skill directory may be in use or have permission issues.
Try manually: rm -rf .claude/skills/<skill>/
```

**Fix**: Verifique los permisos del archivo:
```bash
ls -la .claude/skills/<skill>/
chmod -R u+w .claude/skills/<skill>/
rm -rf .claude/skills/<skill>/
```

### Error al crear la copia de seguridad

```
⚠️  Could not create backup.

Check permissions for:
  .claude/skills-backup/

Manual backup:
  cp -r .claude/skills/<skill> .claude/skills-backup/
```

**Fix**: Asegúrese de que se pueda escribir en el directorio de respaldo:
```bash
mkdir -p .claude/skills-backup/
chmod u+w .claude/skills-backup/
```

## Mejores prácticas

### Flujo de trabajo de eliminación

1. **Use `/skill:list`** para ver todas las habilidades
2. **Test before deleting** si no está seguro:
   ```bash
   /agileflow:skill:test <skill-name>
   ```
3. **Use "Export first"** opción de copia de seguridad
4. **Confirm deletion** explícitamente
5. **Note in git** por qué se eliminó la habilidad

### Estrategia de respaldo

1. **Always export** para habilidades creadas hace más de un mes
2. **Keep backups** por 30 días mínimo
3. **Document** qué habilidades están respaldadas
4. **Test restores** periódicamente

### Flujo de trabajo del equipo

Si comparte habilidades en git:

1. **Commit deletion** con razón:
   ```bash
   git commit -m "Remove: deprecated-api-client skill - \
   consolidated into api-integration skill"
   ```

2. **Notify team** de eliminación

3. **Keep backup** en la rama git (opcional):
   ```bash
   git checkout -b backup-deprecated-skills
   git checkout main
   ```

## Comandos relacionados

- [`/habilidad:lista`](/commands/skill-list) - Ver todas las habilidades antes de eliminar
- [`/habilidad:crear`](/commands/skill-create) - Crear habilidad de reemplazo
- [`/ habilidad: editar`](/commands/skill-edit) - Cambiar nombre en lugar de eliminar
- [`/habilidad:prueba`](/commands/skill-test) - Verificar antes de eliminar
- [`/habilidad:actualizar`](/commands/skill-upgrade) - Mejorar las habilidades existentes.

## Próximos pasos

1. **List skills**: `/agileflow:skill:list`
2. **Identify obsolete**: ¿Cuáles no se utilizan?
3. **Backup first**: Utilice la opción "Exportar primero"
4. **Delete carefully**: Confirmar cada eliminación
5. **Clean backups**: Elimina las copias de seguridad antiguas periódicamente
