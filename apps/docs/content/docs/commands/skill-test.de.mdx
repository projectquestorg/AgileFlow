---
title: /skill:test
description: Stellen Sie sicher, dass ein Skill ordnungsgemäß funktioniert, indem Sie seine Aktivierung und Funktionalität testen
---

# /skill:test

Testen Sie eine Fertigkeit, um sicherzustellen, dass sie korrekt aktiviert wird, die richtige Struktur aufweist und die erwarteten Ergebnisse liefert.

## Schnellstart

```bash
# Interactive mode
/agileflow:skill:test

# Test specific skill
/agileflow:skill:test supabase-swift
```

## Was es tut

Der Testbefehl validiert Fähigkeiten in drei Phasen:

1. **Structure Validation** - Dateien sind vorhanden und ordnungsgemäß formatiert
2. **Content Validation** - Erforderliche Abschnitte vorhanden, keine defekten Links
3. **Activation Test** (optional) – Überprüfen Sie die Skill-Trigger an Beispiel-Eingabeaufforderungen

## Arbeitsablauf

### Phase 1: Fertigkeit auswählen

Wenn Sie keine Fertigkeit angeben:

```bash
/agileflow:skill:test
```

**Choose from available skills:**
```
Which skill would you like to test?

[1] supabase-swift
    Supabase database operations for Swift iOS apps

[2] ui-components
    React component patterns with accessibility

[3] api-error-handling
    REST API error handling patterns

Select: 1
```

### Phase 2: Strukturvalidierung

Der Test führt automatisierte Prüfungen durch:

```
🧪 Testing: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE VALIDATION
  ✅ SKILL.md exists
  ✅ Frontmatter valid (name: supabase-swift, description: present)
  ✅ references.md exists
  ✅ cookbook/ directory found (2 entries)
  ⚠️  No .mcp.json (MCP not configured)

CONTENT VALIDATION
  ✅ "When to Use" section present
  ✅ Description under 1024 characters (467 chars)
  ✅ SKILL.md under 500 lines (287 lines)
  ✅ All cookbook files referenced in SKILL.md exist
  ✅ No broken file references

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Phase 3: Optionaler Aktivierungstest

Nach der Strukturvalidierung:

```
Would you like to test skill activation with a sample prompt?

[1] Yes, test activation (Recommended)
    I'll simulate using the skill

[2] Skip activation test
    Structure validation is enough

[3] View SKILL.md content
    Read the full skill instructions

Select: 1
```

Wenn Sie den Aktivierungstest wählen:

```
Sample prompts that should activate this skill:

[1] "Help me set up Supabase authentication in my iOS app"
[2] "Create CRUD operations for my user database"
[3] "How do I query Supabase from Swift?"

Which would you like to test? (or enter custom prompt)
> 1
```

### Phase 4: Testergebnisse

```
🧪 Test Results: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE VALIDATION
  ✅ PASS (5/5 checks)

CONTENT VALIDATION
  ✅ PASS (5/5 checks)

ACTIVATION TEST
  ✅ PASS - Skill triggered on test prompt
  ✅ Response includes cookbook references
  ✅ All referenced files are accessible

MCP CONFIGURATION
  ⚠️  NOT CONFIGURED - Skills work without MCP

OVERALL: ✅ SKILL IS FUNCTIONAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Validierungsprüfungen

### Strukturprüfungen

| Check | What It Verifies |
|-------|------------------|
| **SKILL.md exists** | Main skill file is present |
| **Frontmatter valid** | Has `name:` and `description:` |
| **references.md** | Documentation links file (recommended) |
| **cookbook/ directory** | Workflows exist (if referenced) |
| **.mcp.json valid** | JSON syntax correct (if present) |

### Inhaltsprüfungen

| Check | What It Verifies |
|-------|------------------|
| **"When to Use" section** | Activation triggers defined |
| **Description length** | Under 1024 characters |
| **SKILL.md size** | Under 500 lines |
| **Cookbook references** | All files in SKILL.md exist |
| **No broken links** | Local file paths are valid |

### Indikatoren

- **✅ PASS** - Scheck bestanden
- **⚠️ WARNING** - Optionale Datei fehlt (nicht erforderlich)
- **❌ FAIL** - Erforderliche Prüfung fehlgeschlagen

## Beispiele

### Beispiel 1: Bestehensfähigkeit testen

```bash
/agileflow:skill:test react-components
```

**Output:**
```
🧪 Test Results: react-components
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE:     ✅ PASS (all 5 checks)
CONTENT:       ✅ PASS (all 5 checks)
ACTIVATION:    ✅ PASS (triggered correctly)

Overall: ✅ FULLY FUNCTIONAL
```

### Beispiel 2: Fähigkeit mit Warnungen

```bash
/agileflow:skill:test api-basic
```

**Output:**
```
🧪 Test Results: api-basic
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE:     ⚠️  WARNINGS (3/5 checks)
  ✅ SKILL.md exists
  ✅ Frontmatter valid
  ⚠️  No references.md (recommended but not required)
  ⚠️  No cookbook/ directory
  ✅ No .mcp.json (simple skill)

CONTENT:       ✅ PASS (all 5 checks)
ACTIVATION:    ✅ PASS (triggered correctly)

Overall: ⚠️  FUNCTIONAL WITH RECOMMENDATIONS

Recommendations:
  - Add references.md to link external documentation
  - Consider adding cookbook/ entries for multiple use cases
```

### Beispiel 3: Geschick mit Fehlern

```bash
/agileflow:skill:test broken-skill
```

**Output:**
```
🧪 Test Results: broken-skill
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE:     ❌ FAIL (1/5 checks)
  ❌ SKILL.md missing or corrupt
  ❌ Cannot parse frontmatter
  ⚠️  references.md exists
  ⚠️  No cookbook/
  ⚠️  No .mcp.json

CONTENT:       ⚠️  SKIPPED (structure issues)
ACTIVATION:    ❌ FAIL (cannot test without valid SKILL.md)

Overall: ❌ SKILL NEEDS FIXING

Issues Found:
  1. SKILL.md frontmatter is invalid
  2. Missing "When to Use" section
  3. Description exceeds 1024 characters

Fix With: /agileflow:skill:edit broken-skill
```

## Nächste Aktionen nach dem Test

### Wenn der Test erfolgreich ist

```
✅ Skill is working perfectly!

What would you like to do?

[1] Test another skill
[2] Use this skill now
[3] Upgrade with learning capability
    /agileflow:skill:upgrade <skill-name>
[4] Done
```

### Wenn der Test Warnungen enthält

```
⚠️  Skill works but could be improved.

What would you like to do?

[1] Edit skill to fix issues
    /agileflow:skill:edit <skill-name>
[2] Ignore warnings and continue
[3] View detailed report
[4] Done
```

### Wenn der Test fehlschlägt

```
❌ Skill has issues that need fixing.

What would you like to do?

[1] Edit skill to fix problems
    /agileflow:skill:edit <skill-name>
[2] Delete and recreate skill
    /agileflow:skill:delete <skill-name>
[3] View detailed report
[4] Done
```

## Ergebnisse interpretieren

### Strukturprobleme

**Problem**: „SKILL.md fehlt“
- **Cause**: Das Skill-Verzeichnis enthält SKILL.md nicht
- **Fix**: Überprüfen Sie, ob das Verzeichnis vorhanden ist: `ls .claude/skills/<name>/SKILL.md`
- **Solution**: Verwenden `/skill:edit` neu erschaffen

**Problem**: „Frontmatter ungültig“
- **Cause**: SKILL.md YAML ist fehlerhaft
- **Fix**: Überprüfen Sie, ob die ersten fünf Zeilen gültiges YAML haben:
  ```yaml
  ---
  name: skill-name
  description: Purpose
  ---
  ```
- **Solution**: Verwenden `/skill:edit` Frontmaterie reparieren

**Problem**: „Kein Abschnitt ‚Wann zu verwenden‘“
- **Cause**: Aktivierungsauslöser nicht definiert
- **Fix**: Abschnitt zu SKILL.md hinzufügen:
  ```markdown
  ## When to Use

  This skill activates when:
  - Condition 1
  - Keywords: "keyword1", "keyword2"
  ```
- **Solution**: Verwenden `/skill:edit` Abschnitt hinzufügen

### Inhaltliche Probleme

**Problem**: „Beschreibung überschreitet 1024 Zeichen“
- **Cause**: Die Beschreibung der Fertigkeit ist zu lang
- **Fix**: Kürzen Sie die Beschreibung in SKILL.md frontmatter
- **Solution**: Verwenden `/skill:edit` um die Länge zu reduzieren

**Problem**: „SKILL.md überschreitet 500 Zeilen“
- **Cause**: Der Skill ist zu groß/komplex
- **Fix**: Inhalt in Kochbuch/Dateien verschieben
- **Solution**: Verwenden `/skill:edit` neu organisieren

**Problem**: „Defekter Link: Cookbook/missing.md referenziert, aber nicht gefunden“
- **Cause**: Kochbuch-Dateireferenz ohne tatsächliche Datei
- **Fix**: Entweder die Datei erstellen oder den Verweis entfernen
- **Solution**: Verwenden `/skill:edit` reparieren

### MCP-Probleme

**Problem**: „Ungültiger JSON in .mcp.json“
- **Cause**: Syntaxfehler in der MCP-Konfiguration
- **Fix**: Überprüfen Sie, ob die JSON-Syntax gültig ist
- **Solution**: Verwenden `/skill:edit` um JSON zu reparieren

**Problem**: „Kein mcpServers-Objekt“
- **Cause**: .mcp.json fehlt die erforderliche Struktur
- **Fix**: Richtige Struktur hinzufügen:
  ```json
  {
    "mcpServers": {
      "name": { ... }
    }
  }
  ```
- **Solution**: Verwenden `/skill:edit` zu aktualisieren

## Test-Workflow

### Schnelltest (nur Struktur)

```bash
/agileflow:skill:test supabase-swift
# When asked: select "Skip activation test"
```

Die Überprüfung der Dateistruktur und des Inhalts dauert Sekunden.

### Vollständiger Test (Struktur + Aktivierung)

```bash
/agileflow:skill:test supabase-swift
# When asked: select "Yes, test activation"
# When asked: pick or enter test prompt
```

Testet, ob die Fähigkeit bei relevanten Eingabeaufforderungen tatsächlich aktiviert wird.

### Detaillierter Test (Struktur + Aktivierung + Inhalt anzeigen)

```bash
/agileflow:skill:test supabase-swift
# When asked: select "View SKILL.md content"
# Then manually review the skill
```

Hilft zu verstehen, was die Fertigkeit bewirkt, bevor man sie verwendet.

## Best Practices

### Test nach der Erstellung

Testen Sie einen Skill immer direkt nach dem Erstellen:

```bash
/agileflow:skill:create my-skill
/agileflow:skill:test my-skill  # ← Right after creation
```

### Testen Sie nach der Bearbeitung

Test nach Änderungen:

```bash
/agileflow:skill:edit my-skill
/agileflow:skill:test my-skill  # ← Verify changes worked
```

### Regelmäßige Audits

Testen Sie regelmäßig alle Fähigkeiten:

```bash
/agileflow:skill:list
# For each skill:
/agileflow:skill:test <skill-name>
```

### Testen Sie neue Trigger

Wenn Sie neue Aktivierungsauslöser hinzufügen:

```bash
/agileflow:skill:edit my-skill  # Add trigger keywords
/agileflow:skill:test my-skill  # Test with those keywords
```

## Fehlerbehebung

### Test hängt

Wenn der Test zu hängen scheint:

1. **Wait up to 30 seconds** - Der Aktivierungstest kann einige Zeit dauern
2. **Check internet connection** - Beim Testen von Webquellen
3. **Cancel (Ctrl+C)** und versuchen Sie es erneut
4. **Skip activation test** - Versuchen Sie es mit einem reinen Strukturtest

### Inkonsistente Ergebnisse

Wenn die Testergebnisse variieren:

1. **Network issues** - Einige Quellen sind möglicherweise vorübergehend nicht verfügbar
2. **Race conditions** - Versuchen Sie es erneut
3. **Check file system** - Stellen Sie sicher, dass die Dateien lesbar sind:
   ```bash
   ls -la .claude/skills/<skill>/
   ```

### Aktivierungstest schlägt fehl

Wenn „Skill korrekt ausgelöst“ als fehlgeschlagen angezeigt wird:

1. Update-Aktivierungsauslöser:
   ```bash
   /agileflow:skill:edit <skill-name>
   # Edit "When to Use" section
   ```

2. Testen Sie mit verschiedenen Eingabeaufforderungen

3. Überprüfen Sie, ob die Schlüsselwörter spezifisch genug sind

## Verwandte Befehle

- [`/skill:create`](/commands/skill-create) - Generieren Sie eine neue Fähigkeit
- [`/skill:list`](/commands/skill-list) - Alle Fähigkeiten anzeigen
- [`/skill:edit`](/commands/skill-edit) - Beheben Sie Fähigkeitsprobleme
- [`/skill:delete`](/commands/skill-delete) - Eine Fertigkeit entfernen
- [`/skill:upgrade`](/commands/skill-upgrade) - Lernfähigkeit hinzufügen

## Nächste Schritte

1. **Test a skill**: `/agileflow:skill:test <skill-name>`
2. **If passes**: Nutzen Sie die Fertigkeit in Ihrer Arbeit
3. **If fails**: Bearbeiten mit `/agileflow:skill:edit`
4. **After fixing**: Zur Verifizierung erneut testen
