---
title: / تشخيص
description: تشخيص صحة النظام
---

# / تشخيص

قم بإجراء فحوصات شاملة لسلامة نظام AgileFlow لتحديد المشكلات المحتملة قبل أن تتسبب في حدوث أعطال.

## بداية سريعة

```bash
/agileflow:diagnose
```

يؤدي هذا إلى تنفيذ مجموعة تشخيصية كاملة دون الحاجة إلى وسائط.

## حدود

لا يأخذ هذا الأمر أي معلمات - فهو يقوم تلقائيًا بتشغيل كافة الاختبارات التشخيصية.

## ما يتحقق

يتحقق نظام التشخيص من صحة تثبيت AgileFlow بالكامل:

### 1. التحقق من صحة ملف JSON
- التحقق من صحة جميع ملفات JSON الهامة (status.json، البيانات الوصفية، الإعدادات)
- التحقق من تنسيق الملف وبنيته
- تقارير أحجام الملفات بالكيلوبايت
- يحذر إذا تجاوز Status.json 100 كيلو بايت (يوصي بالأرشفة)
- يظهر عدد القصص في الملفات النشطة والمؤرشفة

### 2. سلامة هيكل الملف
- التحقق من وجود الأدلة المطلوبة (docs/، .agileflow/، .claude/)
- التحقق من الملفات الهامة (metadata.json،status.json)
- يميز بين الملفات الهامة والاختيارية

### 3. نظام الأرشفة التلقائية
- يتحقق من وجود البرنامج النصي للأرشيف وقابل للتنفيذ
- التحقق من تكوين الخطاف في .claude/settings.json
- تقارير عتبة الأرشفة من البيانات الوصفية
- يحدد المكونات المفقودة أو التي تم تكوينها بشكل خاطئ

### 4. نظام الخطافات
- التحقق من صحة بنية .claude/settings.json
- تحسب الخطافات SessionStart وUserPromptSubmit وStop
- تقارير مشاكل التكوين
- يتحقق من قابلية تنفيذ الخطاف

### 5. تحليل حجم الملف
- يراقب حجم Status.json مع الحدود
- يظهر عدد القصص النشطة والمؤرشفة
- يوصي بالأرشفة عند الحاجة
- يقدم توصيات على أساس الحجم

## أمثلة

### التشخيصات الأساسية

```bash
/agileflow:diagnose
```

يقوم بإجراء فحص كامل لسلامة النظام ويعرض النتائج.

### مؤشرات الإخراج

يستخدم التشخيص مؤشرات الحالة هذه:
- **✅** (علامة اختيار): تم اجتياز فحص الصحة
- **❌** (تقاطع): فشل التحقق من الصحة
- **⚠️** (تحذير): تم العثور على مشكلة غير حرجة
- **ℹ️** (معلومات): حالة إعلامية

## الإخراج النموذجي

```text
🔍 AgileFlow System Diagnostics
================================

📋 JSON File Validation
----------------------
  ✅ docs/00-meta/agileflow-metadata.json (42KB)
  ✅ docs/09-agents/status.json (28KB, 12 stories)
  ℹ️  docs/09-agents/status-archive.json - not found (optional)
  ✅ .claude/settings.json (5KB)

📦 Auto-Archival System
----------------------
  ✅ Archive script exists and is executable
  ✅ Auto-archival hook configured
  ✅ Archival threshold: 7 days

🪝 Hooks System
---------------
  ✅ .claude/settings.json is valid JSON
  ℹ️  SessionStart hooks: 2
  ℹ️  UserPromptSubmit hooks: 1
  ℹ️  Stop hooks: 1

📏 File Size Analysis
---------------------
  status.json: 28KB (12 stories)
  ✅ status.json size is healthy

📊 Diagnostic Summary
=====================
✅ All checks passed! System is healthy.
```

## تم اكتشاف المشكلات الشائعة

| Issue | Indicator | Solution |
|-------|-----------|----------|
| Invalid JSON syntax | ❌ | Fix JSON using `jq . <file>` to identify errors |
| Large status.json (>100KB) | ⚠️ | Run archival to reduce file size |
| Missing critical files | ❌ | Run `npx agileflow setup` to recreate |
| Archive script not executable | ⚠️ | Run `chmod +x scripts/archive-completed-stories.sh` |
| Archival hook not configured | ❌ | Run `/agileflow:configure --enable=archival` |
| Invalid .claude/settings.json | ❌ | Run `/agileflow:configure --migrate` to fix format |

## متى يتم التشغيل

تشغيل التشخيص:
- **After setup**: تحقق من اكتمال تثبيت AgileFlow بنجاح
- **Before major operations**: التحقق من صحة النظام قبل إجراء تغييرات كبيرة
- **Periodically**: تشغيل شهري/ربع سنوي للحفاظ على صحة النظام
- **After configuration changes**: التحقق من أن التغييرات سارية المفعول بشكل صحيح
- **On startup**: إذا واجهت مشكلات، فابدأ هنا لاستكشاف الأخطاء وإصلاحها

## رموز الخروج

- **0**: تم اجتياز جميع الفحوصات، والنظام سليم
- **1**: تم العثور على مشكلة واحدة أو أكثر تحتاج إلى الاهتمام

يسمح هذا باستخدام الأمر في خطوط أنابيب CI/CD والبرامج النصية الآلية.

## الإجراءات الموصى بها

إذا تم العثور على مشكلات، فاتبع الخطوات التالية:

1. **Invalid JSON**: يستخدم `jq . <file>` لتحديد أخطاء بناء الجملة وإصلاحها يدويًا
2. **Large files**: تشغيل الأرشيف: `bash scripts/archive-completed-stories.sh`
3. **Missing files**: إعادة التثبيت: `npx agileflow setup`
4. **Format issues**: تشغيل الترحيل: `/agileflow:configure --migrate`
5. **Re-run diagnostics**: التحقق من حل المشكلات

## الأوامر ذات الصلة

- [`/تكوين`](/commands/configure) - تكوين ميزات AgileFlow
- [`/التحقق من صحة الخبرة`](/commands/validate-expertise) - التحقق من صحة ملفات الخبرة
- [`/ضغط`](/commands/compress) - ضغط Status.json وتقليل حجم الملف
