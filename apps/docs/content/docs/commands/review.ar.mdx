---
title: "/مراجعة"
description: مراجعة التعليمات البرمجية المدعومة بالذكاء الاصطناعي مع اقتراحات الجودة
---

# /مراجعة

قم بإجراء مراجعة شاملة للتعليمات البرمجية مدعومة بالذكاء الاصطناعي بشأن تغييرات طلبات السحب. قم بمراجعة التعليمات البرمجية لتحليل الجودة والأمان والأداء وأفضل الممارسات، وتقديم تعليقات قابلة للتنفيذ مع أمثلة محددة للتعليمات البرمجية.

## بداية سريعة

```bash
# Review current branch against main
/agileflow:review

# Review specific branch
/agileflow:review BRANCH=feature/auth

# Review with focus on security
/agileflow:review FOCUS=security
```

الأمر سوف:
1. الحصول على تغييرات التعليمات البرمجية (git diff)
2. التحليل عبر 6 فئات (الجودة، الأمان، الأداء، أفضل الممارسات، الاختبار، التوثيق)
3. إنشاء تقرير مفصل بالمشكلات ذات الأولوية حسب الخطورة
4. حساب نقاط جودة الكود
5. اقتراح الإصلاحات التلقائية ومتابعة العمل

## الميزات الرئيسية

### تحليل شامل

تقوم المراجعة بفحص تغييرات التعليمات البرمجية الخاصة بك عبر 6 فئات مهمة:

| Category | Checks |
|----------|--------|
| **Code Quality** | Complexity, duplication, naming, function/file length |
| **Security** | SQL injection, XSS, hardcoded secrets, unsafe deserialization |
| **Performance** | N+1 queries, inefficient algorithms, memory leaks |
| **Best Practices** | Error handling, logging, type safety, async/await |
| **Testing** | Missing tests, test quality, coverage gaps |
| **Documentation** | Missing JSDoc, outdated comments, README updates |

### تحديد الأولويات على أساس الخطورة

يتم تصنيف القضايا حسب التأثير:

```text
🔴 CRITICAL - Must fix before merge
🟠 HIGH    - Fix before merge (blocks release)
🟡 MEDIUM  - Address soon (technical debt)
⚪ LOW     - Nice to have (style/consistency)
```

### نقاط جودة الكود

يحصل علاقاتك العامة على درجة (0-100) مقسمة حسب الفئة:

```text
Code Quality Score: 72/100

Breakdown:
- Security: 40/100 (critical issues found)
- Performance: 75/100 (one N+1 query)
- Maintainability: 80/100 (some complexity)
- Testing: 65/100 (coverage gaps)
- Style: 90/100 (mostly consistent)
```

### ردود فعل بناءة

كل قضية تشمل:
- أين هو (الملف، رقم السطر)
- لماذا يهم (المخاطر / التأثير)
- كيفية إصلاحها (أمثلة التعليمات البرمجية المحددة)
- مثال على ❌ كود سيء
- مثال على ✅ كود جيد

## الاستخدام

### الأساسية - مراجعة الفرع الحالي

```bash
/agileflow:review
```

يراجع التغييرات في الفرع الحالي مقابل الرئيسي/الرئيسي.

### مراجعة فرع معين

```bash
/agileflow:review BRANCH=feature/user-auth
```

يراجع الفرع المحدد مقابل الرئيسي.

### مراجعة ضد قاعدة مختلفة

```bash
/agileflow:review BRANCH=feature/auth BASE=develop
```

يقارن مع فرع قاعدة مختلفة.

### مراجعة مع منطقة التركيز

```bash
# Security-only review
/agileflow:review FOCUS=security

# Performance-only review
/agileflow:review FOCUS=performance

# Testing coverage review
/agileflow:review FOCUS=tests
```

### التصفية حسب الخطورة

```bash
# Show only critical/high issues
/agileflow:review SEVERITY=high

# Show all issues
/agileflow:review SEVERITY=all
```

## حدود

| Parameter | Required | Default | Options |
|-----------|----------|---------|---------|
| `BRANCH` | No | Current | Branch name to review |
| `BASE` | No | main/master | Base branch for comparison |
| `FOCUS` | No | all | all, security, performance, style, tests |
| `SEVERITY` | No | all | critical, high, medium, low, all |

## أمثلة

### مراجعة العلاقات العامة النموذجية

```bash
/agileflow:review

# Output:
# AI Code Review Report
# Branch: feature/user-auth
# Files Changed: 5
# Critical: 1 | High: 3 | Medium: 5 | Low: 2
# Code Quality Score: 72/100
#
# 🔴 CRITICAL ISSUES
# - Hardcoded API key in stripe.ts:15
#
# 🟠 HIGH ISSUES
# - N+1 query in posts API
# - Missing error handling in login
# ...
```

### مراجعة تركز على الأمن

```bash
/agileflow:review FOCUS=security

# Analyzes only for:
# - Hardcoded secrets
# - SQL injection risks
# - XSS vulnerabilities
# - Authentication issues
# - CORS misconfiguration
```

### مراجعة الأداء

```bash
/agileflow:review FOCUS=performance

# Checks for:
# - N+1 database queries
# - Inefficient algorithms
# - Missing indexes
# - Memory leaks
# - Bundle size issues
```

## مراجعة هيكل التقرير

### رأس

```text
# AI Code Review Report

**Branch**: feature/user-auth
**Base**: main
**Files Changed**: 8
**Lines Added**: 245
**Lines Removed**: 32
**Generated**: 2025-10-16T10:30:00Z
```

### ملخص

```text
## Summary

**Critical**: 2 | **High**: 5 | **Medium**: 12 | **Low**: 8
**Must Fix Before Merge**: 7 issues
```

### القضايا حسب الخطورة

لكل قضية:

```text
### 1. Hardcoded API Key (SECURITY)
**File**: src/api/payments/stripe.ts:15
**Severity**: CRITICAL

// ❌ BAD
const stripeKey = "sk_live_abc123...";

// ✅ GOOD
const stripeKey = process.env.STRIPE_SECRET_KEY;
if (!stripeKey) throw new Error("STRIPE_SECRET_KEY not set");

**Risk**: API key exposed in version control.
**Fix**: Move to environment variable. Rotate immediately.
**Priority**: Block merge
```

### ملاحظات إيجابية

```text
## Positive Observations ✅

- ✅ Good use of TypeScript strict mode
- ✅ Consistent error handling pattern
- ✅ Well-structured component separation
- ✅ Clear commit messages
```

### تحليل جودة الكود

```text
## Code Quality Score: 72/100

**Breakdown**:
- Security: 40/100 (2 critical issues)
- Performance: 75/100 (1 N+1 query)
- Maintainability: 80/100 (some complexity)
- Testing: 65/100 (coverage gaps)
- Style: 90/100 (mostly consistent)
```

## التخصيص

### القواعد الخاصة بالمشروع

إنشاء قواعد مراجعة مخصصة في:
- `.agileflow/review-rules.md`
- `docs/02-practices/code-standards.md`

مثال:

```markdown
# Code Review Rules

## Critical
- No console.log in production code
- All API endpoints must have rate limiting
- All database queries must use ORM

## High
- Functions >30 lines should be refactored
- Test coverage must be >85%
```

## الإجراءات بعد المراجعة

### مشاكل الإصلاح التلقائي

```text
Review asks: "Fix auto-fixable issues? (YES/NO)"

If YES:
  - Runs npm run lint -- --fix
  - Fixes 8 style issues automatically
  - Shows which issues were fixed
```

### إنشاء قصص المتابعة

```text
Review asks: "Create stories for follow-up work?"

If YES:
  - Creates story for each medium/low issue
  - Adds to your backlog
  - Links to code review report
```

### منع الدمج للقضايا الحرجة

```text
Review asks: "Block merge for critical/high issues?"

If YES:
  - Marks merge as blocked
  - Provides failure reason
  - Suggests resolution steps
```

### حفظ التقرير

تم حفظ التقرير تلقائيًا في:
```text
docs/08-project/code-reviews/<YYYYMMDD>-<BRANCH>.md
```

مثال:
```text
docs/08-project/code-reviews/20251016-feature-user-auth.md
```

## الشيكات الأمنية

تقوم المراجعة تلقائيًا بالتحقق من:

### أسرار وأوراق اعتماد
- مفاتيح API المشفرة
- كلمات مرور قاعدة البيانات
- رموز OAuth
- أسرار جي دبليو تي
- بيانات اعتماد AWS

### نقاط الضعف المشتركة
- حقن SQL (استعلامات غير محددة)
- هجمات XSS (إخراج غير مهرب)
- إلغاء التسلسل غير الآمن
- نقاط الضعف CSRF
- التحقق من صحة CORS مفقود

### أنماط غير آمنة
- إيفال () أو وظيفة ()
- تسلسل سلسلة غير آمنة
- التحقق من صحة الإدخال مفقود
- تشفير ضعيف
- التبعيات غير الآمنة

## فحوصات الأداء

المراجعة تبحث عن:

### قاعدة البيانات
- مشاكل الاستعلام N+1
- الفهارس المفقودة
- الانضمامات غير الفعالة
- استفسارات غير ضرورية

### الخوارزميات
- O(n²) عندما يكون O(n) ممكنًا
- حلقات متداخلة يمكن أن تكون مسطحة
- الحسابات المتكررة
- العودية غير الأمثل

### ذاكرة
- تسرب الذاكرة
- لم يتم تحرير الكائنات الكبيرة
- المراجع الدائرية
- هياكل البيانات التي تستهلك الذاكرة

## تغطية الاختبار

مراجعة الشيكات:

### كود جديد
- هل هناك اختبار للوظائف الجديدة؟
- هل يتم تغطية حالات الحافة؟
- هل جودة الاختبار جيدة؟

### فجوات التغطية
- تمت إضافة الأسطر بدون اختبارات
- وظائف مع عدم وجود تغطية الاختبار
- لم يتم اختبار مسارات الخطأ

### جودة الاختبار
- هل التأكيدات ذات معنى؟
- هل الاختبارات تتحقق من السلوك؟
- هل الاختبارات قابلة للصيانة؟

## المخرجات والتوصيات

### الإجراءات الفورية (دمج الكتل)

```text
1. Remove hardcoded API key (critical)
2. Fix SQL injection (critical)
3. Add error handling to login (high)
```

### قبل الدمج

```text
4. Add tests for auth endpoints (high)
5. Fix N+1 query in posts API (high)
6. Reduce complexity in validator.ts (medium)
```

### المتابعة (إنشاء قصص)

```text
7. Standardize naming conventions (medium)
8. Add ESLint rule for secrets (medium)
9. Set up automated security scanning (low)
```

## التكامل مع CI

أقترح إضافة إلى `.github/workflows/pr.yml`:

```yaml
- name: AI Code Review
  run: npx claude-code /agileflow:review BRANCH=${{ github.head_ref }}

- name: Check for critical issues
  run: |
    if grep -q "CRITICAL" code-review-report.md; then
      echo "::error::Critical issues found."
      exit 1
    fi
```

## نصائح

### قبل تقديم العلاقات العامة

```bash
/agileflow:review
# Fix any CRITICAL or HIGH issues
# Address MEDIUM issues if possible
# Then submit PR with confidence
```

### التحسين التكراري

```bash
# After feedback, run review again
/agileflow:review

# See if score improved
# Ensure critical issues fixed
# Check test coverage
```

### نهج الأمن أولا

```bash
# Always check security first
/agileflow:review FOCUS=security

# Fix any CRITICAL security issues
# Then review other categories
```

## مبادئ

- **Be constructive** - تقديم تعليقات قابلة للتنفيذ، وليس مجرد انتقاد
- **Show examples** - قم بتضمين رمز ❌ BAD و ✅ GOOD
- **Prioritize** - القضايا الحاسمة أولا، والأسلوب أخيرا
- **Celebrate** - تضمين الملاحظات الإيجابية
- **Suggest fixes** - لا تكتفي بتحديد المشاكل
- **No force commits** - اسأل دائمًا قبل الإصلاح التلقائي

## الأوامر ذات الصلة

- [`/العلاقات العامة`](/commands/pr) - إنشاء وصف للعلاقات العامة (المراجعة قبل الإرسال)
- [`/ تأثير`](/commands/impact) - تحليل تأثير التغيير
- [`/ جليسة الأطفال`](/commands/babysit) - إرشادات التنفيذ (تتم المراجعة قبل إجراء العلاقات العامة)
- [`/التحقق`](/commands/verify) - التحقق من اجتياز الاختبارات
- [`/خط الأساس`](/commands/baseline) - إنشاء نقطة تفتيش تم التحقق منها
