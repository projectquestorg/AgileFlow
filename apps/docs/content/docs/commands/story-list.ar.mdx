---
title: "/قصة:قائمة"
description: قم بإدراج جميع القصص مع خيارات الحالة والتصفية
---

# /قصة:قائمة

اعرض جميع قصص المستخدمين مع خيارات التصفية وميزات الإجراء السريع.

## بداية سريعة

```bash
/agileflow:story:list
```

## حدود

| Parameter | Required | Default | Description |
|-----------|----------|---------|-------------|
| `EPIC` | No | - | Filter by epic (e.g., EP-0001) |
| `STATUS` | No | - | Filter by status: `ready`, `in_progress`, `blocked`, `done` |
| `OWNER` | No | - | Filter by owner (e.g., AG-UI) |

## أمثلة

### قائمة بجميع القصص

```bash
/agileflow:story:list
```

تظهر جميع القصص منظمة حسب الملحمة مع حالتها.

### قائمة قصص لملحمة محددة

```bash
/agileflow:story:list EPIC=EP-0001
```

يظهر فقط القصص في ملحمة المصادقة.

### قائمة القصص الجاهزة

```bash
/agileflow:story:list STATUS=ready
```

تظهر جميع القصص الجاهزة لبدء العمل عليها.

### قائمة القصص قيد التقدم

```bash
/agileflow:story:list STATUS=in_progress
```

يظهر عناصر العمل الحالية.

### قائمة القصص حسب المالك

```bash
/agileflow:story:list OWNER=AG-UI
```

يعرض جميع القصص المخصصة لوكيل واجهة المستخدم.

### مرشحات مجتمعة

```bash
/agileflow:story:list EPIC=EP-0001 STATUS=ready
```

يظهر القصص الجاهزة فقط في ملحمة التوثيق.

```bash
/agileflow:story:list STATUS=blocked OWNER=AG-API
```

يعرض قصص API المحظورة حاليًا.

## تنسيق الإخراج

يتم تنظيم القصص حسب الملحمة مع أعمدة الحالة:

```markdown
## Stories

### EP-0001: Authentication System
| Story | Title | Status | Owner | Estimate |
|-------|-------|--------|-------|----------|
| US-0001 | Login form | done | AG-UI | 2h |
| US-0002 | Password reset | in_progress | AG-API | 3h |
| US-0003 | Session management | ready | AG-API | 4h |

### EP-0002: User Dashboard
| Story | Title | Status | Owner | Estimate |
|-------|-------|--------|-------|----------|
| US-0004 | Dashboard layout | ready | AG-UI | 2h |
| US-0005 | Activity feed | ready | AG-UI | 3h |

---
**Summary**: 5 stories (1 done, 1 in_progress, 3 ready)
```

## تعريفات الحالة

| Status | Meaning | Action |
|--------|---------|--------|
| **ready** | Ready to start, no blockers | Start work / view details |
| **in_progress** | Currently being worked on | Continue work / mark blocked if needed |
| **blocked** | Waiting for dependency or clarification | Resolve blocker / unblock when ready |
| **done** | Completed and tested | Move to next story |

## إجراءات سريعة

بعد عرض القائمة، يمكنك:

### عرض تفاصيل القصة

اختر قصة لتراها:
- معايير القبول الكاملة
- حالة الاختبار
- التبعيات
- حاصرات (إن وجدت)
- ملاحظات التقدم

```bash
/agileflow:story:view STORY=US-0003
```

### ابدأ العمل على القصة

اختر قصة جاهزة وقم بوضع علامة عليها كـ in_progress:

```bash
/agileflow:status US-0003 STATUS=in_progress
```

### إنشاء قصة جديدة

أضف قصة إلى ملحمة:

```bash
/agileflow:story EPIC=EP-0001 ...
```

## تكامل سير العمل

### الوقوف اليومي

```bash
/agileflow:story:list STATUS=in_progress
# See what's being worked on
/agileflow:story:list STATUS=blocked
# See what's blocked
```

### تخطيط سبرينت

```bash
/agileflow:story:list STATUS=ready
# See available work
/agileflow:epic:list
# See epic status
# Select stories for sprint
```

### رفع الحظر عن سير العمل

```bash
/agileflow:story:list STATUS=blocked
# See all blocked stories
# Resolve dependencies
/agileflow:story:view STORY=<blocked>
# See blocker details
/agileflow:status <story> STATUS=ready
# When unblocked, mark ready again
```

### تتبع الإنجاز

```bash
/agileflow:story:list STATUS=done
# See what was completed
/agileflow:story:list STATUS=in_progress
# See what's being worked on
/agileflow:story:list STATUS=ready
# See what's queued up
```

## استراتيجيات التصفية

### بواسطة ملحمة
```bash
/agileflow:story:list EPIC=EP-0001
# Focus on one feature
```

### حسب الحالة
```bash
/agileflow:story:list STATUS=ready
# Find available work
```

```bash
/agileflow:story:list STATUS=blocked
# Find blockers to resolve
```

### بواسطة المالك
```bash
/agileflow:story:list OWNER=AG-API
# See what an agent is working on
```

### حسب الحالة + ملحمة
```bash
/agileflow:story:list EPIC=EP-0001 STATUS=ready
# Ready stories in specific epic
```

### حسب الحالة + المالك
```bash
/agileflow:story:list STATUS=in_progress OWNER=AG-UI
# Current work for specific agent
```

## تفسير النتائج

### توزيع قصة صحية

```
5 stories (1 done, 2 in_progress, 2 ready)
```

- ✅ الناس لديهم عمل للقيام به
- ✅ العمل يتدفق
- ✅البلوك جاهز

### القضايا المحتملة

**Too many in_progress (>3)**
```
5 stories (0 done, 4 in_progress, 1 ready)
```
- فكر في تقليل العمل قيد التنفيذ (العمل قيد التقدم)
- التركيز على إكمال القصص

**No ready stories**
```
5 stories (2 done, 2 in_progress, 0 ready)
```
- بحاجة إلى التخطيط لمزيد من القصص
- يخلق `/agileflow:story` لعناصر العمل القادمة

**Lots of blocked**
```
7 stories (1 done, 1 in_progress, 3 blocked, 2 ready)
```
- معالجة حاصرات على وجه السرعة
- قد تحتاج إلى إلغاء حظر التبعيات

## الأوامر ذات الصلة

- [`/قصة:عرض`](/commands/story-view) - عرض تفاصيل القصة
- [`/قصة`](/commands/story) - إنشاء قصة جديدة
- [`/التحقق من صحة القصة`](/commands/story-validate) - التحقق من اكتمال القصة
- [`/الحالة`](/commands/status) - تحديث حالة القصة
- [`/ملحمة:قائمة`](/commands/epic-list) - عرض الملاحم
- [`/اللوحة`](/commands/board) - لوحة كانبان المرئية
- [`/الحاصرات`](/commands/blockers) - البحث عن الحواجز وحلها
