---
title: / التمهيدي-مزامنة
description: قم بمزامنة ملف README.md الخاص بالمجلد مع محتوياته الحالية
---

# / التمهيدي-مزامنة

حافظ على تحديث ملفات المجلد README.md عن طريق إنشاء قسم محتويات يطابق الملفات الفعلية الموجودة في المجلد تلقائيًا.

## بداية سريعة

```bash
/agileflow:readme-sync FOLDER=docs/02-practices
```

لمزامنة جميع مجلدات الوثائق مرة واحدة:

```bash
/agileflow:readme-sync FOLDER=all
```

## حدود

| Parameter | Required | Default | Description |
|-----------|----------|---------|-------------|
| `FOLDER` | No | - | Path to folder (e.g., `docs/02-practices`), or `all` to sync entire docs/ |

## أمثلة

### مزامنة مجلد واحد

```bash
/agileflow:readme-sync FOLDER=docs/02-practices
```

المسح `docs/02-practices/` لكافة الملفات والأدلة الفرعية، ثم يقوم بتحديث ملف `## Contents` قسم من `docs/02-practices/README.md` مع العناصر الحالية.

### مزامنة كافة مجلدات الوثائق

```bash
/agileflow:readme-sync FOLDER=all
```

يقوم تلقائيًا بمزامنة جميع المجلدات الموجودة فيه `docs/` الدليل بالتوازي:
- `docs/00-meta/`
- `docs/01-brainstorming/`
- `docs/02-practices/`
- `docs/03-decisions/`
- ... وأكثر

### اختيار المجلد التفاعلي

```bash
/agileflow:readme-sync
```

إذا لم تحدد مجلدًا، فسيطلب منك الأمر اختيار:

```text
Which folder should I sync?

1. docs/02-practices (Recommended)
2. docs/04-architecture
3. all
4. Other (enter custom path)
```

## الإخراج

ينشئ الأمر أ `## Contents` القسم في الملف التمهيدي:

```markdown
## Contents

- **testing.md** – Test strategy, patterns, and test infrastructure
- **git-branching.md** – Git workflow, branching strategy, and commit conventions
- **ci.md** – CI/CD pipeline configuration and testing gates
- **security.md** – Security practices, input validation, and authentication
- **releasing.md** – Release procedures, versioning, and changelog management

### Sub-folder: design/
- **design/colors.md** – Color palette and branding guidelines
- **design/typography.md** – Font stack and text styling rules
```

## كيف يعمل

### الخطوة 1: قائمة المحتويات
يقوم الأمر بفحص المجلد بحثًا عن:
- جميع تخفيض السعر وغيرها من الملفات
- Subdirectories and their contents

### الخطوة 2: استخراج الأوصاف
لكل ملف، يستخرج:
- **Markdown files**: العنوان الأول (# Heading) أو الجملة الأولى
- **Other files**: السطر الأول أو الوصف القائم على اسم الملف

### الخطوة 3: بناء قسم المحتويات
يُنشئ تنسيق قائمة نقطية نظيفًا باستخدام:
- أسماء الملفات بالخط العريض
- أوصاف جملة واحدة
- بنية متداخلة للأدلة الفرعية

### الخطوة 4: المعاينة والتأكيد
يظهر لك التغييرات المقترحة:

```text
📁 Syncing docs/02-practices/README.md
=====================================

Proposed Changes to ## Contents Section:
─────────────────────────────────────────
- **testing.md** – Test strategy, patterns, test infrastructure
- **git-branching.md** – Git workflow, branching strategy, commit conventions
...

Update README.md with these changes? (YES/NO)
```

## أفضل الممارسات

### حافظ على تحديث المحتويات
تشغيل بعد إضافة ملفات جديدة إلى مجلد:

```bash
# Just added a new file
vim docs/02-practices/performance-tuning.md

# Update the README
/agileflow:readme-sync FOLDER=docs/02-practices
```

### قائمة مراجعة ما قبل الإصدار
قبل الإصدارات الرئيسية، قم بمزامنة جميع الوثائق:

```bash
/agileflow:readme-sync FOLDER=all
```

### صيانة ربع سنوية
جدولة عمليات تدقيق الوثائق العادية:

```bash
# Sync entire docs/ and review changes
/agileflow:readme-sync FOLDER=all
```

### عند إعادة التنظيم
بعد إعادة هيكلة المجلد، قم بالمزامنة على الفور:

```bash
/agileflow:readme-sync FOLDER=docs/your-folder
```

## ما يتم تحديثه

الأمر **only updates the `## Contents` section**:

- يزيل قوائم الملفات القديمة
- يضيف كافة الملفات الحالية مع الأوصاف
- يحافظ على جميع أقسام README الأخرى دون تغيير
- يحافظ على الملاحظات والروابط المخصصة
- يحافظ على أقسام الرأس والمقدمة والتذييل سليمة

وهذا يعني أنه يمكنك استخدام الأمر بأمان دون فقدان الوثائق المخصصة.

## استخراج وصف الملف

يستخرج الأمر الأوصاف بذكاء:

```markdown
# Testing Strategy

Guidelines for writing and running tests...
```

مقتطفات على النحو التالي: "استراتيجية الاختبار - إرشادات لكتابة الاختبارات وإجرائها"

```markdown
<!-- This file documents git workflow -->
Guidelines for branching and committing
```

يستخرج السطر الأول ذو المعنى كوصف.

## الأوامر ذات الصلة

- [`/ مزامنة المستندات`](/commands/docs-sync) - مزامنة الوثائق مع تغييرات التعليمات البرمجية
- [`/ السياق`](/commands/context) - إنشاء سياق المشروع الشامل
- [`/ القالب`](/commands/template) - إنشاء قوالب وثائق مخصصة
