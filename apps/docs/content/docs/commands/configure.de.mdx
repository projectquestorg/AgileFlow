---
title: /konfigurieren
description: Erweiterte AgileFlow-Funktionen konfigurieren (Git, Hooks, Archivierung, CI, Statuszeile)
---
# /configure

Konfigurieren Sie erweiterte AgileFlow-Funktionen mit Profilen, aktivieren/deaktivieren Sie Optionen und Formatmigration, um Ihr Workflow-Setup anzupassen.

## Schnellstart

```bash
/agileflow:configure
```

Dadurch wird ein interaktives Konfigurationsmenü geöffnet. Sie können auch mit bestimmten Optionen ausführen:

```bash
/agileflow:configure --profile=full
/agileflow:configure --enable=sessionstart,precompact
/agileflow:configure --disable=archival
```

## Parameter

| Parameter | Erforderlich | Standard | Beschreibung |
|-----------|----------|---------|-------------|
| `--profile` | Nein | - | Konfigurationsprofil: `full`, `basic`, `minimal` oder `none` |
| `--enable` | Nein | - | Durch Kommas getrennte Liste der zu aktivierenden Funktionen |
| `--disable` | Nein | - | Durch Kommas getrennte Liste der zu deaktivierenden Funktionen |
| `--migrate` | Nein | - | Formatprobleme in vorhandener Konfiguration beheben |
| `--detect` | Nein | - | Aktuellen Konfigurationsstatus prüfen |
| `--archival-days` | Nein | 7 | Tage vor der Archivierung abgeschlossener Geschichten |

## Konfigurationsprofile

| Profil | Sitzungsstart | Vorverdichten | Stopphaken | Bogenhival | Statuszeile |
|---------|-------------|------------|-----------|----------|------------|
| `full` | ✅ | ✅ | ✅ | ✅ 7 Tage | ✅ |
| `basic` | ✅ | ✅ | ❌ | ✅ 7 Tage | ❌ |
| `minimal` | ✅ | ❌ | ❌ | ✅ 7 Tage | ❌ |
| `none` | ❌ | ❌ | ❌ | ❌ | ❌ |

## Verfügbare Funktionen

- **sessionstart**: Begrüßungsanzeige mit Projektstatus beim Sitzungsstart
- **Vorkomprimieren**: Beim Komprimieren von Gesprächen bleibt der Kontext erhalten
- **Stop**: Warnt vor nicht festgeschriebenen Git-Änderungen, bevor die Sitzung beendet wird
- **Archivierung**: Alte abgeschlossene Storys automatisch archivieren (konfigurierbare Tage)
- **Statuszeile**: Benutzerdefinierte Statusleiste, die die aktuelle Story und den Fortschritt anzeigt

## Beispiele

### Grundeinrichtung (empfohlen)

```bash
/agileflow:configure --profile=full
```

Aktiviert alle Funktionen: Hooks, Archivierung und Statuszeile.

### Spezifische Funktionen aktivieren

```bash
/agileflow:configure --enable=sessionstart,precompact
```

Aktivieren Sie nur SessionStart- und PreCompact-Hooks, deaktivieren Sie andere.

### Funktion deaktivieren

```bash
/agileflow:configure --disable=statusline
```

Deaktivieren Sie die benutzerdefinierte Statuszeile, während Sie andere Funktionen beibehalten.

### Konfigurationsprobleme beheben

```bash
/agileflow:configure --migrate
```

Wenn Sie Formatwarnungen sehen, werden diese automatisch behoben und ein Backup unter `.claude/settings.json.backup` erstellt.

### Überprüfen Sie den aktuellen Status

```bash
/agileflow:configure --detect
```

Zeigt an, was derzeit aktiviert oder deaktiviert ist, ohne dass Änderungen vorgenommen werden müssen.

### Benutzerdefinierter Archivierungsschwellenwert

```bash
/agileflow:configure --enable=archival --archival-days=14
```

Archivieren Sie abgeschlossene Storys nach 14 Tagen statt nach den standardmäßigen 7 Tagen.

## Was wird konfiguriert?

### Hakensystem
Konfiguriert Hooks in `.claude/settings.json` für:
- SessionStart: Zeigt Projektinformationen an, wenn Sie eine Sitzung starten
- UserPromptSubmit: Führt eine Validierung vor dem Festschreiben durch
- Stop: Warnt vor nicht festgeschriebenen Änderungen beim Beenden der Sitzung

### Archivsystem
Aktualisiert `docs/00-meta/agileflow-metadata.json`, um die automatische Archivierung abgeschlossener Storys zu ermöglichen, die älter als der angegebene Schwellenwert sind.

### Statuszeile
Konfiguriert die benutzerdefinierte Claude-Code-Statusleiste, die Folgendes anzeigt:
- Aktueller Git-Zweig
- Aktive Story-ID
- WIP-Anzahl (Work-in-Progress).
- Abschlussprozentsatz

## Ausgabe

Nach der Konfiguration sehen Sie:

```text
✅ Configuration Complete!

Enabled:
  ✅ sessionstart
  ✅ precompact
  ✅ stop

Disabled:
  ❌ archival
  ❌ statusline

═══════════════════════════════════════════════════════
🔴 RESTART CLAUDE CODE NOW!
   Quit completely, wait 5 seconds, restart
═══════════════════════════════════════════════════════
```

Starten Sie Claude Code nach Konfigurationsänderungen immer neu, damit diese wirksam werden.

## Wichtige Hinweise

- **Formatmigration**: Wenn Sie `⚠️ INVALID FORMAT`-Warnungen sehen, führen Sie zuerst `--migrate` aus, bevor Sie andere Optionen verwenden
- **Backup erstellt**: Die Migrationsoption erstellt automatisch ein Backup bei `.claude/settings.json.backup`
- **Neustart erforderlich**: Sie müssen Claude Code vollständig beenden und neu starten, damit die Änderungen wirksam werden
- **Aktualisierte Metadaten**: Die Funktionskonfiguration wird in `docs/00-meta/agileflow-metadata.json` verfolgt.

## Verwandte Befehle

- [`/diagnose`](/commands/diagnose) – Überprüfen Sie den Systemzustand und die Konfiguration
- [`/validate-expertise`](/commands/validate-expertise) – Gutachtendateien validieren
- [`/help`](/commands/help) – Alle verfügbaren Befehle anzeigen