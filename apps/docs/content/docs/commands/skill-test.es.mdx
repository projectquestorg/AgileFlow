---
title: /habilidad:prueba
description: Verifique que una habilidad funcione correctamente probando su activación y funcionalidad
---

# /habilidad:prueba

Pruebe una habilidad para verificar que se active correctamente, tenga la estructura adecuada y produzca los resultados esperados.

## Inicio rápido

```bash
# Interactive mode
/agileflow:skill:test

# Test specific skill
/agileflow:skill:test supabase-swift
```

## Qué hace

El comando de prueba valida las habilidades a través de tres fases:

1. **Structure Validation** - Los archivos existen y están formateados correctamente.
2. **Content Validation** - Secciones obligatorias presentes, sin enlaces rotos
3. **Activation Test** (opcional): verificar los activadores de habilidades en mensajes de muestra

## Flujo de trabajo

### Fase 1: Seleccionar habilidad

Si no especificas una habilidad:

```bash
/agileflow:skill:test
```

**Choose from available skills:**
```
Which skill would you like to test?

[1] supabase-swift
    Supabase database operations for Swift iOS apps

[2] ui-components
    React component patterns with accessibility

[3] api-error-handling
    REST API error handling patterns

Select: 1
```

### Fase 2: Validación de la estructura

La prueba ejecuta comprobaciones automáticas:

```
🧪 Testing: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE VALIDATION
  ✅ SKILL.md exists
  ✅ Frontmatter valid (name: supabase-swift, description: present)
  ✅ references.md exists
  ✅ cookbook/ directory found (2 entries)
  ⚠️  No .mcp.json (MCP not configured)

CONTENT VALIDATION
  ✅ "When to Use" section present
  ✅ Description under 1024 characters (467 chars)
  ✅ SKILL.md under 500 lines (287 lines)
  ✅ All cookbook files referenced in SKILL.md exist
  ✅ No broken file references

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Fase 3: Prueba de activación opcional

Después de la validación de la estructura:

```
Would you like to test skill activation with a sample prompt?

[1] Yes, test activation (Recommended)
    I'll simulate using the skill

[2] Skip activation test
    Structure validation is enough

[3] View SKILL.md content
    Read the full skill instructions

Select: 1
```

Si elige la prueba de activación:

```
Sample prompts that should activate this skill:

[1] "Help me set up Supabase authentication in my iOS app"
[2] "Create CRUD operations for my user database"
[3] "How do I query Supabase from Swift?"

Which would you like to test? (or enter custom prompt)
> 1
```

### Fase 4: Resultados de la prueba

```
🧪 Test Results: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE VALIDATION
  ✅ PASS (5/5 checks)

CONTENT VALIDATION
  ✅ PASS (5/5 checks)

ACTIVATION TEST
  ✅ PASS - Skill triggered on test prompt
  ✅ Response includes cookbook references
  ✅ All referenced files are accessible

MCP CONFIGURATION
  ⚠️  NOT CONFIGURED - Skills work without MCP

OVERALL: ✅ SKILL IS FUNCTIONAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Comprobaciones de validación

### Comprobaciones de estructura

| Check | What It Verifies |
|-------|------------------|
| **SKILL.md exists** | Main skill file is present |
| **Frontmatter valid** | Has `name:` and `description:` |
| **references.md** | Documentation links file (recommended) |
| **cookbook/ directory** | Workflows exist (if referenced) |
| **.mcp.json valid** | JSON syntax correct (if present) |

### Comprobaciones de contenido

| Check | What It Verifies |
|-------|------------------|
| **"When to Use" section** | Activation triggers defined |
| **Description length** | Under 1024 characters |
| **SKILL.md size** | Under 500 lines |
| **Cookbook references** | All files in SKILL.md exist |
| **No broken links** | Local file paths are valid |

### Indicadores

- **✅ PASS** - Cheque aprobado
- **⚠️ WARNING** - Falta el archivo opcional (no requerido)
- **❌ FAIL** - La verificación requerida falló

## Ejemplos

### Ejemplo 1: Habilidad para aprobar pruebas

```bash
/agileflow:skill:test react-components
```

**Output:**
```
🧪 Test Results: react-components
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE:     ✅ PASS (all 5 checks)
CONTENT:       ✅ PASS (all 5 checks)
ACTIVATION:    ✅ PASS (triggered correctly)

Overall: ✅ FULLY FUNCTIONAL
```

### Ejemplo 2: Habilidad con Advertencias

```bash
/agileflow:skill:test api-basic
```

**Output:**
```
🧪 Test Results: api-basic
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE:     ⚠️  WARNINGS (3/5 checks)
  ✅ SKILL.md exists
  ✅ Frontmatter valid
  ⚠️  No references.md (recommended but not required)
  ⚠️  No cookbook/ directory
  ✅ No .mcp.json (simple skill)

CONTENT:       ✅ PASS (all 5 checks)
ACTIVATION:    ✅ PASS (triggered correctly)

Overall: ⚠️  FUNCTIONAL WITH RECOMMENDATIONS

Recommendations:
  - Add references.md to link external documentation
  - Consider adding cookbook/ entries for multiple use cases
```

### Ejemplo 3: Habilidad con fallas

```bash
/agileflow:skill:test broken-skill
```

**Output:**
```
🧪 Test Results: broken-skill
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STRUCTURE:     ❌ FAIL (1/5 checks)
  ❌ SKILL.md missing or corrupt
  ❌ Cannot parse frontmatter
  ⚠️  references.md exists
  ⚠️  No cookbook/
  ⚠️  No .mcp.json

CONTENT:       ⚠️  SKIPPED (structure issues)
ACTIVATION:    ❌ FAIL (cannot test without valid SKILL.md)

Overall: ❌ SKILL NEEDS FIXING

Issues Found:
  1. SKILL.md frontmatter is invalid
  2. Missing "When to Use" section
  3. Description exceeds 1024 characters

Fix With: /agileflow:skill:edit broken-skill
```

## Próximas acciones después de la prueba

### Si la prueba pasa

```
✅ Skill is working perfectly!

What would you like to do?

[1] Test another skill
[2] Use this skill now
[3] Upgrade with learning capability
    /agileflow:skill:upgrade <skill-name>
[4] Done
```

### Si la prueba tiene advertencias

```
⚠️  Skill works but could be improved.

What would you like to do?

[1] Edit skill to fix issues
    /agileflow:skill:edit <skill-name>
[2] Ignore warnings and continue
[3] View detailed report
[4] Done
```

### Si la prueba falla

```
❌ Skill has issues that need fixing.

What would you like to do?

[1] Edit skill to fix problems
    /agileflow:skill:edit <skill-name>
[2] Delete and recreate skill
    /agileflow:skill:delete <skill-name>
[3] View detailed report
[4] Done
```

## Interpretación de resultados

### Problemas de estructura

**Problem**: "Falta SKILL.md"
- **Cause**: El directorio de habilidades no contiene SKILL.md
- **Fix**: Verifique que el directorio exista: `ls .claude/skills/<name>/SKILL.md`
- **Solution**: Usar `/skill:edit` recrear

**Problem**: "Asunto frontal no válido"
- **Cause**: SKILL.md YAML tiene un formato incorrecto
- **Fix**: Verifique que las primeras 5 líneas tengan YAML válido:
  ```yaml
  ---
  name: skill-name
  description: Purpose
  ---
  ```
- **Solution**: Usar `/skill:edit` para arreglar la cuestión frontal

**Problem**: "No hay sección 'Cuándo utilizar'"
- **Cause**: Activadores de activación no definidos
- **Fix**: Agregar sección a SKILL.md:
  ```markdown
  ## When to Use

  This skill activates when:
  - Condition 1
  - Keywords: "keyword1", "keyword2"
  ```
- **Solution**: Usar `/skill:edit` para agregar sección

### Problemas de contenido

**Problem**: "La descripción supera los 1024 caracteres"
- **Cause**: La descripción de la habilidad es demasiado larga
- **Fix**: Acorta la descripción en SKILL.md frontmatter
- **Solution**: Usar `/skill:edit` para reducir la longitud

**Problem**: "SKILL.md supera las 500 líneas"
- **Cause**: La habilidad es demasiado grande/compleja
- **Fix**: Mover contenido a libros/archivos de cocina
- **Solution**: Usar `/skill:edit` reorganizar

**Problem**: "Enlace roto: libro de cocina/missing.md referenciado pero no encontrado"
- **Cause**: Referencia de archivo de libro de cocina sin archivo real
- **Fix**: Cree el archivo o elimine la referencia.
- **Solution**: Usar `/skill:edit` arreglar

### Problemas con el MCP

**Problem**: "JSON no válido en .mcp.json"
- **Cause**: Error de sintaxis en la configuración de MCP
- **Fix**: Compruebe que la sintaxis JSON sea válida
- **Solution**: Usar `/skill:edit` para arreglar JSON

**Problem**: "Ningún objeto mcpServers"
- **Cause**: .mcp.json falta la estructura requerida
- **Fix**: Agregue la estructura adecuada:
  ```json
  {
    "mcpServers": {
      "name": { ... }
    }
  }
  ```
- **Solution**: Usar `/skill:edit` actualizar

## Flujo de trabajo de prueba

### Prueba rápida (solo estructura)

```bash
/agileflow:skill:test supabase-swift
# When asked: select "Skip activation test"
```

Se necesitan unos segundos para verificar la estructura y el contenido del archivo.

### Prueba completa (Estructura + Activación)

```bash
/agileflow:skill:test supabase-swift
# When asked: select "Yes, test activation"
# When asked: pick or enter test prompt
```

Prueba que la habilidad realmente se activa ante indicaciones relevantes.

### Prueba Detallada (Estructura + Activación + Ver Contenido)

```bash
/agileflow:skill:test supabase-swift
# When asked: select "View SKILL.md content"
# Then manually review the skill
```

Ayuda a comprender qué hace la habilidad antes de usarla.

## Mejores prácticas

### Prueba después de la creación

Pruebe siempre una habilidad inmediatamente después de crearla:

```bash
/agileflow:skill:create my-skill
/agileflow:skill:test my-skill  # ← Right after creation
```

### Prueba después de editar

Prueba después de realizar cambios:

```bash
/agileflow:skill:edit my-skill
/agileflow:skill:test my-skill  # ← Verify changes worked
```

### Auditorías periódicas

Pruebe todas las habilidades periódicamente:

```bash
/agileflow:skill:list
# For each skill:
/agileflow:skill:test <skill-name>
```

### Pruebe nuevos desencadenantes

Cuando agrega nuevos activadores de activación:

```bash
/agileflow:skill:edit my-skill  # Add trigger keywords
/agileflow:skill:test my-skill  # Test with those keywords
```

## Solución de problemas

### La prueba se bloquea

Si la prueba parece bloquearse:

1. **Wait up to 30 seconds** - Las pruebas de activación pueden llevar tiempo
2. **Check internet connection** - Si prueba fuentes web
3. **Cancel (Ctrl+C)** y vuelve a intentarlo
4. **Skip activation test** - Pruebe la prueba de solo estructura

### Resultados inconsistentes

Si los resultados de la prueba varían:

1. **Network issues** - Algunas fuentes pueden no estar disponibles temporalmente
2. **Race conditions** - Intente realizar la prueba nuevamente
3. **Check file system** - Asegúrese de que los archivos sean legibles:
   ```bash
   ls -la .claude/skills/<skill>/
   ```

### La prueba de activación falla

Si "Habilidad activada correctamente" aparece como fallida:

1. Activadores de activación de actualización:
   ```bash
   /agileflow:skill:edit <skill-name>
   # Edit "When to Use" section
   ```

2. Prueba con diferentes indicaciones

3. Compruebe si las palabras clave son lo suficientemente específicas

## Comandos relacionados

- [`/habilidad:crear`](/commands/skill-create) - Generar una nueva habilidad
- [`/habilidad:lista`](/commands/skill-list) - Ver todas las habilidades
- [`/ habilidad: editar`](/commands/skill-edit) - Solucionar problemas de habilidades
- [`/habilidad:eliminar`](/commands/skill-delete) - Eliminar una habilidad
- [`/habilidad:actualizar`](/commands/skill-upgrade) - Agregar capacidad de aprendizaje

## Próximos pasos

1. **Test a skill**: `/agileflow:skill:test <skill-name>`
2. **If passes**: Usa la habilidad en tu trabajo
3. **If fails**: Editar con `/agileflow:skill:edit`
4. **After fixing**: Pruebe nuevamente para verificar
