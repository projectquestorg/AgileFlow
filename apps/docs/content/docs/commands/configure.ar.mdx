---
title: "/تكوين"
description: تكوين ميزات AgileFlow المتقدمة (git، والخطافات، والأرشفة، وCI، وخط الحالة)
---

# /تكوين

قم بتكوين ميزات AgileFlow المتقدمة باستخدام ملفات التعريف وخيارات التمكين/التعطيل وترحيل التنسيق لتخصيص إعداد سير العمل الخاص بك.

## بداية سريعة

```bash
/agileflow:configure
```

يؤدي هذا إلى فتح قائمة تكوين تفاعلية. يمكنك أيضًا التشغيل بخيارات محددة:

```bash
/agileflow:configure --profile=full
/agileflow:configure --enable=sessionstart,precompact
/agileflow:configure --disable=archival
```

## حدود

| Parameter | Required | Default | Description |
|-----------|----------|---------|-------------|
| `--profile` | No | - | Configuration profile: `full`, `basic`, `minimal`, or `none` |
| `--enable` | No | - | Comma-separated list of features to enable |
| `--disable` | No | - | Comma-separated list of features to disable |
| `--migrate` | No | - | Fix format issues in existing configuration |
| `--detect` | No | - | Check current configuration status |
| `--archival-days` | No | 7 | Days before archiving completed stories |

## ملفات تعريف التكوين

| Profile | SessionStart | PreCompact | Stop Hook | Archival | Status Line |
|---------|-------------|------------|-----------|----------|------------|
| `full` | ✅ | ✅ | ✅ | ✅ 7 days | ✅ |
| `basic` | ✅ | ✅ | ❌ | ✅ 7 days | ❌ |
| `minimal` | ✅ | ❌ | ❌ | ✅ 7 days | ❌ |
| `none` | ❌ | ❌ | ❌ | ❌ | ❌ |

## الميزات المتوفرة

- **sessionstart**: عرض الترحيب مع حالة المشروع عند بدء الجلسة
- **precompact**: الحفاظ على السياق عند ضغط المحادثات
- **stop**: تحذير بشأن تغييرات git غير الملتزم بها قبل إنهاء الجلسة
- **archival**: الأرشفة التلقائية للقصص القديمة المكتملة (أيام قابلة للتكوين)
- **statusline**: شريط الحالة المخصص يعرض القصة الحالية والتقدم

## أمثلة

### الإعداد الأساسي (مستحسن)

```bash
/agileflow:configure --profile=full
```

تمكين جميع الميزات: الخطافات والأرشفة وخط الحالة.

### تمكين ميزات محددة

```bash
/agileflow:configure --enable=sessionstart,precompact
```

تمكين خطافات SessionStart وPreCompact فقط، وتعطيل الآخرين.

### تعطيل الميزة

```bash
/agileflow:configure --disable=statusline
```

قم بتعطيل سطر الحالة المخصص مع الاحتفاظ بالميزات الأخرى.

### إصلاح مشكلات التكوين

```bash
/agileflow:configure --migrate
```

إذا رأيت تحذيرات بشأن التنسيق، فسيؤدي ذلك إلى إصلاحها تلقائيًا وإنشاء نسخة احتياطية على `.claude/settings.json.backup`.

### التحقق من الوضع الحالي

```bash
/agileflow:configure --detect
```

يعرض ما تم تمكينه أو تعطيله حاليًا دون إجراء تغييرات.

### عتبة الأرشفة المخصصة

```bash
/agileflow:configure --enable=archival --archival-days=14
```

أرشفة القصص المكتملة بعد 14 يومًا بدلاً من 7 أيام الافتراضية.

## ما الذي يتم تكوينه

### نظام السنانير
تكوين الخطافات في `.claude/settings.json` ل:
- SessionStart: يعرض معلومات المشروع عند بدء الجلسة
- UserPromptSubmit: يقوم بتشغيل التحقق من الصحة قبل الالتزامات
- التوقف: يحذر من التغييرات غير الملتزم بها عند إنهاء الجلسة

### نظام الأرشفة
التحديثات `docs/00-meta/agileflow-metadata.json` لتمكين الأرشفة التلقائية للقصص المكتملة الأقدم من الحد المحدد.

### خط الحالة
يقوم بتكوين شريط حالة Claude Code المخصص الذي يظهر:
- فرع بوابة الحالي
- معرف القصة النشطة
- عدد WIP (العمل قيد التقدم).
- نسبة الإنجاز

## الإخراج

بعد التكوين، سترى:

```text
✅ Configuration Complete!

Enabled:
  ✅ sessionstart
  ✅ precompact
  ✅ stop

Disabled:
  ❌ archival
  ❌ statusline

═══════════════════════════════════════════════════════
🔴 RESTART CLAUDE CODE NOW!
   Quit completely, wait 5 seconds, restart
═══════════════════════════════════════════════════════
```

قم دائمًا بإعادة تشغيل Claude Code بعد تغييرات التكوين حتى تدخل حيز التنفيذ.

## ملاحظات هامة

- **Format Migration**: إذا رأيت `⚠️ INVALID FORMAT` تحذيرات، تشغيل `--migrate` أولا قبل الخيارات الأخرى
- **Backup Created**: يقوم خيار الترحيل تلقائيًا بإنشاء نسخة احتياطية في `.claude/settings.json.backup`
- **Restart Required**: يجب عليك إنهاء Claude Code بالكامل وإعادة تشغيله حتى يتم تطبيق التغييرات
- **Metadata Updated**: يتم تعقب تكوين الميزة `docs/00-meta/agileflow-metadata.json`

## الأوامر ذات الصلة

- [`/تشخيص`](/commands/diagnose) - التحقق من صحة النظام وتكوينه
- [`/التحقق من صحة الخبرة`](/commands/validate-expertise) - التحقق من صحة ملفات الخبرة
- [`/مساعدة`](/commands/help) - عرض جميع الأوامر المتاحة
