---
title: /skill:bearbeiten
description: Bearbeiten Sie SKILL.md, Kochbucheinträge oder Referenzen eines vorhandenen Skills
---

# /skill:bearbeiten

Ändern Sie einen vorhandenen Skill in `.claude/skills/` - Anweisungen aktualisieren, Kochbucheinträge hinzufügen, Referenzen ändern oder mit neuen Forschungsergebnissen komplett neu schreiben.

## Schnellstart

```bash
# Interactive mode
/agileflow:skill:edit

# Edit specific skill
/agileflow:skill:edit supabase-swift
```

## Was es tut

Ermöglicht Ihnen, jeden Aspekt einer Fertigkeit zu ändern:

- **SKILL.md** - Aktivierungsauslöser und -anweisungen aktualisieren
- **references.md** - Dokumentationslinks hinzufügen oder aktualisieren
- **Cookbook entries** - Bearbeiten Sie spezifische Anwendungsfall-Workflows
- **Add new cookbook** - Erstellen Sie Workflows für neue Anwendungsfälle
- **MCP configuration** - Aktualisieren Sie die MCP-Servereinstellungen
- **Rename skill** - Ändern Sie den Skill-Namen und den Ordner

## Arbeitsablauf

### Schritt 1: Wählen Sie Fähigkeit aus

Wenn Sie keinen Skill-Namen angeben, können Sie aus den verfügbaren Skills auswählen:

```bash
/agileflow:skill:edit
```

**Interactive selection:**
```
Which skill would you like to edit?

[1] supabase-swift
    Supabase database operations for Swift iOS apps

[2] ui-components
    React component patterns with accessibility

[3] api-error-handling
    REST API error handling patterns

Select: 1
```

### Schritt 2: Fertigkeitsstruktur anzeigen

Der Editor zeigt Ihnen, womit Sie arbeiten:

```
📝 Editing: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Files:
  1. SKILL.md (main skill file)
  2. references.md (documentation links)
  3. cookbook/crud-operations.md
  4. cookbook/authentication.md
  5. .mcp.json (MCP configuration)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Schritt 3: Wählen Sie aus, was bearbeitet werden soll

```
What would you like to modify?

[1] SKILL.md
    Edit main skill instructions

[2] references.md
    Update documentation links

[3] Cookbook entry
    Edit a specific workflow

[4] Add new cookbook
    Create a new workflow

[5] MCP configuration
    Modify MCP server settings

[6] Rename skill
    Change skill name and folder

Select: 1
```

### Schritt 4: Nehmen Sie Änderungen vor

Basierend auf Ihrer Auswahl:

**For SKILL.md or references.md:**
```
How would you like to modify this file?

[1] Describe changes
    Tell me what to change and I'll update it

[2] Add content
    Append new sections or examples

[3] Replace section
    Rewrite a specific section

[4] Full rewrite
    Regenerate with fresh research (Recommended)

[5] Cancel
    Return without changes

Select: 1
```

**Then provide details:**
```
Describe what you'd like to change:
> Update the "When to Use" section to include GraphQL queries
> Add a new cookbook entry for Pagination
> Fix the authentication workflow steps
```

### Schritt 5: Vorschau der Änderungen

Der Editor zeigt Ihnen genau, was sich ändern wird:

```
📝 Changes to SKILL.md:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- ## When to Use
+ ## When to Use
+
+ This skill activates when:
+ - You're working with Supabase databases
+ - Keywords: "Supabase", "database", "query"

  ## Instructions

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Apply these changes?

[1] Yes, save changes
    Write the updated file

[2] No, discard
    Cancel without saving

[3] Edit more
    Make additional changes first

Select: 1
```

### Schritt 6: Bestätigen und speichern

Änderungen werden erst geschrieben, nachdem Sie Folgendes bestätigt haben:

```
✅ Updated: .claude/skills/supabase-swift/SKILL.md

Your changes have been saved. The skill will use the updated
instructions on next use.
```

## Beispiele

### Beispiel 1: Aktivierungsauslöser aktualisieren

```bash
/agileflow:skill:edit supabase-swift
```

**Selections:**
1. Wählen Sie „SKILL.md“
2. Wählen Sie „Abschnitt ersetzen“
3. Wählen Sie den Abschnitt: „Wann zu verwenden“
4. Geben Sie neue Trigger-Schlüsselwörter an

**Before:**
```markdown
## When to Use

This skill activates when:
- You're working with Supabase
- Keywords: "supabase", "database"
```

**After:**
```markdown
## When to Use

This skill activates when:
- You're working with Supabase in iOS Swift
- You need database operations, auth, or real-time
- Keywords: "supabase", "database", "auth", "realtime", "swift"
```

### Beispiel 2: Neuen Kochbucheintrag hinzufügen

```bash
/agileflow:skill:edit supabase-swift
```

**Selections:**
1. Wählen Sie „Neues Kochbuch hinzufügen“
2. Geben Sie den Anwendungsfallnamen ein: „Paginierung“.

**Generated:**
```
cookbook/pagination.md
├─ # Pagination with Supabase
├─ Prerequisites
├─ Instructions (step-by-step)
├─ Example
└─ Troubleshooting
```

Der Editor aktualisiert SKILL.md automatisch, um auf das neue Kochbuch zu verweisen.

### Beispiel 3: Dokumentationslinks aktualisieren

```bash
/agileflow:skill:edit api-error-handling
```

**Selections:**
1. Wählen Sie „Referenzen.md“
2. Wählen Sie „Inhalt hinzufügen“
3. Stellen Sie neue Ressourcen bereit

**Result:**
```markdown
# References for API Error Handling

## External Documentation

- RFC 7231 HTTP Status Codes: https://tools.ietf.org/html/rfc7231
- HTTP Status Codes Guide: https://httpwg.org/specs/rfc7231.html
- Your Project API Design: /docs/04-architecture/api-design.md
```

### Beispiel 4: Fertigkeit umbenennen

```bash
/agileflow:skill:edit
```

**Selections:**
1. Wählen Sie den zu bearbeitenden Skill aus
2. Wählen Sie „Skill umbenennen“
3. Neuen Namen eingeben: `supabase-ios` (War `supabase-swift`)

**Actions:**
1. Erstellt ein neues Verzeichnis mit neuem Namen
2. Kopiert alle Dateien
3. Aktualisiert SKILL.md frontmatter
4. Löscht altes Verzeichnis

**Result:**
```bash
# Before
.claude/skills/supabase-swift/

# After
.claude/skills/supabase-ios/
```

## Spezielle Bearbeitungsvorgänge

### Vollständige Neufassung (empfohlen bei größeren Änderungen)

Wenn Sie „Vollständig neu schreiben“ auswählen:

1. **Enter Plan Mode** - Das Kommando erforscht aktuelle Best Practices
2. **Collect new sources**:
   - Aktualisierte Webdokumentation
   - Neue lokale Dokumentation
   - Aktuelle Codebasismuster
3. **Show updated plan** - Sie genehmigen Änderungen
4. **Regenerate** - Erstellt eine neue Version mit neuer Forschung
5. **Preserve history** - Alte Version wird gesichert

Dies ist ideal, wenn:
- Die Technologieversion enthält wichtige Updates
- Ihre Projektmuster haben sich weiterentwickelt
- Sie möchten alle Inhalte aktualisieren

### Neuen Kochbucheintrag hinzufügen

Wenn Sie „Neues Kochbuch hinzufügen“ auswählen:

1. **Name the use case** - z. B. „Paginierung“, „Massenoperationen“
2. **Choose template** - Die Kochbuchstruktur wird automatisch ausgefüllt
3. **Update SKILL.md** - Referenzen werden automatisch hinzugefügt
4. **Edit if needed** - Verfeinern Sie den generierten Inhalt

### MCP-Konfiguration aktualisieren

Wenn Sie „MCP-Konfiguration“ auswählen:

1. **View current .mcp.json**
2. **Edit server settings**
3. **Add/remove environment variables**
4. **Test configuration** - Überprüfen Sie die Syntax

## Vorher-Nachher-Vergleich

Der Editor zeigt vor dem Speichern immer Unterschiede an:

```diff
## Cookbook

Based on context, select the appropriate workflow:

- ### Retrieve Data
- If you need to get data from Supabase:
- Then read cookbook/crud-operations.md

+ ### Query Data
+ If you need to retrieve rows from Supabase:
+ Then read cookbook/queries.md

+ ### Paginate Results
+ If you need to paginate large result sets:
+ Then read cookbook/pagination.md
```

## Kochbuchvorlage

Beim Hinzufügen eines neuen Kochbucheintrags wird automatisch Folgendes ausgefüllt:

```markdown
# [Use Case Title]

[Brief description of this workflow.]

## Prerequisites

- [Prerequisite 1]
- [Prerequisite 2]

## Instructions

1. **[Step 1]**:
   [Details]

2. **[Step 2]**:
   [language]
   [code example]

3. **[Step 3]**:
   [Details]

## Example

**Input**: [sample]
**Output**: [result]

## Troubleshooting

- **[Problem]**: [Solution]
```

## Best Practices

### Wann bearbeiten?

Bearbeiten Sie einen Skill, wenn:
- Aktualisierungen der Technologieversion
- Ihr Team entdeckt neue Muster
- Es entstehen neue Anwendungsfälle
- Dokumentationsänderungen
- Fehler müssen behoben werden

### Kenntnisse auf dem neuesten Stand halten

1. **Regular audits** - Testen Sie Ihre Fähigkeiten vierteljährlich
2. **User feedback** - Bearbeiten Sie es basierend auf dem, was Claude falsch macht
3. **Pattern evolution** - Aktualisieren Sie, wenn sich Ihre Codemuster weiterentwickeln
4. **Docs updates** - Halten Sie die Links zu externen Dokumenten aktuell

### Wirksame Veränderungen vornehmen

1. **Small, focused changes** - Aktualisieren Sie jeweils eine Sache
2. **Always preview** - Überprüfen Sie die Unterschiede vor dem Speichern
3. **Test after editing** - Laufen `/skill:test` nach großen Veränderungen
4. **Document why** - Fügen Sie Kommentare hinzu, die die Änderungen erläutern

## Fehlerbehandlung

### Fertigkeit nicht gefunden

```
❌ Skill "<name>" not found.

Available skills:
  - supabase-swift
  - ui-components

Use /agileflow:skill:list to see all skills.
```

**Fix**: Überprüfen Sie die Schreibweise des Fertigkeitsnamens oder listen Sie die verfügbaren Fertigkeiten auf.

### Frontmatter kann nicht geparst werden

```
⚠️ Could not parse SKILL.md frontmatter.

The skill file may have formatting issues.
Would you like me to try anyway, or edit manually?
```

**Fix**: Überprüfen Sie, ob SKILL.md über einen gültigen YAML-Frontmatter verfügt:
```yaml
---
name: skill-name
description: Skill purpose
---
```

### Fehler bei der Schreibberechtigung

```
❌ Cannot write to skill file.

Check permissions for: .claude/skills/<skill>/
```

**Fix**: Verzeichnisberechtigungen überprüfen:
```bash
chmod -R u+w .claude/skills/<skill>/
```

## Nach der Bearbeitung

Sobald die Änderungen gespeichert sind:

1. **Test the skill**:
   ```bash
   /agileflow:skill:test <skill-name>
   ```

2. **Use immediately** - Änderungen werden bei der nächsten Verwendung wirksam

3. **If errors** - Verwenden `/skill:edit` nochmal zum reparieren

4. **Enable learning** - Falls Sie es noch nicht getan haben:
   ```bash
   /agileflow:skill:upgrade <skill-name>
   ```

## Verwandte Befehle

- [`/skill:create`](/commands/skill-create) - Generieren Sie eine neue Fähigkeit
- [`/skill:list`](/commands/skill-list) - Alle Fähigkeiten anzeigen
- [`/skill:test`](/commands/skill-test) - Überprüfen Sie, ob die Änderungen funktioniert haben
- [`/skill:delete`](/commands/skill-delete) - Eine Fertigkeit entfernen
- [`/skill:upgrade`](/commands/skill-upgrade) - Lernfähigkeit hinzufügen

## Nächste Schritte

1. **List skills**: `/agileflow:skill:list`
2. **Edit one**: `/agileflow:skill:edit <skill-name>`
3. **Test changes**: `/agileflow:skill:test <skill-name>`
4. **Use it**: Ihre Änderungen sind sofort live
