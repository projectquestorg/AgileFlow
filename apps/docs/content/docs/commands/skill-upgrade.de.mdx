---
title: "/skill:upgrade"
description: Verbessern Sie vorhandene Fähigkeiten durch selbstverbessernde Lernfähigkeiten
---

# /skill:upgrade

Fügen Sie Fähigkeiten zur Selbstverbesserung hinzu, damit sie aus Ihren Korrekturen lernen und sich kontinuierlich verbessern.

## Schnellstart

```bash
# Interactive mode
/agileflow:skill:upgrade

# Upgrade specific skill
/agileflow:skill:upgrade supabase-swift

# Upgrade all skills at once
/agileflow:skill:upgrade --all
```

## Was es tut

Fertigkeiten, die vor der Einführung des Lernsystems erworben wurden, können aufgewertet werden, um Folgendes zu erlangen:

1. **Learnings file** - Verfolgt Vorlieben, Konventionen und Muster
2. **Self-improvement** - Lernt aus Ihren Korrekturen
3. **Preference persistence** - Merkt sich, was Sie bevorzugen
4. **Context accumulation** - Baut projektspezifisches Wissen auf
5. **Team consistency** - Gemeinsame Erkenntnisse im gesamten Team

## Wie es funktioniert

Der Upgrade-Prozess:

1. **Creates learnings file** - `.agileflow/skills/_learnings/<skill>.yaml`
2. **Updates SKILL.md** - Fügt Lernanweisungen hinzu
3. **Preserves content** - Bestehende Fertigkeitsinhalte unverändert
4. **Enables learning** - Fertigkeit kann jetzt aus Korrekturen lernen

## Lernsystem

### Welche Fähigkeiten lernen

Fähigkeiten verfolgen drei Arten von Lernen:

**Preferences** - Wie Sie erledigte Dinge mögen:
```yaml
preferences:
  code_style: "async/await over callbacks"
  error_handling: "Result<T, Error> type"
  naming: "camelCase for functions"
```

**Conventions** - Zu befolgende Projektmuster:
```yaml
conventions:
  - "Always validate input at service layer"
  - "Use interfaces for dependency injection"
  - "Database queries in separate service files"
```

**Anti-patterns** - Zu vermeidende Dinge:
```yaml
anti_patterns:
  - "Avoid callback patterns (use async/await)"
  - "Never hardcode configuration"
  - "Don't put business logic in controllers"
```

### Struktur der Learnings-Datei

Jede verbesserte Fähigkeit erhält eine Lerndatei:

```
.agileflow/skills/_learnings/
├── supabase-swift.yaml
├── react-components.yaml
└── api-client.yaml
```

Dateiformat:

```yaml
skill: supabase-swift
version: 1
last_updated: 2025-01-09T10:30:00Z

preferences:
  async_pattern: "async/await"
  error_handling: "Result<T, Error>"

conventions:
  - "Separate data, service, and UI layers"
  - "Always handle network errors explicitly"

anti_patterns:
  - "Avoid callback patterns"
  - "Never hardcode API keys"

context:
  # Project-specific learnings

examples:
  good:
    - "let user = try await supabase.auth.user()"
  bad:
    - "var apiKey = 'sk-...'"

metadata:
  corrections_count: 3
  approvals_count: 5
  last_correction: 2025-01-08T10:30:00Z
  created: 2025-01-01T00:00:00Z
```

## Arbeitsablauf

### Schritt 1: Wählen Sie Fähigkeit aus

Falls nicht angegeben:

```bash
/agileflow:skill:upgrade
```

**Choose a skill:**
```
Which skill would you like to upgrade?

[1] supabase-swift
    Supabase database operations for Swift iOS apps

[2] ui-components
    React component patterns with accessibility

[3] api-error-handling
    REST API error handling patterns

Select: 1
```

### Schritt 2: Überprüfen Sie den Status

Der Befehl prüft, ob der Skill bereits aktualisiert wurde:

```
Checking upgrade status for: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Skill is eligible for upgrade

Current state:
  - SKILL.md: exists
  - Learnings file: NOT YET CREATED
  - Learning capability: NOT ENABLED
```

Falls bereits aktualisiert:

```
✅ Skill "supabase-swift" already has self-improving capability!

Learnings file: .agileflow/skills/_learnings/supabase-swift.yaml
  - Corrections: 5
  - Last learning: 2025-01-08T10:30:00Z

View learnings: /agileflow:skill:edit supabase-swift
```

### Schritt 3: Zeigen Sie, was hinzugefügt wird

```
📦 Upgrade: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Changes to be made:

1. SKILL.md - Add sections:
   - ## Self-Improving Learnings
   - Update ## Instructions to include learning workflow

2. Create new file:
   - .agileflow/skills/_learnings/supabase-swift.yaml

The skill will now learn from your corrections!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Schritt 4: Vorschau (optional)

```
Proceed with upgrade?

[1] Yes, upgrade skill
    Add learning capability now

[2] Preview changes first (Recommended)
    Show exact changes before applying

[3] Cancel
    Exit without changes

Select: 2
```

Wenn Sie eine Vorschau anzeigen:

```
📝 Changes to SKILL.md (preview):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ## When to Use

+ ## Self-Improving Learnings
+
+ This skill learns from your corrections and preferences.
+
+ **On invocation**:
+ 1. Check if `.agileflow/skills/_learnings/supabase-swift.yaml` exists
+ 2. If exists, read and apply learned preferences
+ 3. Follow conventions and avoid anti-patterns from learnings

New file preview:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

.agileflow/skills/_learnings/supabase-swift.yaml

skill: supabase-swift
version: 1
last_updated: 2025-01-09T10:30:00Z

preferences: {}
conventions: []
anti_patterns: []
...

Apply changes? (Yes/No)
```

### Schritt 5: Lerndatei erstellen

Das System erstellt `.agileflow/skills/_learnings/<skill>.yaml`:

```yaml
skill: supabase-swift
version: 1
last_updated: 2025-01-09T10:30:00Z

# =============================================================================
# PREFERENCES
# =============================================================================
preferences:
  # Preferences will be learned from corrections

# =============================================================================
# CONVENTIONS
# =============================================================================
conventions:
  # - "Convention will be added from corrections"

# =============================================================================
# ANTI-PATTERNS
# =============================================================================
anti_patterns:
  # - "Things to avoid will be added here"

# =============================================================================
# CONTEXT
# =============================================================================
context:
  # Project-specific context will be learned

# =============================================================================
# EXAMPLES
# =============================================================================
examples:
  good: []
  bad: []

# =============================================================================
# METADATA
# =============================================================================
metadata:
  corrections_count: 0
  approvals_count: 0
  created: 2025-01-09T10:30:00Z
  sessions: []
```

### Schritt 6: SKILL.md aktualisieren

Das System fügt SKILL.md hinzu:

**New section:**
```markdown
## Self-Improving Learnings

This skill learns from your corrections and preferences.

**On invocation**:
1. Check if `.agileflow/skills/_learnings/supabase-swift.yaml` exists
2. If exists, read and apply learned preferences
3. Follow conventions and avoid anti-patterns from learnings

**On correction**:
1. When user corrects output, extract the signal
2. Determine confidence level:
   - **high**: Explicit correction ("never do X", "always do Y")
   - **medium**: User approved or pattern worked well
   - **low**: Observation to review later
3. Update the learnings file with new preference

**Learnings file location**: `.agileflow/skills/_learnings/supabase-swift.yaml`
```

### Schritt 7: Erfolg bestätigen

```
✅ Skill upgraded successfully!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Modified: .claude/skills/supabase-swift/SKILL.md
Created:  .agileflow/skills/_learnings/supabase-swift.yaml

The skill will now:
  • Load learned preferences on each invocation
  • Extract signals from your corrections
  • Build up conventions over time
  • Avoid learned anti-patterns

Start using the skill - it will learn from your feedback!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Beispiele

### Beispiel 1: Einzelne Fertigkeit verbessern

```bash
/agileflow:skill:upgrade api-client
```

**Response:**
```
✅ api-client upgraded successfully!

Created: .agileflow/skills/_learnings/api-client.yaml

The skill now has:
  • Learnings file for tracking preferences
  • Self-improvement capability
  • Context accumulation from corrections
```

### Beispiel 2: Batch-Upgrade aller Fertigkeiten

```bash
/agileflow:skill:upgrade --all
```

**Response:**
```
📦 Upgrading all skills...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Skills to upgrade:
  [1/3] supabase-swift... ✅
  [2/3] ui-components... ✅
  [3/3] api-client... ✅

✅ Upgraded 3 skills successfully!

All skills now have self-improving capability.
```

### Beispiel 3: Bereits aktualisiert

```bash
/agileflow:skill:upgrade react-components
```

**Response:**
```
✅ react-components already has self-improving capability!

Learnings file:
  .agileflow/skills/_learnings/react-components.yaml

Stats:
  - Corrections: 8
  - Last learning: 2025-01-08T15:30:00Z
  - Created: 2025-01-01T00:00:00Z

The skill is learning from your feedback. Keep using it!
```

## Wie Fähigkeiten lernen

### Phase 1: Bei der Fähigkeitsaufrufung

Wenn Sie eine verbesserte Fertigkeit verwenden:

1. Claude prüft, ob die Lerndatei vorhanden ist
2. Liest Vorlieben, Konventionen, Anti-Muster
3. Wendet sie beim Ausführen der Fertigkeit an
4. Beim nächsten Mal werde ich mich an Ihre Vorlieben erinnern

### Phase 2: Zur Korrektur

Wenn Sie Claudes Ausgabe korrigieren:

1. **You provide feedback**: „Nein, stattdessen async/await verwenden“
2. **Claude extracts signal**: „Async/await gegenüber Rückrufen bevorzugen“
3. **Determines confidence**:
   - **High**: Explizite Korrektur (zu den Erkenntnissen hinzufügen)
   - **Medium**: Sie haben die Ausgabe genehmigt (zu den Erkenntnissen hinzufügen)
   - **Low**: Beobachtung (Hinweis zur Überprüfung)
4. **Updates learnings file**: Neue Einstellung gespeichert
5. **Next use applies it**: Skill merkt sich Ihre Präferenz

### Beispiel-Lernzyklus

**Session 1:**
```
You: "Create an API error handler"

Claude: [uses try-catch blocks]

You: "No, prefer using Result<T, Error> type"

Claude: Extracts signal and updates learnings file
  preferences.error_handling = "Result<T, Error>"
```

**Session 2:**
```
You: "Create another API error handler"

Claude: [reads learnings file]
Claude: [uses Result<T, Error> type]

You: "Perfect!"

Claude: Updates learnings metadata: approvals_count++
```

**Session 3:**
```
You: "New API handler needed"

Claude: [automatically uses Result<T, Error>]
Claude: [follows all learned conventions]

The skill improved through learning!
```

## Erkenntnisse nutzen

### Manuelle Überprüfung

Überprüfen Sie, was der Skill gelernt hat:

```bash
cat .agileflow/skills/_learnings/supabase-swift.yaml
```

### Manuelle Updates

Fügen Sie vor Korrekturen Ihre eigenen Präferenzen hinzu:

```bash
# Edit the learnings file directly
vim .agileflow/skills/_learnings/api-client.yaml

# Add your preferences
preferences:
  error_handling: "Custom error types"
  logging: "Structured JSON logging"
```

### Teamfreigabe

Übertragen Sie die Erkenntnisse auf Git, damit das Team sie erhält:

```bash
git add .agileflow/skills/_learnings/
git commit -m "Add api-client learning from team feedback"
```

Teammitglieder:
```bash
git pull
# Now has the same learnings
```

## Best Practices

### Wann sollte ein Upgrade durchgeführt werden?

Verbessere eine Fertigkeit, wenn:

1. **Skill is frequently used** - Mehr Korrekturen = besseres Lernen
2. **Team has patterns** - Erkenntnisse erfassen Teampräferenzen
3. **Creating new skills** - Ermöglichen Sie das Lernen von Anfang an
4. **Consolidating skills** - Erkenntnisse aus alten Fähigkeiten zusammenführen

### Erkenntnisse effektiv nutzen

1. **Correct mistakes** - Jede Korrektur verbessert die Fertigkeit
2. **Approve good output** - Auch Freigaben sind Learnings
3. **Document patterns** - Konventionen manuell hinzufügen
4. **Review periodically** - Überprüfen Sie, was gelernt wurde
5. **Commit to git** - Teilen Sie Ihre Erkenntnisse mit dem Team

### Erkenntnisse bewahren

1. **Keep files updated** - Erkenntnisse bleiben aktuell
2. **Monitor corrections** - Die korrekte Zählung zeigt die Nutzung an
3. **Archive old learnings** - Bei der Festigung von Fähigkeiten
4. **Share improvements** - Teamkonsistenz

## Fehlerbehebung

### Bereits aktualisiert?

Wenn die Fähigkeit bereits gelernt hat:

```
✅ Skill already upgraded!

To:
  - View learnings: cat .agileflow/skills/_learnings/<name>.yaml
  - Edit learnings: /agileflow:skill:edit <name>
  - Clear learnings: rm .agileflow/skills/_learnings/<name>.yaml
```

### Es wurde keine Lerndatei erstellt

Wenn das Upgrade erfolgreich war, aber keine Lerndatei vorhanden ist:

1. Dateisystem prüfen:
   ```bash
   ls .agileflow/skills/_learnings/
   ```

2. Überprüfen Sie, ob das Verzeichnis vorhanden ist:
   ```bash
   mkdir -p .agileflow/skills/_learnings/
   ```

3. Upgrade erneut ausführen:
   ```bash
   /agileflow:skill:upgrade <skill-name>
   ```

### Upgrade fehlgeschlagen

Wenn Sie einen Fehler sehen:

```
❌ Failed to upgrade skill: Permission denied
```

**Fix**: Berechtigungen prüfen:
```bash
chmod -R u+w .agileflow/skills/
chmod -R u+w .agileflow/skills/_learnings/
```

Versuchen Sie dann das Upgrade erneut.

## Nach dem Upgrade

Nach dem Upgrade:

1. **Use the skill** - Fangen Sie an, damit zu arbeiten
2. **Correct Claude** - Jede Korrektur lehrt die Fähigkeit
3. **Monitor learning** - Überprüfen Sie die Anhäufung von Lerndateien
4. **Share with team** - Übertragen Sie Ihre Erkenntnisse auf Git

## Verwandte Befehle

- [`/skill:create`](/commands/skill-create) - Erstellen Sie Fertigkeiten mit aktiviertem Lernen
- [`/skill:list`](/commands/skill-list) - Alle Fähigkeiten anzeigen
- [`/skill:edit`](/commands/skill-edit) - Lerndatei bearbeiten
- [`/skill:test`](/commands/skill-test) - Überprüfen Sie, ob die Fertigkeit funktioniert
- [`/skill:delete`](/commands/skill-delete) - Fertigkeit entfernen

## Nächste Schritte

1. **List skills**: `/agileflow:skill:list`
2. **Upgrade frequently used**: `/agileflow:skill:upgrade <skill>`
3. **Use the skill** - Aufgaben bearbeiten und Claude korrigieren
4. **Review learnings**: `cat .agileflow/skills/_learnings/<skill>.yaml`
5. **Commit to git**: Erkenntnisse mit dem Team teilen
