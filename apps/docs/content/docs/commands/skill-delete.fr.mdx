---
title: /compétence : supprimer
description: Supprimer une compétence installée de .claude/skills/
---
# /compétence : supprimer

Supprimez en toute sécurité une compétence de `.claude/skills/` avec des fonctionnalités de sécurité complètes, notamment un aperçu, une confirmation et une sauvegarde facultative.

## Démarrage rapide

```bash
# Interactive mode
/agileflow:skill:delete

# Delete specific skill
/agileflow:skill:delete supabase-swift
```

## Ce qu'il fait

La commande de suppression :

1. **Affiche ce qui sera supprimé** - Arborescence de tous les fichiers
2. **Confirme avec vous** - Nécessite une approbation explicite
3. **Offre une option de sauvegarde** - Enregistrez une copie avant de la supprimer
4. **Supprime définitivement** - Supprime le répertoire de compétences
5. **Suggère les prochaines actions** - Que faire après la suppression

## Fonctionnalités de sécurité

Cette commande comporte plusieurs couches de sécurité :

- **Aucune suppression accidentelle** - Confirmez toujours avant de supprimer
- **Aperçu de l'arborescence** - Voir exactement ce qui est supprimé
- **Option de sauvegarde** - Exporter la compétence avant suppression
- **Un à la fois** - Supprimer des compétences individuelles (pas de suppression par lots)
- **Confirmation claire** - Choix explicite OUI/NON

## Flux de travail

### Étape 1 : Sélectionnez la compétence

Si vous ne précisez pas de compétence :

```bash
/agileflow:skill:delete
```

**Choisissez parmi les compétences disponibles :**
```
Which skill would you like to delete?

[1] supabase-swift
    Supabase database operations for Swift iOS apps

[2] ui-components (deprecated)
    Old component patterns (no longer using)

[3] api-error-handling
    REST API error handling patterns

Select: 2
```

### Étape2 : Aperçu de ce qui sera supprimé

La commande montre exactement le contenu de la compétence :

```
⚠️  About to delete: ui-components
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This will permanently remove:

  .claude/skills/ui-components/
  ├── SKILL.md
  ├── references.md
  ├── cookbook/
  │   ├── create-component.md
  │   ├── styling-patterns.md
  │   └── accessibility.md
  └── .mcp.json

Total: 6 files in 2 directories

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Étape 3 : Confirmer la suppression

```
Are you sure you want to delete this skill?
This cannot be undone.

[1] Yes, delete permanently
    Remove the skill and all its files

[2] No, keep it
    Cancel and return

[3] Export first
    Save a backup before deleting

Select: 3
```

### Étape 4 : Créer une sauvegarde (si demandé)

Si vous choisissez "Exporter d'abord" :

```
📦 Creating backup...

Backup saved to:
  .claude/skills-backup/ui-components-20251209-143052/

This contains a copy of everything. You can restore it later
if needed, or just delete the backup directory.

Ready to delete? (Yes/No)
```

Emplacement de sauvegarde :
```
.claude/skills-backup/
└── ui-components-20251209-143052/
    ├── SKILL.md
    ├── references.md
    ├── cookbook/
    └── .mcp.json
```

### Étape 5 : Suppression confirmée

```
✅ Deleted: ui-components

The skill has been permanently removed from .claude/skills/
```

### Étape 6 : Actions suivantes

Après la suppression, des options vous sont proposées :

```
What would you like to do next?

[1] Delete another skill
    Remove more skills

[2] Create new skill
    Generate a replacement skill

[3] List remaining skills
    See what's still installed

[4] Done
    Exit skill management

Select: 4
```

## Exemples

### Exemple 1 : Supprimer une compétence obsolète

```bash
/agileflow:skill:delete old-pattern
```

**Réponse :**
```
⚠️  About to delete: old-pattern
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This will permanently remove:
  .claude/skills/old-pattern/
  ├── SKILL.md
  └── references.md

Total: 2 files

Are you sure? (Yes/No/Export first)
> Yes

✅ Deleted: old-pattern
```

### Exemple 2 : Supprimer avec sauvegarde

```bash
/agileflow:skill:delete supabase-swift
```

**Réponse :**
```
⚠️  About to delete: supabase-swift
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This will permanently remove:
  .claude/skills/supabase-swift/
  ├── SKILL.md
  ├── references.md
  ├── cookbook/ (3 files)
  └── .mcp.json

Total: 6 files

Are you sure? (Yes/No/Export first)
> Export first

📦 Creating backup...

Backup saved to:
  .claude/skills-backup/supabase-swift-20251209-150000/

Ready to delete? (Yes/No)
> Yes

✅ Deleted: supabase-swift
```

### Exemple 3 : Sélection interactive

```bash
/agileflow:skill:delete
```

**Réponse :**
```
Which skill would you like to delete?

[1] api-integration
[2] database-queries
[3] react-components

Select: 2

⚠️  About to delete: database-queries
...
```

## Gestion des sauvegardes

### Emplacement de sauvegarde

Les sauvegardes sont enregistrées avec un horodatage :

```
.claude/skills-backup/
├── ui-components-20251208-093000/
├── api-client-20251208-103500/
└── database-20251209-150000/
```

Format : `<skill-name>-<YYYYMMDD-HHMMSS>/`

### Restaurer à partir d'une sauvegarde

Si vous supprimez une compétence et souhaitez la récupérer :

```bash
# Copy from backup
cp -r .claude/skills-backup/supabase-swift-20251209-150000 \
      .claude/skills/supabase-swift

# Verify it works
/agileflow:skill:test supabase-swift
```

### Nettoyer les anciennes sauvegardes

Supprimez périodiquement les anciennes sauvegardes :

```bash
# List backups
ls -la .claude/skills-backup/

# Remove old backup
rm -rf .claude/skills-backup/old-skill-20250101-120000/
```

## Quand Dsupprimer

Supprimez les compétences lorsque :

- **Plus nécessaire** - La technologie est obsolète
- **Obsolète** - Une meilleure alternative existe
- **Dupliqué** - Consolidé en une seule compétence
- **Faible qualité** - La compétence n'est pas utile
- **Organisation** - Trop de compétences similaires

## Avant de supprimer

Considérez :

1. **Êtes-vous sûr ?** - La suppression est définitive
2. **Documenter pourquoi** - Remarque dans git commit
3. **Sauvegarder d'abord** - Utilisez l'option "Exporter d'abord"
4. **Créer un remplacement** - Si consolidation des compétences
5. **Notifier l'équipe** - Si partagé dans git

## Alternative à la suppression

Au lieu de supprimer, vous pouvez :

1. **Renommer** pour indiquer qu'il est obsolète :
   ```bash
   /agileflow:skill:edit <skill-name>
   # Select "Rename skill"
   # Rename to: deprecated-<old-name>
   ```

2. **Archiver** en quittant les compétences actives :
   ```bash
   mv .claude/skills/old-skill .claude/skills-archive/
   ```

3. **Marquer dans la documentation** :
   - Modifiez SKILL.md pour ajouter un avis de dépréciation
   - Toujours disponible mais marqué comme obsolète

## Gestion des erreurs

### Compétence introuvable

```
❌ Skill "<name>" not found.

Available skills:
  - supabase-swift
  - ui-components
  - api-error-handling

Use /agileflow:skill:list to see all skills.
```

**Correction** : vérifiez l'orthographe du nom de la compétence ou répertoriez les compétences disponibles.

### Aucune compétence à supprimer

```
❌ No skills to delete.

No skills are currently installed in .claude/skills/

Create a skill first: /agileflow:skill:create
```

**Correction** :Créez une compétence avant d’essayer d’en supprimer une.

### Échec de la suppression

```
❌ Failed to delete skill: Permission denied

The skill directory may be in use or have permission issues.
Try manually: rm -rf .claude/skills/<skill>/
```

**Correction** : Vérifiez les autorisations des fichiers :
```bash
ls -la .claude/skills/<skill>/
chmod -R u+w .claude/skills/<skill>/
rm -rf .claude/skills/<skill>/
```

### Échec de la création de la sauvegarde

```
⚠️  Could not create backup.

Check permissions for:
  .claude/skills-backup/

Manual backup:
  cp -r .claude/skills/<skill> .claude/skills-backup/
```

**Correction** : assurez-vous que le répertoire de sauvegarde est accessible en écriture :
```bash
mkdir -p .claude/skills-backup/
chmod u+w .claude/skills-backup/
```

## meilleures pratiques

### Workflow de suppression

1. **Utilisez `/skill:list`** pour voir toutes les compétences
2. **Testez avant de supprimer** en cas de doute :
   ```bash
   /agileflow:skill:test <skill-name>
   ```
3. **Utilisez l'option "Exporter d'abord"** pour la sauvegarde
4. **Confirmer la suppression** explicitement
5. **Notez dans git** pourquoi la compétence a été supprimée

### Stratégie de sauvegarde

1. **Toujours exporter** pour les compétences créées il y a plus d'un mois
2. **Conserver les sauvegardes** pendant 30 jours minimum
3. **Document** quelles compétences sont sauvegardées
4. **Tester les restaurations** périodiquement

### Flux de travail de l'équipe

Si vous partagez des compétences dans git :

1. **Commettre la suppression** avec le motif :
   ```bash
   git commit -m "Remove: deprecated-api-client skill - \
   consolidated into api-integration skill"
   ```

2. **Informer l'équipe** de la suppression

3. **Conserver la sauvegarde** dans la branche git (facultatif) :
   ```bash
   git checkout -b backup-deprecated-skills
   git checkout main
   ```

## Commandes associées

- [`/skill:list`](/commands/skill-list) - Voir toutes les compétencesls avant de supprimer
- [`/skill:create`](/commands/skill-create) - Créer une compétence de remplacement
- [`/skill:edit`](/commands/skill-edit) - Renommer au lieu de supprimer
- [`/skill:test`](/commands/skill-test) - Vérifier avant suppression
- [`/skill:upgrade`](/commands/skill-upgrade) - Améliorer les compétences existantes

## Prochaines étapes

1. **Liste des compétences** : `/agileflow:skill:list`
2. **Identifier les obsolètes** : lesquels ne sont pas utilisés ?
3. **Sauvegarder d'abord** : utilisez l'option "Exporter d'abord"
4. **Supprimer soigneusement** : Confirmez chaque suppression
5. **Nettoyer les sauvegardes** : supprimez périodiquement les anciennes sauvegardes