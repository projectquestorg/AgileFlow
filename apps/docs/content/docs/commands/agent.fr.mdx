---
title: /agent
description: Embarquer un nouvel agent avec profil et contrat
---
# /agent

Intégrez de nouveaux agents avec des profils, des invites système (contrats) et des limites de portée pour une collaboration d'équipe efficace.

## Démarrage rapide

```bash
/agileflow:agent AGENT_ID=AG-UI ROLE="Frontend Developer" TOOLS="Read,Write,Bash" SCOPE="src/components/,frontend"
```

## Paramètres

| Paramètre | Obligatoire | Par défaut | Descriptif |
|-----------|----------|---------|-------------|
| `AGENT_ID` | Oui | - | Identifiant unique de l'agent (par exemple, AG-UI, AG-API) |
| `ROLE` | Oui | - | Rôle ou titre de l'agent |
| `TOOLS` | Non | Lire, écrire, modifier, Bash | Outils disponibles séparés par des virgules |
| `SCOPE` | Non | - | Répertoires et balises d'histoire auxquels l'agent peut accéder |

## Exemples

### Développeur frontend intégré

```bash
/agileflow:agent AGENT_ID=AG-UI ROLE="Frontend Developer" TOOLS="Read,Write,Edit,Bash,Glob" SCOPE="src/components/,src/pages/,frontend,ui"
```

Crée un profil d'agent avec :
- Rôle et responsabilités
- Outils disponibles (Lecture, Écriture, Édition, Bash)
- Limites de portée (uniquement src/components/ et src/pages/)
- Invite système (contrat) avec des règles strictes

### Développeur d'API intégré

```bash
/agileflow:agent AGENT_ID=AG-API ROLE="Backend API Developer" TOOLS="Read,Write,Edit,Bash,Grep" SCOPE="src/api/,src/services/,backend,api"
```

### Spécialiste CI/CD embarqué

```bash
/agileflow:agent AGENT_ID=AG-CI ROLE="CI/CD Automation Engineer" TOOLS="Read,Write,Bash,Grep" SCOPE=".github/,scripts/,ci,devops"
```

## Sortie

### 1. Profil de l'agent

Crée le profil d'agent markdown à `docs/02-practices/prompts/agents/agent-<AGENT_ID>.md`

Exemple de contenu :

```text
---
agent_id: AG-UI
role: Frontend Developer
tools: [Read, Write, Edit, Bash, Glob]
scope:
  directories: [src/components/, src/pages/]
  story_tags: [frontend, ui, ux]
---

# AG-UI: Frontend Developer Agent

## Responsibilities
Build and maintain user interface components with React and Tailwind CSS.

## Available Tools
- Read - Read files and directories
- Write - Create new files
- Edit - Modify existing files
- Bash - Run commands (npm, git)
- Glob - Search for files by pattern

## Scope Boundaries

### Allowed Directories
- `src/components/` - UI components
- `src/pages/` - Page layouts
- `public/` - Static assets

### Restricted Directories
- `src/api/` - Backend code
- `.github/` - CI/CD workflows

### Story Tags
- `frontend` - UI features
- `ui` - Component work
- `ux` - User experience
- `design` - Design implementation

## System Prompt (Contract)

### Scope Restrictions
You can ONLY modify files in:
- `src/components/**`
- `src/pages/**`

You CANNOT modify:
- Backend code in `src/api/`
- CI/CD workflows in `.github/`

### Testing Requirements
Before committing, run:
- `npm test -- src/components/` - Run component tests
- `npm run lint:css` - Check Tailwind usage

### Commit Message Format
Use conventional commits with `ui:` prefix:
- `feat(ui): add new button component`
- `fix(ui): correct responsive breakpoint`
- `style(ui): update component spacing`

### Status Updates
After completing a story:
1. Update `docs/09-agents/status.json` with new status
2. Log to `docs/09-agents/bus/log.jsonl`
3. Include completion time and any blockers

### Code Standards
- TypeScript required (no .js files)
- JSDoc comments on exported components
- Storybook stories for all components
- 80%+ test coverage for components
- Accessibility checks (axe-core)
```

### 2. Liste des agents

Met à jour ou crée `docs/09-agents/roster.yaml` :

```yaml
agents:
  AG-UI:
    id: AG-UI
    role: Frontend Developer
    tools: [Read, Write, Edit, Bash, Glob]
    scope:
      directories: [src/components/, src/pages/]
      story_tags: [frontend, ui, ux]
  AG-API:
    id: AG-API
    role: Backend API Developer
    tools: [Read, Write, Edit, Bash, Grep]
    scope:
      directories: [src/api/, src/services/]
      story_tags: [backend, api, database]
```

### 3. Invite système

Génère une invite système condensée à copier/coller en tant qu'invite système du terminal :

```text
You are AG-UI, a Frontend Developer Agent.

ROLE: Build and maintain user interface components.

SCOPE BOUNDARIES (Strict):
- Can modify: src/components/, src/pages/
- Cannot modify: src/api/, .github/
- Story tags: frontend, ui, ux

TESTING:
- Run npm test before committing
- Maintain 80%+ coverage
- Check accessibility with axe-core

COMMITS:
- Format: "feat(ui):" or "fix(ui):"
- Conventional commits required

STATUS:
- Update docs/09-agents/status.json after completing stories
- Log events to docs/09-agents/bus/log.jsonl
```

## Composants du profil d'agent

### Responsabilités

Description claire de ce que fait l'agent :
- Domaine spécifique (UI, API, CI/CD)
- Technologies utilisées
- Livrables clés
- Rôle d'équipe

### Outils disponibles

Ce que l'agent peut utiliser :
- **Lire** - Lire des fichiers et analyser le code
- **Écrire** - Créer de nouveaux fichiers
- **Modifier** - Modifier les fichiers existants
- **Bash** - Exécuter des commandes shell (git, npm, docker)
- **Glob** - Rechercher des fichiers par modèle
- **Grep** - Rechercher le contenu du fichier

### Limites de la portée

Où l'agent peut travailler :
- **Répertoires autorisés** - L'agent de fichiers peut modifier
- **Répertoires restreints** - L'agent de fichiers NE PEUT PAS toucher
- **Balises d'histoire** - Quel travail est attribué à cet agent

### Invite système (contrat)

L'agent de règles doit suivre :
- **Restrictions de portée** - Chemins d'accès exacts aux fichiers
- **Exigences de test** - Quand/comment tester
- **Conventions de validation** - Format de branche et de message
- **Mises à jour du statut** - Comment suivre les progrès
- **Normes du code** - Exigences de qualité

## meilleures pratiques

### Nommer des agents
Utilisez des identifiants descriptifs qui indiquent clairement le rôle :
- ✅ Bon : AG-UI, AG-API, AG-CI, AG-DEVOPS, AG-SECURITY
- ❌ Mauvais : AG-001, AG-A, AG-DEV1

### Définir la portée

Établissez des limites claires :

**Bon** - Parcours spécifiques :
- `src/components/`
- `src/api/routes/`
- `.github/workflows/`

**Mauvais** – Limites vagues :
- "trucs frontaux"
- "choses back-end"

### Exigences de test

Incluez des commandes de test spécifiques :

**Bon** – Effacer les commandes :
- Exécutez `npm test -- src/components/`
- Vérifiez la couverture avec `npm run coverage`
- Peluches avec `npm run lint:tsx`

**Mauvais** – Instructions vagues :
- "Ecrire des tests"
- "Assurez-vous que cela fonctionne"

### Cartographie des balises d'histoire

Attribuez des histoires en fonction de balises :

| Agent | Balises d'histoire |
|-------|------------|
| AG-UI | frontend, interface utilisateur, ux, conception |
| AG-API | dosd, api, database |
| AG-CI | ci, devops, automation |

## Workflow

1. Gather agent details:
   - Unique ID (AG-DOMAIN)
   - Role/title
   - Available tools
   - Scope boundaries
   - Story tags

2. Create agent profile from template
   - Responsibilities section
   - Tools list
   - Scope boundaries (clear and specific)
   - System prompt (contract with rules)

3. Update agent roster
   - Add entry to docs/09-agents/roster.yaml
   - Map ID → role, tools, scope

4. Generate system prompt
   - Output condensed prompt
   - Ready to paste as terminal system prompt

5. Validate agent
   - Review scope boundaries (no conflicts)
   - Confirm testing requirements
   - Check story tag assignments

## Integration

Agent profiles integrate with:
- **Status tracking** - Roster shows active agents
- **Story assignment** - Use agent ID and tags
- **Capacity planning** - See who's available
- **Handoffs** - Reference agent profiles in comms
- **Scope validation** - Prevent out-of-scope changes

## Use quand

- **Intégration des nouveaux membres de l'équipe** - Créer un profil et un contrat
- **Ajout de rôles spécialisés** - Agents de sécurité, performances, accessibilité
- **Équipe de restructuration** - Mettre à jour les limites du périmètre
- **Agents de formation** - Utiliser l'invite du système comme référence
- **Résoudre les conflits** - Une portée claire évite les chevauchements

## Commandes associées

- [`/handoff`](/commands/handoff) - Documenter les transitions de travail entre les agents
- [`/status`](/commands/status) - Afficher tous les agents actifs et affectations
- [`/babysit`](/commands/babysit) - Surveiller la progression de l'agent
- [`/metrics`](/commands/metrics) - Mesures de performances de l'agent