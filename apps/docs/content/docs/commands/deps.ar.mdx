---
title: /deps
description: تصور الرسم البياني للتبعية مع الكشف عن المسار الحرج
---

# /deps

تصور وتحليل الرسوم البيانية للتبعية القصة/الملحمية من خلال اكتشاف المسار الحرج وتحديد التبعية الدائرية والتوصيات القابلة للتنفيذ.

## بداية سريعة

```bash
/agileflow:deps
```

## حدود

| Parameter | Required | Default | Description |
|-----------|----------|---------|-------------|
| `SCOPE` | No | `all` | `story`, `epic`, or `all` - scope of visualization |
| `EPIC` | No | - | Show dependencies within specific epic (e.g., `EP-0010`) |
| `STORY` | No | - | Show dependencies for specific story (e.g., `US-0032`) |
| `FORMAT` | No | `ascii` | Output format: `ascii`, `mermaid`, `graphviz`, or `json` |
| `ANALYSIS` | No | `all` | Analysis type: `critical-path`, `circular`, `blocking`, or `all` |

## أمثلة

### عرض الرسم البياني الكامل للتبعية

```bash
/agileflow:deps
```

يُظهر الرسم البياني ASCII لجميع القصص وتبعياتها مع مؤشرات الحالة والمسار الحرج وتحليل القصة المحظورة.

### تحليل التبعيات الملحمية

```bash
/agileflow:deps EPIC=EP-0010
```

تصور التبعيات فقط ضمن ملحمة المصادقة، مما يساعد على فهم ترتيب العمل.

### تحقق من تبعيات القصة المحددة

```bash
/agileflow:deps STORY=US-0032
```

يوضح ما تعتمد عليه هذه القصة وما تعتمد عليه القصص، بالإضافة إلى تحليل التأثير.

### تصدير كمخطط حورية البحر

```bash
/agileflow:deps FORMAT=mermaid
```

يُنشئ تنسيق مخطط حورية البحر مناسبًا للتوثيق أو GitHub.

### البحث عن المسار الحرج فقط

```bash
/agileflow:deps ANALYSIS=critical-path
```

يُظهر فقط أطول مسار تبعية (عنق الزجاجة لإكمال المشروع).

### كشف التبعيات الدائرية

```bash
/agileflow:deps ANALYSIS=circular
```

يقوم بالمسح بحثًا عن الدورات التي يستحيل إكمالها ويقترح الإصلاحات.

## تنسيق الإخراج

### رسم بياني ASCII (افتراضي)

```text
Story Dependency Graph
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Legend:
  ✅ Done    🟢 Ready    🟡 In Progress    🔵 In Review    ⚪ Blocked

EP-0010: Authentication Epic
┌────────────────────────────────────────────────────────────┐
│                                                            │
│  ✅ US-0030 - Database schema                              │
│  │                                                         │
│  ├─▶ ✅ US-0031 - User model                               │
│  │   │                                                     │
│  │   ├─▶ 🟡 US-0032 - Login endpoint (in-progress)        │
│  │   │   │                                                 │
│  │   │   ├─▶ 🟢 US-0033 - Login UI                         │
│  │   │   │                                                 │
│  │   │   └─▶ ⚪ US-0045 - Password reset (blocked)         │
│  │   │                                                     │
│  │   └─▶ ✅ US-0034 - JWT middleware                       │
│  │                                                         │
│  └─▶ 🔵 US-0035 - User registration (in-review)            │
│                                                            │
└────────────────────────────────────────────────────────────┘

Critical Path:
  US-0030 → US-0031 → US-0032 → US-0033  (11 days total)

Blocking Stories:
  US-0040 blocks: US-0041, US-0042  (⚠️ High impact)
  US-0032 blocks: US-0033, US-0045  (⚠️ High impact)

Circular Dependencies: None detected ✅
```

### تنسيق مخطط حورية البحر

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="/images/docs/commands/deps-graph.dark.svg" />
  <img alt="Dependency Graph Example" src="/images/docs/commands/deps-graph.light.svg" />
</picture>

## أنواع التحليل

### المسار الحرج
**Shows**: أطول سلسلة تبعية من البداية إلى النهاية

يساعد على التعرف على:
- ما هي القصص الاختناقات
- المسار الأكثر أهمية لإنجاز المشروع
- حيث يكون للتأخير أقصى تأثير
- قصص لتحديد أولويات الانتهاء

```text
Critical Path: US-0030 → US-0031 → US-0032 → US-0033
Duration: 8 days
⚠️ US-0032 is in-progress on critical path
   → Any delay here delays entire epic
```

### التبعيات الدائرية
**Shows**: القصص التي تعتمد على بعضها البعض في دورة

مثال على مشكلة التبعية الدائرية:
```text
❌ CIRCULAR DETECTED!

Cycle: US-0050 → US-0051 → US-0052 → US-0050

Impact: ⚠️ CRITICAL - These stories cannot be completed
```

**How to fix**:
1. مراجعة التصميم المعماري
2. كسر التبعية عن طريق إدخال التجريد
3. إنشاء قصة الواجهة/العقد
4. أعد صياغة قصة واحدة لكسر الدورة

### حظر القصص
**Shows**: القصص التي تحجب معظم الأعمال الأخرى

يحدد الاختناقات عالية التأثير:
```text
High Impact Blockers:
┌────────┬──────────────────┬────────┬──────────────┐
│ Story  │ Title            │ Status │ Blocks       │
├────────┼──────────────────┼────────┼──────────────┤
│ US-0040│ Stripe integ.    │ 🟢 Ready│ 2 stories  │
│ US-0032│ Login endpoint   │ 🟡 WIP │ 2 stories  │
└────────┴──────────────────┴────────┴──────────────┘

Action: Start US-0040 immediately (blocks payment epic)
```

## مؤشرات حالة القصة

| Indicator | Meaning | Action |
|-----------|---------|--------|
| ✅ Done | Story completed | Doesn't block others anymore |
| 🟢 Ready | Ready for development | Can be started anytime |
| 🟡 In Progress | Currently being worked | Monitor for delays |
| 🔵 In Review | Awaiting approval | May complete soon |
| ⚪ Blocked | Waiting on dependencies | Can't start until unblocked |

## السيناريوهات المشتركة

### السيناريو: القصة ذات أولوية عالية ولكنها محظورة

```text
⚪ US-0045 - Password reset (blocked)
   Waiting for: US-0032 (Login endpoint, 75% done)

Recommendation:
  - Check US-0032 progress daily
  - Consider pairing to accelerate if delayed
  - US-0045 can start as soon as US-0032 is done
```

### السيناريو: العثور على تبعية دائرية

```text
❌ Services have circular dependency

Current: Auth → User → Session → Auth (cycle)

Solution:
  1. Create US-0055: "Auth/User service contract"
  2. Make all three depend on US-0055 (new foundation)
  3. Now: Auth/User/Session → US-0055 (no cycle)
```

### السيناريو: قصة المسار الحرج معرضة للخطر

```text
US-0032 is on CRITICAL PATH but in-progress
⚠️ Currently 75% done, but original estimate was 2d

If it slips:
  - US-0033 blocked (1d delay)
  - US-0045 blocked (2d delay)
  - Entire epic completion delayed

Recommendation:
  - Daily standup on progress
  - Pair if needed to accelerate
  - Identify blockers immediately
```

## فرص العمل الموازية

يحدد الأمر القصص التي يمكن العمل عليها بالتوازي:

```text
Stories That Can Start Now (No Blockers):

EP-0010: Authentication
  ✅ US-0033 (Login UI)         - AG-UI available
  ✅ US-0040 (Stripe integ.)    - HIGH PRIORITY

Recommended Assignments:
  AG-UI:  US-0033 (ready now)
  AG-API: US-0040 (high impact blocker)
```

## التكامل مع الأوامر الأخرى

- **Before sprint planning**: يجري `/deps` لفهم المخاطر
- **During sprint**: مراقبة المسار الحرج مع `/board`
- **When investigating blockers**: يستخدم `/deps` لنرى ما يعتمد على هذه القصة
- **When adding dependencies**: أعد التشغيل للتحقق من وجود مخاطر جديدة
- **When a story is blocked**: يستخدم `/deps STORY=US-XXXX` لرؤية السلسلة

## أفضل الممارسات

- **Check before each sprint** - يمكن أن تتغير التبعيات مع تحسين القصص
- **Prioritize blocking stories** - ابدأ بالحاصرات عالية التأثير مبكرًا
- **Monitor critical path** - القصص على المسار الحرج تستحق المزيد من الاهتمام
- **Detect cycles early** - يجري `/deps ANALYSIS=circular` بانتظام
- **Sequence dependent work** - ابدأ القصص التأسيسية أولاً
- **Document dependencies clearly** - استخدم واجهة القصة لربط التبعيات

## الأوامر ذات الصلة

- [`/ سبرينت`](/commands/sprint) - تخطيط Sprint يحترم التبعيات
- [`/اللوحة`](/commands/board) - عرض التقدم السريع والحاصرات
- [`/الحاصرات`](/commands/blockers) - تتبع وحل القصص المحظورة
- [`/التحقق من صحة القصة`](/commands/story-validate) - التحقق من وجود التبعيات
- [`/المقاييس`](/commands/metrics) - ربط وقت الدورة بعمق التبعية
