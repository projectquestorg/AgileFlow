---
title: ""/مهارة:إنشاء""
description: أنشئ مهارة مخصصة من خلال البحث على الويب ونمط كتاب الطبخ وتكامل MCP
---

# /مهارة:إنشاء

أنشئ مهارات ديناميكية مدعومة بالأبحاث تجمع بين وثائق الويب ومستنداتك المحلية وأنماط قاعدة التعليمات البرمجية.

## بداية سريعة

```bash
# Interactive mode (recommended)
/agileflow:skill:create

# With skill name hint
/agileflow:skill:create supabase-swift
```

## ماذا يفعل

ال `/skill:create` ينشئ الأمر مهارات مخصصة من خلال البحث في ثلاثة مصادر وبناء مسارات عمل مدعومة بالأبحاث:

1. **Web Documentation** - المستندات الرسمية ومراجع API والبرامج التعليمية
2. **Local Project Documentation** - ممارساتك، قراراتك المعمارية
3. **Codebase Patterns** - أمثلة واتفاقيات التعليمات البرمجية الموجودة

ثم يقوم بإنشاء:
- **SKILL.md** - ملف المهارات الرئيسية مع مشغلات التنشيط
- **references.md** - روابط لجميع مصادر الوثائق
- **cookbook/** - سير العمل خطوة بخطوة لحالات الاستخدام المختلفة
- **.mcp.json** - تكوين خادم MCP اختياري

## كيف يعمل

### المرحلة الأولى: المتطلبات

تصف المهارة التي تحتاجها:

```xml
Skill categories:
- Database integration (Supabase, PostgreSQL, MongoDB)
- API integration (REST, GraphQL, webhooks)
- UI/Frontend (Components, styling, design system)
- Development workflow (Testing, deployment, CI/CD)
- Custom description (describe what you need)
```

### المرحلة الثانية: البحث

يدخل الأمر في وضع الخطة ويبحث:

- **Web docs**: عناوين URL للوثائق الرسمية، والبرامج التعليمية، والأدلة
- **Local docs**: الملفات الموجودة في الخاص بك `docs/` الدليل المطابق لمجال المهارة
- **Codebase patterns**: الكود الموجود يوضح كيفية القيام بالأشياء
- **MCP servers**: الأدوات المتاحة للتكنولوجيا

فهو يجمع عناوين URL والأنماط دون نسخ المحتوى.

### المرحلة 3: التوليد

وبناءً على نتائج الأبحاث، فإنه يولد:

- **SKILL.md** مع مشغلات التنشيط ومراجع كتاب الطبخ
- **references.md** ربط جميع مصادر الويب/المحلية/قاعدة التعليمات البرمجية
- **cookbook/** الدليل بملف واحد لكل حالة استخدام
- **.mcp.json** إذا تم العثور على خادم MCP

### المرحلة الرابعة: التأكيد

ترى البنية التي تم إنشاؤها، ويمكنك معاينة المحتوى، والموافقة على الإنشاء.

## حدود

لا يحتوي هذا الأمر على أي معلمات - فهو تفاعلي تمامًا.

```bash
/agileflow:skill:create              # Interactive mode
/agileflow:skill:create my-skill     # Optional: suggest skill name
```

## أمثلة

### مثال 1: إنشاء مهارة Supabase

```bash
/agileflow:skill:create
```

**Your responses:**
```
What skill would you like to create?
> [Select "Database integration"]

Which database?
> Supabase

What language/framework?
> Swift iOS app

What use cases?
> Authentication, CRUD operations, Real-time subscriptions
```

**Generated structure:**
```
.claude/skills/supabase-swift/
├── SKILL.md (activation triggers, cookbook references)
├── references.md (links to Supabase docs, local patterns)
├── cookbook/
│   ├── authentication.md (sign up, sign in, sign out)
│   ├── crud-operations.md (create, read, update, delete)
│   └── realtime-subscriptions.md (listen to changes)
└── .mcp.json (Supabase MCP server config)
```

### المثال 2: إنشاء مهارة مكونات التفاعل

```bash
/agileflow:skill:create ui-components
```

**Interactive prompts:**
```
Describe what you need:
> Building reusable React components with accessibility and Tailwind CSS

Frameworks/libraries:
> React 18, TypeScript, Tailwind CSS, shadcn/ui

Use cases:
> Creating new components, styling patterns, making accessible
```

**Generated files include:**
- مراجع لمستندات React ومستندات Tailwind وإرشادات إمكانية الوصول
- أنماط قاعدة التعليمات البرمجية من `src/components/`
- إدخالات كتاب الطبخ لأنواع المكونات المشتركة

## الإخراج

يظهر لك الأمر:

1. **Research Summary** - ما وجده في كل مصدر
2. **Skill Structure** - الملفات التي سيتم إنشاؤها
3. **Preview** - أول 50 سطرًا من SKILL.md
4. **Confirmation** - نعم/لا للإنشاء

مثال الإخراج:

```
📚 Research Complete
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Web Docs:
  ✓ Supabase Swift SDK docs
  ✓ API reference
  ✓ Authentication guide

Local Documentation:
  ✓ docs/02-practices/database.md
  ⚠ No local patterns found

Codebase Patterns:
  ✓ src/lib/supabase.ts (client setup)
  ✓ src/services/auth/ (auth service)

MCP Server:
  ✓ @supabase/mcp-server available

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Will create:
.claude/skills/supabase-swift/
├── SKILL.md
├── references.md
├── cookbook/
│   ├── authentication.md
│   ├── crud-operations.md
│   └── realtime-subscriptions.md
└── .mcp.json

Create this skill? (Yes/No)
```

## أفضل الممارسات

### مهارات التسمية

استخدم أسماء وصفية محددة مع الواصلات:

- ✅ `supabase-swift` - تكنولوجيا وسياق واضحان
- ✅ `react-components-accessible` - الإطار والميزة الرئيسية
- ✅ `rest-api-error-handling` - المجال والتركيز
- ❌ `database` - غامضة جدا
- ❌ `api` - عامة جدًا
- ❌ `my_skill` - استخدم الواصلات وليس الشرطات السفلية

### خلق المهارات الفعالة

1. **Use specific domains** - تقنية واحدة لكل مهارة، وليست واسعة جدًا
2. **Include multiple use cases** - إضافة إدخالات كتاب الطبخ لسيناريوهات مختلفة
3. **Link to project patterns** - يجب أن يشير References.md إلى الكود الفعلي الخاص بك
4. **Add troubleshooting** - توقع ومعالجة القضايا المشتركة
5. **Keep focused** - 2-4 إدخالات كتاب الطبخ لكل مهارة

### متى يجب إنشاء مهارات جديدة؟

اصنع مهارة عندما:
- أنت تستخدم التكنولوجيا بشكل متكرر
- فريقك لديه أنماط لشيء ما
- تريد تشفير أفضل الممارسات
- أنت بحاجة إلى إرشادات خطوة بخطوة

لا تقم بالإنشاء إذا:
- الموضوع ضيق جدًا (إدخال واحد في كتاب الطبخ)
- نادرا ما تستخدمه
- المستندات الخارجية كافية

## إدخالات كتاب الطبخ

كتب الطبخ هي أدلة خطوة بخطوة لحالات استخدام محددة. يمكن أن تحتوي كل مهارة على كتب طبخ متعددة.

### هيكل كتاب الطبخ

```markdown
# Use Case Title

Brief description of this workflow.

## Prerequisites

- Prerequisite 1
- Prerequisite 2

## Instructions

1. **Step 1**: Description with example
   ```language
   مثال التعليمات البرمجية
   ```

2. **Step 2**: Next step
   ```language
   مثال التعليمات البرمجية
   ```

## Example

**Input**: Sample input or scenario
**Output**: Expected output

## Troubleshooting

- **Problem**: Solution
```

### أمثلة كتاب الطبخ

يجب أن يتناول كل إدخال في كتاب الطبخ مهمة محددة ومشتركة:

- **Supabase skill**:
  - `authentication.md` - قم بالتسجيل، تسجيل الدخول، تسجيل الخروج
  - `crud-operations.md` - إنشاء، قراءة، تحديث، حذف
  - `realtime-subscriptions.md` - الاستماع إلى تغييرات قاعدة البيانات

- **React components skill**:
  - `create-component.md` - بناء مكون جديد
  - `styling-patterns.md` - تطبيق أنماط متسقة
  - `accessibility.md` - جعلها في متناول الجميع

## التكامل MCP

إذا كانت تقنية مهارتك تحتوي على خادم MCP متاح، فإن الأمر يتضمن ذلك `.mcp.json`:

```json
{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": ["-y", "@supabase/mcp-server"],
      "env": {
        "SUPABASE_URL": "${SUPABASE_URL}",
        "SUPABASE_KEY": "${SUPABASE_KEY}"
      }
    }
  }
}
```

يسمح هذا لكلود باستخدام أدوات MCP عند العمل بالمهارة. ستحتاج إلى تعيين متغيرات البيئة حتى تعمل الأدوات.

## معالجة الأخطاء

### بحث الويب لا يُرجع أية نتائج

إذا لم يتم العثور على الوثائق عبر الإنترنت:
- يقوم الأمر بإنشاء المهارة باستخدام أنماط عامة
- يمكنك إضافة عناوين URL يدويًا إلى `references.md` لاحقاً
- يستخدم `/agileflow:skill:edit` لتعزيزها

### لم يتم العثور على خادم MCP

في حالة عدم وجود خادم MCP للتقنية الخاصة بك:
- يتم إنشاء المهارة بدون `.mcp.json`
- يمكنك إضافة MCP لاحقًا إذا أصبح الخادم متاحًا
- يتم توفير تعليمات الإعداد اليدوية لـ MCP

### أخطاء التحقق من الصحة

قبل الكتابة، يتم التحقق من صحة الأمر:
- يحتوي اسم المهارة على أحرف وأرقام وواصلات فقط
- الوصف أقل من 1024 حرفًا
- SKILL.md أقل من 500 سطر
- تتم الإشارة إلى كافة ملفات كتاب الطبخ

إذا فشل التحقق من الصحة، سيُطلب منك الإصلاح قبل المتابعة.

## بعد الخلق

بمجرد إنشاء المهارة:

1. **Test it**:
   ```bash
   /agileflow:skill:test supabase-swift
   ```

2. **Use it** - العمل على المهام وسيتم تفعيل المهارة تلقائيا

3. **Add learning** - تمكين التحسين الذاتي:
   ```bash
   /agileflow:skill:upgrade supabase-swift
   ```

4. **Improve it** - صحح مخرجات كلود وتتعلم المهارة:
   ```bash
   /agileflow:skill:edit supabase-swift
   ```

## استكشاف الأخطاء وإصلاحها

### المهارة لا تعمل

إذا بدا أن المهارة التي تم إنشاؤها حديثًا لا يتم تنشيطها:

1. تحقق من قسم "متى يتم الاستخدام" في SKILL.md
2. تأكد من أن سؤالك يتطابق مع مشغلات التنشيط
3. يجري `/agileflow:skill:test` للتحقق من الهيكل
4. تحرير مع `/agileflow:skill:edit` لضبط المشغلات

### الوثائق المحلية مفقودة

إذا لم يتمكن الأمر من العثور على ممارساتك المحلية:

1. إنشاء وثائق في `docs/02-practices/` أو `docs/04-architecture/`
2. قم بتحرير المهارة للإشارة إلى ما تقوم بإنشائه
3. استخدم كلمات رئيسية أكثر تحديدًا في وصف مهاراتك

### إدخالات كتاب الطبخ غير معروضة

إذا لم تكن إدخالات كتاب الطبخ مرئية في Claude Code:

1. يجري `/agileflow:skill:test` للتحقق من وجود الملفات
2. تأكد من أن SKILL.md يشير إليها بشكل صحيح
3. تأكد من أن ملفات كتاب الطبخ صالحة لـ Markdown

## الأوامر ذات الصلة

- [`/ المهارة: القائمة`](/commands/skill-list) - عرض جميع المهارات المثبتة
- [`/المهارة: تحرير`](/commands/skill-edit) - تعديل مهارة موجودة
- [`/المهارة:اختبار`](/commands/skill-test) - التحقق من عمل المهارة
- [`/المهارة:حذف`](/commands/skill-delete) - إزالة المهارة
- [`/المهارة:ترقية`](/commands/skill-upgrade) - إضافة القدرة على التعلم

## الخطوات التالية

1. اصنع مهارتك الأولى: `/agileflow:skill:create`
2. اختبار أنه يعمل: `/agileflow:skill:test`
3. استخدمه في عملك - سيتم تفعيله تلقائيًا
4. تحسينه مع `/agileflow:skill:upgrade`
