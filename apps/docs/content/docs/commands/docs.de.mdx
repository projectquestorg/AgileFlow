---
title: /docs-sync
description: Synchronisieren Sie die Dokumentation mit Codeänderungen
---
# /docs-sync

Synchronisieren Sie Ihre Dokumentation mit Codebasisänderungen, indem Sie neuen Code erkennen und sicherstellen, dass entsprechende Dokumentation vorhanden ist.

## Schnellstart

```bash
/agileflow:docs-sync
```

Zum Vergleich mit einem bestimmten Zweig:

```bash
/agileflow:docs-sync BRANCH=feature-auth BASE=main
```

## Parameter

| Parameter | Erforderlich | Standard | Beschreibung |
|-----------|----------|---------|-------------|
| `BRANCH` | Nein | Aktueller Zweig | Verzweigung zur Analyse auf Änderungen |
| `BASE` | Nein | main/master | Basiszweig zum Vergleich mit |
| `AUTO_CREATE` | Nein | nein | Fehlende Dokumentation automatisch erstellen (ja/nein) |

## Beispiele

### Aktuellen Zweig analysieren

```bash
/agileflow:docs-sync
```

Analysiert Ihren aktuellen Zweig anhand von `main` und meldet:
– Neue API-Endpunkte ohne Dokumentation
- Neue UI-Komponenten ohne Dokumentation
- Konfigurationsänderungen nicht dokumentiert
- Veraltete Dokumentation

### Funktionszweig vergleichen

```bash
/agileflow:docs-sync BRANCH=feature-oauth BASE=main
```

Vergleicht `feature-oauth` mit `main`, um alle Dokumentationslücken zu finden.

### Fehlende Dokumente automatisch erstellen

```bash
/agileflow:docs-sync BRANCH=feature-payments AUTO_CREATE=yes
```

Erstellt automatisch eine Stub-Dokumentation für alle fehlenden Elemente und führt einen Commit mit der Meldung `docs: sync with codebase changes` durch.

## Ausgabe

Der Befehl generiert einen umfassenden Bericht:

```markdown
# Documentation Sync Report

**Branch**: feature-auth | **Base**: main | **Generated**: 2025-12-24T10:30:00Z

## Missing Documentation

### API Endpoints (3)
- ❌ POST /api/auth/login (src/api/auth/login.ts)
- ❌ POST /api/auth/logout (src/api/auth/logout.ts)
- ❌ GET /api/auth/verify (src/api/auth/verify.ts)

### UI Components (2)
- ❌ LoginForm (src/components/LoginForm.tsx)
- ⚠️ UserProfile (src/components/UserProfile.tsx) - stub exists, needs details

### Configuration (1)
- ❌ New env var: JWT_SECRET (.env.example)

## Outdated Documentation

### Deprecated
- 📄 docs/04-architecture/api.md mentions /api/v1/login (removed in this branch)

## Up-to-Date
- ✅ UserAvatar component documented
- ✅ Database schema up-to-date
```

## Wie es funktioniert

### Änderungserkennung

Der Befehl:
1. **Vergleicht Zweige** mit `git diff`, um alle Codeänderungen zu identifizieren
2. **Kategorisiert Änderungen** in API-Endpunkte, UI-Komponenten, Dienste, Konfiguration und Datenbankschemata
3. **Zuordnung zu Dokumentationsspeicherorten**, an denen Dokumente vorhanden sein sollten

### Intelligente Schlussfolgerung

Die Dokumentation stammt aus:
- **TypeScript-Typen und -Schnittstellen** - Funktionssignaturen und Rückgabetypen
– **JSDoc-Kommentare** – Vorhandene Codedokumentation
- **Testdateien** – Anwendungsbeispiele
- **OpenAPI-Dekoratoren** – API-Spezifikationen

### Lückenanalyse

Für jede geänderte Datei prüft der Befehl Folgendes:
- **Fehlende Dokumentation**: Neu hinzugefügter Code ohne Dokumente
- **Veraltete Dokumentation**: Verweise auf entfernten oder geänderten Code
- **UnvollständigDokumentation**: Stubs, die Details benötigen

## Dokumentationszuordnung

Codeänderungen werden der erwarteten Dokumentation zugeordnet:

| Code-Speicherort | Erwartete Dokumentation |
|--------------|--------|
| `src/api/` oder `src/routes/` | `docs/04-architecture/api.md` oder OpenAPI-Spezifikation |
| `src/components/` oder `src/pages/` | `docs/04-architecture/components.md` oder Bilderbuch |
| `src/services/` oder `src/utils/` | `docs/04-architecture/services.md` |
| `*.config.js`, `.env.example` | `docs/02-practices/configuration.md` |
| `migrations/` oder `schema/` | `docs/04-architecture/database.md` |

## Best Practices

### Vor dem Festschreiben des Codes
Führen Sie `docs-sync` aus, um Dokumentationslücken frühzeitig zu erkennen:

```bash
/agileflow:docs-sync BRANCH=your-feature BASE=main
```

### Beim Hinzufügen neuer Endpunkte
Der Befehl markiert sie automatisch, sodass Sie sich die Dokumentation leichter merken können:

```bash
/agileflow:docs-sync AUTO_CREATE=yes
```

Dadurch werden Stubs erstellt, die mit „TODO: Details hinzufügen“ zur manuellen Überprüfung gekennzeichnet sind.

### Zu pflegende Dokumentationsabschnitte
Halten Sie diese mit dem Code synchronisiert:
- API-Anfrage/Antwort-Beispiels
- Komponenten-Requisitendefinitionen
- Konfigurationsoptionen
- Änderungen des Datenbankschemas
– Veraltete API-Endpunkte

## Verwandte Befehle

- [`/context`](/commands/context) – Generieren Sie eine umfassende Kontextbeschreibung
- [`/readme-sync`](/commands/readme-sync) – README-Dateien mit Ordnerinhalten synchronisieren
- [`/adr`](/commands/adr) – Architekturentscheidungen dokumentieren