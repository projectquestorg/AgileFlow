---
title: Prise en charge des langues RTL
description: Prise en charge des langues s'écrivant de droite à gauche comme l'arabe, l'hébreu, le persan et l'ourdou.
---
# Prise en charge des langues RTL

La documentation AgileFlow prend en charge les langues s'écrivant de droite à gauche (RTL), notamment l'arabe, l'hébreu, le persan et l'ourdou. Ce guide explique comment utiliser le support RTL et comment contribuer aux traductions.

## Utilisation du mode RTL

### Sélecteur de langue

Cliquez sur l'icône langue/globe dans l'en-tête du site pour basculer entre les modes LTR et RTL. Le site ajustera automatiquement :

- Sens du texte
- Mise en miroir de la mise en page
- Alignement de la navigation
- Positionnement des composants

### Détection automatique

Si votre navigateur est réglé sur une langue RTL, le site démarrera automatiquement en mode RTL. Langues RTL prises en charge :

- Arabe (ar)
- Hébreu (il)
- persan/farsi (fa)
- Ourdou (ur)

## CSS compatible RTL

Le site utilise Tailwind CSS avec des variantes RTL. Utilisez le préfixe `rtl:` pour les styles spécifiques à RTL :

```css
/* Example RTL styles */
.my-element {
  margin-left: 1rem;   /* LTR default */
}

[dir="rtl"] .my-element {
  margin-left: 0;
  margin-right: 1rem;  /* RTL override */
}

/* Using Tailwind rtl: variant */
<div className="ml-4 rtl:ml-0 rtl:mr-4">
  Content
</div>
```

### Propriétés logiques

Dans la plupart des cas, préférez les propriétés logiques CSS qui gèrent automatiquement la direction :

```css
/* Instead of this */
margin-left: 1rem;
padding-right: 2rem;
text-align: left;

/* Use this */
margin-inline-start: 1rem;
padding-inline-end: 2rem;
text-align: start;
```

Equivalents de vent arrière :
- `ms-*` = marge-inline-start
-`me-*` = marge-fin-en-ligne
- `ps-*` = remplissage-inline-start
- `pe-*` = remplissage-en-ligne-fin
- `start-*` = démarrage en ligne
- `end-*` = fin en incrustation

## Flux de travail de traduction

### Ajout d'une nouvelle langue

1. Créez des fichiers de traduction dans `/locales/{lang}/` :

```
locales/
├── en/
│   ├── common.json
│   └── docs.json
├── ar/
│   ├── common.json
│   └── docs.json
└── he/
    ├── common.json
    └── docs.json
```

2. Ajoutez la langue à `LANGUAGES` dans `components/language-switcher.tsx` :

```typescript
const LANGUAGES: Language[] = [
  { code: "en", name: "English", nativeName: "English", direction: "ltr" },
  { code: "ar", name: "Arabic", nativeName: "العربية", direction: "rtl" },
  // Add new language here
]
```

3. Créez des fichiers MDX traduits :

```
content/docs/
├── index.mdx           # English (default)
└── ar/
    └── index.mdx       # Arabic translation
```

### Directives de traduction

1. **Préserver le formatage** : conserver les blocs de code, les liens et les composants intacts
2. **Traduire le texte alternatif** : les attributs alternatifs de l'image doivent être traduits
3. **Conserver les URL** : ne traduisez pas les slugs d'URL sauf si cela est absolument nécessaire
4. **Revoir les termes techniques** : certains termes techniques peuvent rester en anglais

### Exemple de contenu RTL

Voici un exemple de texte arabe pour tester la mise en page RTL :

```arabic
مرحبًا بكم في توثيق AgileFlow. هذا النص باللغة العربية لاختبار دعم اللغات من اليمين إلى اليسار.
```

```hebrew
ברוכים הבאים לתיעוד AgileFlow. זהו טקסט בעברית לבדיקת תמיכה בשפות מימין לשמאל.
```

## Test de la mise en page RTL

1. Passez en mode RTL à l'aide du sélecteur de langue
2. Vérifiez ces zones pour vérifiermise en miroir :
   - Barre latérale de navigation (doit être à droite)
   - Alignement des icônes de recherche
   - Sens de défilement du bloc de code
   - Barres de progression
   - Icônes (certaines devraient s'inverser, d'autres non)

### Icônes qui ne doivent PAS basculer

- Icône de recherche
- Icône de lien externe
- Coche
- Plus/moins
- Fermer (X)

### Icônes qui DEVRAIENT retourner

- Flèche gauche/droite
- Chevron gauche/droite
- Retrait/retrait
- Répondre/transférer

## Implémentation technique

### DirectionProvider

Le composant `DirectionProvider` gère l'état de la direction :

```tsx
import { useDirection } from "@/components/direction-provider"

function MyComponent() {
  const { direction, setDirection, isRTL } = useDirection()

  return (
    <div className={isRTL ? "text-right" : "text-left"}>
      Current direction: {direction}
    </div>
  )
}
```

### Accès à la direction en CSS

L'attribut `dir` est défini sur l'élément `<html>` :

```css
/* CSS selector */
[dir="rtl"] .my-element {
  /* RTL styles */
}

/* Tailwind */
<div className="rtl:flex-row-reverse">
```

## Contribuer

Vous souhaitez aider à traduire AgileFlow ? Nous apprécions les contributions !

1. Vérifiez les traductions existantes dans `/locales/`
2. Créez un nouveau dossier pour votre langue
3. Soumettez un PR avec vos traductions

Pour toute question, ouvrez un ticket sur GitHub.