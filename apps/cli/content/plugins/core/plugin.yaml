# Core plugin — essential AgileFlow workflow.
# Always enabled; cannot be disabled.
# Skill content (Epic, Story, Status, Babysit) lands in Phase 4.
id: core
name: Core
description: Epic, Story, Status, Babysit — essential agile workflow.
version: 1.0.0
enabledByDefault: true
cannotDisable: true

depends: []

provides:
  commands: []
  skills: []
  agents: []
  hooks:
    - id: session-welcome
      event: SessionStart
      script: hooks/session-welcome.js
      timeout: 5000
      skipOnError: true
  templates: []
